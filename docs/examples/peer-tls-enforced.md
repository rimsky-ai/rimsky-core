<!-- Generated by /document at d977250c on 2026-08-16. A record of
that release, not a source of truth; regenerated whole at the next
release. Read it only when directed here. -->

# Requiring transport security on a peer entry

`tls: required` on a peer entry is enforced. A peer that cannot present
credentials is refused — and where the peer is a store, the deployment refuses
to start.

## Two executor entries, one peer

Both point at the same plaintext peer process. The transport setting is the only
difference.

```yaml
persistence:
  driver: sqlite
  sqlite:
    path: /var/lib/rimsky/state.db
claim_producers: {}
named_locks: {}
executors:
  "plain-off":
    transport: grpc
    endpoint: "plainpeer:9400"
    tls: "off"
    protocols: ["executor"]
  "plain-required":
    transport: grpc
    endpoint: "plainpeer:9400"
    tls: "required"
    protocols: ["executor"]
```

```bash
curl -sS "$BASE/v1/observability/executors/plain-off"
# reachability_status: reachable, tls: off

curl -sS "$BASE/v1/observability/executors/plain-required"
# reachability_status: unreachable, tls: required
```

The reported failure names both the peer and the setting rather than giving a
bare connection error:

```
peer "plain-required (plainpeer:9400)" (tls: required): ...
authentication handshake failed: tls: first record does not look like a TLS handshake
```

## The refusal reaches the work

A node dispatched at the `plain-off` entry settles fresh with a success
terminal. A node dispatched at the `plain-required` entry — the same peer, the
same process — settles failed with `terminal/error/executor_dial_failed`.

## The store side refuses to start

```yaml
claim_producers:
  "store-required":
    endpoint: "plainpeer:9400"
    tls: "required"
    protocols: ["claim_producer"]
    write_semantics_allowed: ["read_only"]
```

The container exits 1 and its log names the producer and the setting. An
operator cannot end up with a running deployment quietly talking to an
unauthenticated store: the requirement is enforced before any work is
dispatched.

## A credentialed peer satisfies it

Run the deployment under `peer_auth: mtls` with a peer that enrolls and serves
mutually authenticated TLS.

```bash
curl -sS --cacert ca.pem "$BASE/v1/observability/executors/<name>"
# reachability_status: reachable, tls: required
```

The certificate that peer presents verifies against the deployment CA — probe it
from outside with a leaf issued by `POST /v1/enroll` and the root from
`GET /v1/ca-root`. A node driven over that connection settles fresh, carrying
the credentialed peer's own writeback.

The setting is satisfiable, not merely enforceable.
