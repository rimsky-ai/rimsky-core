# Completion report: Recommended intake drain

Sprint: `2026-08-18-recommended-intake-drain.md`. The builder marks each build stage as it lands and records what it did under `## Work done`. The session marks the closing stages after the team retires.

## Stages

1. done — Corpus-only deltas and surface intent: apply the deltas no work item implements (amend `attribute`, `publisher`, `publisher-subscription`, `service-address-book`, `signal`, `wait-set`, `bundled-recipes-production-paths`, `handler-package-in-service-directory`, `licensing-dual-apache-agpl`; retire `bundled-park-resume-recipe`, `config-flip`, `untagged-prose-by-module`); the three surface-intent items (MCP tools public, Go modules, environment variables); regenerate the catalog TOCs.
2. done — Annotation repoints and enforcement-site citations: structural-root repoint (new `structural-root-edges-derived-on-demand`, retire `structural-root-edge-injection-at-registration`, amend `subscription-edges-only-from-explicit-block`); destination-allowlist citations (amend `allowlist-defaults-open`, new `destination-allowlists-default-closed`).
3. done — CLI api-key and compose progress: API key on compose verbs; API key on the publisher conformance poll; api-key universal test; printer as volume × format; frame tick from the one-shot poll loop.
4. done — Compose prefix and MCP tools: remove the `compose:` marker and the server-side reservation; MCP tools for the three unpermissioned actions (deltas `rimsky`, `tag`, `control-api`, story `compose-lifecycle`).
5. done — Persistence and validation: route node-run state writes through the next-state function (`transition-reason`); reject a role-less standalone validator (`validation`); sensor state stores on native pgx + adapter deny (`postgres-pgx-v5`); migration digest check (`migrations-append-only-numbered`, `persistence-database`).
6. done — Process lifecycle: drain-then-escalate helper in the protocols server kit; route all 22 entry points through it (`graceful-shutdown`); shared log level in every rimsky-authored process (`logging-slog-only`, `operator-env-namespaced-per-service`).
7. done — Ports and images: move core listener defaults into the core block (`default-port-allocation`); bundled images declare exactly the ports their binaries open; image-name scheme test (`service`).
8. done — Build and release: CGO off on every build; per-run image tags for verification; multi-platform image push; closing platform verify (`release-chain`, `release-distribution`).
9. done — Test determinism machinery: class marker on every admitted wait, lint classification arm, ratchet arm; align `.claude/rules/rules.md` with the plumbline testing standard (`polling-audit`, `test-wallclock-lint-ratchet`).
10. done — Convert wall-clock idioms, group 1: `test/scenarios` and `test/support` (110 sites).
11. done — Convert wall-clock idioms, group 2: `lib/services` (133 sites).
12. done — Convert wall-clock idioms, group 3: `cmd/`, `lib/runtime`, `lib/control`, `lib/protocols`, `lib/foundation` (68 sites); the baseline empties here.
13. done — Sensor and replica: retire `replica`, amend `sensor`; retire the replica posture test and reframe the deterministic-key test; sensors one-DSN scenario test.
14. done — Ported experiments: dev-loop shortcut verbs; `compose plan` previews `compose up`; `read-only` role covers every read; all-in-one volume persists across replacement.
15. done — Finish the completion report.
16. done — Run `/certify-work` with this sprint's path as its argument.
17. done — Walk the presentation.
18. done — Offer archive-and-commit.

## Work done

### Stage 1 — Corpus-only deltas and surface intent

Applied the six concept amendments (`attribute`, `publisher`, `publisher-subscription`, `service-address-book`, `signal`, `wait-set`) and the three decision amendments (`bundled-recipes-production-paths`, `handler-package-in-service-directory`, `licensing-dual-apache-agpl`) verbatim from the sidecar. Deleted the three retired artifacts: `story:bundled-park-resume-recipe`, `decision:config-flip`, `decision:untagged-prose-by-module`. No source file and no design file cited a retired slug. A reference scan over the nine applied bodies and the surface intent found no dangling `concept:` / `decision:` / `story:` reference, so citation resolution stays green. Wrote the three surface-intent items into `.ok-planner/surface/surface.md`: a new `## MCP tools` class rule beside the HTTP-routes rule, the `## Go modules` section between `## The general rule` and `## CLI verbs`, and the replacement body for `## Environment variables`. The sprint fixes the wording of the last two, and both carry it verbatim. Regenerated the three catalog TOCs. Three concept lines follow the amended `## What it is` first sentences, three decision lines follow the amended Choices, and three retired lines are gone. Each TOC's line count equals its directory's file count at the close of this stage: 75 concepts, 235 decisions, 122 stories. The tree held 237 decisions before the sprint, and this stage retired two of them; stage 2 moves the count to 236. This stage is prose only, so it carries no test.

### Stage 2 — Annotation repoints and enforcement-site citations

Applied five decision deltas from the sidecar: the new `structural-root-edges-derived-on-demand`, the amended `subscription-edges-only-from-explicit-block`, the amended `allowlist-defaults-open`, the new `destination-allowlists-default-closed`, and the retirement of `structural-root-edge-injection-at-registration`. Repointed all eleven `@decision:` annotations carrying the retired slug across the five live files: the graph module's subscription-edge builder and its test, the CLI's structural-root helper, the scenario harness, and the empty-message-wake scenario test. No annotation anywhere in the tree still carries it. Cited `destination-allowlists-default-closed` at four sites: the shared guard's `NewGuardFromEnv`, where an unset variable yields a closed guard, and the three services that construct one from their own operator variable — the http-node executor, the verifier-http executor, and the http sensor. The decisions TOC gained two lines, lost one, and carries rewritten lines for the two amended decisions. Its 236 lines match its 236 files, and the list stays alphabetical.

The proof is the tests the items name, all run with `-count=1` through the progress guard. `lib/graph/node` passes, including the structural-root injection tests. The `empty_message_wake` scenario passes. The egress guard's own unit tests pass, covering the closed default and the malformed-entry refusal. `go build ./...` passes in both modules, `make lint` is clean, and the plumbline lint reports no violation over the nine touched source files, so citation resolution stays green.

### Stage 3 — CLI api-key and compose progress

The four compose verbs now send the key they already parse. `clientForManifest` and `RunComposeDown` each built a client their own way, so both now build one through the same `clientForContext` helper. The helper resolves the key by the concept's precedence and sets it on the client. The publisher conformance verb accepts the same key flag and hands the resolved key to the goroutine that polls the control API for the pushed message. The key flag itself had eight definitions across the tree. It has one now, `RegisterAPIKeyFlag`, which `RegisterCommonFlags` and the seven auth verbs all call. The resolution precedence likewise has one definition, `ResolveAPIKey`, and the auth verbs' own resolver delegates to it.

The compose one-shot's progress printer selects on two axes instead of one. A volume gate — quiet, default, verbose — sits in front of one of two formats, prose or JSON records, so the JSON format no longer overrides quiet or verbose. Each volume reports the same event set under either format. The poll loop reports each newly observed frame. `instanceIsIdle` hands back the running frames it read, and a frame ticker numbers each frame id it has not seen before. Verbose output therefore differs from default output. The verbose flag's help text now names only the frame ticks it emits.

New tests prove each item. `TestComposeVerbsSendTheKeyGivenOnTheFlag` and `TestComposeVerbsFallBackToTheKeyInTheEnvironment` drive up, plan, status and down against a control API stub that demands a bearer token, and assert every request carried it; a third case asserts the stub refuses the same verb when no key resolves, so the gate is live. I removed the `SetAPIKey` call, watched all four verbs fail, and restored it. `TestEveryVerbBuildingAControlAPIClientSendsTheResolvedKey` walks the CLI's call graph, finds the 45 verbs that build a control-api client, and fails any that defines no key flag or sends none; it carries the concept's exceptions as its own list and fails when an exception names a function that no longer exists. It caught eight verbs whose key flag the walk could not see. That finding drove the flag onto one definition. `TestQuietVolumeUnderJSONReportsOnlyTheSummaryRecord` asserts quiet with json emits the summary record and nothing else, and `TestWaitReportsEachNewlyObservedFrameAsATick` drives the poll loop against a stub reporting a second frame and asserts two tick lines under verbose and none under default. `./cmd/...` and `./test/plumbline/...` pass with `-count=1`, `go build ./...` passes in both modules, `make lint` is clean, and the plumbline lint reports no violation over the twenty-two touched source files.

### Stage 4 — Compose prefix and MCP tools

Compose keeps the manifest's project name as its only prefix, and the server reserves nothing. `PrefixedTag` and `PrefixedInstanceKey` now build `<project>:<name>`, and the state query and the apply plan scan that prefix. `bareInstanceName` strips up to the first colon instead of two segments. The CLI's `ReservedTagPrefix` constant is gone, and so are the four guards that consulted it: tag create, tag move, tag remove, and template register. The manifest no longer rejects a tag carrying the marker either. The client no longer carries a compose-origin flag or stamps `X-Rimsky-Compose-Origin`. On the server, `compose_prefix.go` and its test are deleted. The three reserved-prefix checks go with them, on tag create, on template registration with a tag, and on instance create. The `compose:origin` action leaves the registry and the two pins in `actions_test.go`. No bundled role definition named it. The scenario that pinned the reservation is deleted. Three auth helpers it happened to host serve `mcp_transport_parity_e2e_test.go` too, so they moved to `auth_key_helpers_test.go`.

The three unpermissioned control-api actions have their MCP tools: `health_probe`, `auth_whoami`, and `peer_auth_ca_root`, each with an input schema. The catalog now lists a tool by its action's recorded posture rather than by a permission grant. `mcp.RegistryEntry` carries the posture the adapter reads off the action registry, and only a permissioned action consults the grant. The CA-root tool lists unconditionally, as the enrollment and observability tools already do. `routedActionsDeliberatelyWithoutAnMCPTool` is down to `mcp:read` alone, and that parity test is the proof.

Two control-api tests changed polarity rather than disappearing: `TestCreateTag_ReservesNoPrefix` and `TestTemplateRegister_ReservesNoTagPrefix` now assert the server accepts a `compose:`-prefixed name from a plain caller. The manifest validator's test does the same for the CLI side. The four CLI tests that pinned the client-side rejection are deleted. The compose scenario gained the case the item names: another client creates a tag under the project prefix, and the test asserts `compose status` lists it, `compose plan` reports it as drift, and `compose down` tears it down, while the unprefixed manual tag and its template survive. `./cmd/...`, `./lib/control/...` and `./test/plumbline/...` pass with `-count=1`; `go build ./...` passes in both modules and `make lint` is clean. `TestCLICompose_UpThenDown` passes against a freshly built image set. Its teardown log removes `project-alpha:foreign@1` beside compose's own two tags, so compose tears down a foreign tag under its project prefix exactly as it tears down its own. No name in that log carries the retired marker. Each edit moves the content-addressed image tag. Reproducing this run therefore takes `make core-images service-images test-images` over a frozen tree, then `RIMSKY_IMAGE_TAG=src-51fab6abb1cc` on the test invocation. Stage 8's per-run tag item removes that coupling.

### Stage 5 — Persistence and validation

Every write of a node-run's state column now carries a reason and takes its target from the switch, in both backends. The dispatcher's promotion writes `cascade.NextState(stale, dispatch_claimed)` rather than a literal `'running'`. The two claim-release writers read the row's current state, ask the switch under a new `dispatch_released` reason, and compare-and-set on that state, so a write whose row moved underneath it matches nothing and changes nothing; the switch models the release from every in-flight state and refuses it from a settled one. `ForceReleaseClaim`, which no shipped code called, leaves the queue interface, both backends, three test fakes, and the force-override conformance case.

The run-tree writer split in two, because one of its callers was not changing state at all. `UpdateStateAndOutcome` re-wrote the state that `Nodes().UpdateState` had just routed, at all three settlement sites; that call is now `UpdateOutcome`, which writes the settling signal and the changed flag and never touches the state column. The genuine state change — the parent's aggregate write — is `UpdateAggregateState`, which reads the current state, calls `NextStateParent` with the reason, and persists what the switch returns. The `parentAggregateOK` sentinel and the `child_transitioned` reason are gone with it: the switch no longer tells the caller to choose a target. `AggregateSettledReason` maps the aggregation's verdict to `aggregate_settled_success` or `aggregate_settled_failure`, and the parent arm returns fresh or failed for those. The subgraph caller's running-stay write routes the same way under its own reason, which the switch still admits only from running.

The deployment load now refuses a standalone validator entry whose protocols list leaves nothing after the validation protocol, and the error names the entry. That refusal covers both an entry declaring only `validation` and an entry declaring no protocols at all, which the loader defaults to the same thing. One fixture in `peer_auth_test.go` declared such an entry and now names a role.

The four sensor state stores open Postgres through `pgxpool` and the native interface. The lint denies `github.com/jackc/pgx/v5/stdlib` repo-wide with no exemption, and nothing in the tree imports it; the rule names the adapter package alone, so the SQLite backend's use of `database/sql` and the Postgres queue-park code's null types are untouched.

The migration runner records a sha256 of each applied file beside its filename and refuses the next boot by name when a recorded file's contents no longer match. Each driver's bootstrap adds the column — `ADD COLUMN IF NOT EXISTS` on Postgres, a `PRAGMA table_info` check then `ADD COLUMN` on SQLite — so the migrations table needs no migration of its own, and the runner backfills a row applied before the digest existed on the boot that finds it rather than rejecting it.

The proof is in the shared persistence conformance suite, which pins both backends with three cases: `RunStateWritesRouteThroughTheSwitch` (a legal promotion and a legal release; a reason the switch models only from running refused at the writer with nothing written; the aggregate write persisting the switch's target, then re-projecting a settled parent), `DispatchReleaseClaimRefusesTerminalRun`, and `MigrationRejectsAChangedFile`. Both suites pass against real Postgres and SQLite with `-count=1 -timeout 0`. The state machine's own table test grew the release arm and the aggregate arms; the migrator's unit tests grew the rewritten-file refusal and the backfill. `./lib/...`, `./cmd/...`, `./test/scenarios/auth`, `./test/scenarios/empty_message_wake`, and `./test/plumbline/...` pass, the `lib/services` sensor suites pass against a live Postgres, `go build ./...` passes in both modules, and `make lint` is clean.

Two reviewer findings from stage 4 landed here. L6: the parity scenario asserted an `mcp:read`-only key sees zero tools, which stage 4 made false; it now asserts that key sees exactly `health_probe`, `auth_whoami`, and `peer_auth_ca_root` and no permissioned tool, and the catalog's unit test carries a posture-exempt entry beside three permissioned ones. `TestMcpTransportParity` passes against a freshly built image set. L7: `mcp.RegistryEntry` carries `RequiresPermission bool`, decided in `controlapi` beside the identical decision in `actions.go`; the duplicated posture constant and the string comparison are gone.

### Stage 6 — Process lifecycle

The drain-then-escalate helper now lives in the protocols module's server kit, at `lib/protocols/serverkit/shutdown.go`, where the root module and the bundled-services module can both import it. It carries the three grace windows as named values — `CLIChildGrace`, `BundledServiceGrace`, `DeployedCoreGrace` — and three entry points: `NotifyShutdownSignals` for a path that owns its own drain, `InstallSecondSignalHardExit` for the escalation itself, and `ShutdownContext` for a path whose drain is a cancelled context. `InstallSecondSignalHardExit` returns a channel it closes when the escalator retires, so a test observes the retirement instead of assuming it. The foundation module no longer carries a copy: `shared.InstallSecondSignalHardExit`, `shared.HardExitCode`, and `shared.ParseLogLevel` are deleted, and every caller reads the serverkit definitions. `BundledServiceGrace` replaces `serverkit.DefaultStopBudget`. Both name ten seconds; the new name is the decision's.

All 22 production entry points run through the helper. Four installed the escalation before; eighteen did not. The roleboot bootstrap, the container entrypoint, and the compose one-shot's wait phase kept their existing shape and now call the serverkit helper. Fourteen sites gained the escalation: the host-agent proxy, the host agent, the migrate binary, the CLI's agent-foreground verb, the four long-running read verbs (instances, messages, run, watch), the claim-producer shared runner behind both bundled producers, and the nine remaining bundled-service mains. The compose and template one-shots gained the escalation during boot, where they had none. `watchBootSignal` arms the escalator after it cancels, and a `SpawnedRegistry` hands the escalator the children the boot spawned after the watcher started, so the hard exit kills them. The two bundled claim producers converged from a five-second gRPC stop budget onto the ten-second bundled-service window. The host agent's `RIMSKY_AGENT_REAP_GRACE_SEC` stays the one operator-tunable window and needed no change. No production file under `cmd/` or `lib/` calls `signal.Notify` any more; the stubs under `test/support/` and `lib/runtime/hostagent/testdata/` still do, and the check skips those trees.

Every rimsky-authored process takes its level from `RIMSKY_LOG_LEVEL`. Eleven bundled services, the CLI's two self-host launchers, and the CLI-started host agent pinned `slog.LevelInfo` or installed no handler. Each builds its logger with `serverkit.NewJSONLogger` now. Two processes load and default the level in their own configuration — the host agent and the host-agent proxy — and build theirs with `serverkit.NewJSONLoggerAtLevel(serverkit.ParseLogLevel(cfg.LogLevel))`. The CLI's foreground agent installed no handler and logged through the standard library's default text handler; it installs the JSON handler now.

Three proofs cover the stage. The helper's contract suite at `lib/protocols/serverkit/shutdown_test.go` drives a fake signal channel and observes the escalation callback, and no verdict reads a wall clock: a second signal fires the hard exit and logs at warn; a retired escalator ignores a later signal, which the test reads off the retirement channel rather than a wait; the first signal cancels the watcher's context and the second escalates; a released watcher cancels without firing. `test/plumbline/process_lifecycle_test.go` carries two fitness checks. The first enumerates the process entrypoints and fails any that builds its own handler or takes a level the variable cannot reach. Its population is eight core files plus the eleven bundled-service mains, which it finds by walking the four service families for `func main()` and fails when that walk counts other than eleven. The second fails any file under `cmd/` or `lib/` that installs its own signal handler. I probed the second by adding a `signal.Notify` call to `cmd/internal/roleboot`, watched it fail naming the file, and removed the probe. `./lib/...`, `./cmd/...`, `./test/plumbline/...`, and the services module's four shipped families pass with `-count=1 -timeout 0` against a freshly built image set; `go build ./...` passes in both modules and `make lint` is clean.

Three reviewer findings from stages 4 and 5 landed here. L8: `mcp.RegistryEntry.RequiresPermission` is now `ExemptFromPermission`, so the zero value fails closed. `mcp_route.go` sets it as `e.ResolvedPosture() != PosturePermissioned`, and `Catalog.Filtered` reads `!entry.ExemptFromPermission`. The catalog test's fake carries the flag on its posture-exempt entry alone and omits it on the three permissioned entries, which proves the polarity. L9: the `pending` arm for `dispatch_released` is gone from `NextState`, and the release test now walks the four states a claimed run can occupy and asserts `pending` is refused beside the two settled states. L10: the two aggregate arms of `NextStateParent` now consult `current` through `aggregatingParentState` and refuse the reasons the switch cannot model for a parent.

### Stage 7 — Ports and images

Shipped default ports now come from two reserved blocks, and one package names the core half. `lib/foundation/ports` carries both block bounds and the four core defaults — control API 8080, supervisor async-callback 8081, host-agent proxy agent-facing 8090, peer-facing 8091 — and the three core sites read them: `lib/control/launch/controlapi.go`, `lib/control/launch/supervisor.go`, and `cmd/rimsky-host-agent-proxy/config.go`. The proxy moved off 9090 and 9091, which are the claude-agent and http-node gRPC defaults. The supervisor's callback listener had no built-in default — an absent `callback.port` bound an ephemeral port, and only the all-in-one image baked 9100, the filesystem producer's gRPC default. Its port field is now `*int`, so an absent key takes 8081 and an explicit `port: 0` still asks the operating system for one. Both CLI self-host launchers write that explicit zero. The all-in-one's baked supervisor config and the CLI's synthetic copy both moved to 8081, and so did the services harness, which maps that port off the all-in-one container and renders it into the split stack's supervisor YAML.

Every bundled service now declares its defaults as `default<X>Port` constants, which makes the population enumerable: the four sensors, the four executors, and the two claim producers. Openlineage declares none, because it opens no listener. No bundled default moved. Four images changed what they declare: the postgres claim producer dropped 9121, an admin port no operator-free default opens; http-node added the 9092 HTTP bridge it binds one above its gRPC port; and the two verifier images gained the gRPC ports they bind, 9095 and 9096.

Three fitness checks prove it, and I probed each. `TestEveryShippedDefaultPortSitsInItsBlockAndCollidesWithNothing` reads the four core constants and parses every bundled service's constants out of the source, then fails a default outside its block and a number two sites share; moving sensor-cron to 8081 made it name both faults. `TestEveryBundledImageDeclaresExactlyThePortsItsBinaryOpens` compares each image's `EXPOSE` set against those constants and named the same service. `TestEveryShippedServiceImageNameIsDerivableFromItsKindAndName` reads the fifteen image names out of the Makefile's two build targets, matches the eleven service images against `rimsky-<kind>-<name>`, asserts all four kinds appear, and asserts the remaining four are exactly the core images and name no kind; it ports the held probe `assumption-image-names-follow-one-scheme` and boots nothing.

The sweep covered the config defaults, the two baked supervisor configs, the four Dockerfiles, the services harness, the callback-advertise unit tests, the address-book test's proxy endpoint, and the CLAUDE.md gotcha. That gotcha gained a paragraph naming the two blocks and the explicit-zero rule. The `docs/` tree is `/document`'s release-stamped snapshot, not a source of truth, so it stays for the next documentation run. `TestResolveSupervisorConfig_CallbackPortDefaultsIntoTheCoreBlockAndZeroStaysEphemeral` pins both halves of the callback default. `go build ./...` passes in both modules, `make lint` and the plumbline lint are clean, and every suite passes against a freshly built image set: `./lib/...`, `./cmd/...`, `./test/plumbline/...`, `./test/scenarios/...`, the services module's four shipped families, and the whole services scenario suite — the all-in-one and split-topology stacks among them, which is the proof the moved callback port works, because the harness resolves the container's mapped 8081 at boot.

Nine reviewer findings from stage 6 landed here. L11: `cmd/rimsky/main.go` installs `serverkit.NewJSONLogger` as the process default, so the CLI's own shutdown lines are JSON at the operator's level, and the file joins the entrypoint population. L12: the core half of that population is discovered — the check walks `cmd/` for `func main()` and admits a binary that installs the handler itself or delegates to `roleboot.Main`, so a new role binary that pins info fails. L13: a new arm asserts every package taking its own signal channel also calls `serverkit.InstallSecondSignalHardExit`; I probed it by disabling that call in the compose package and watched it name the package. L14: `ParseLogLevel` parses case-insensitively, reports whether it recognised the token, and `NewJSONLoggerForLevel` emits `LOGGING.LEVEL.UNRECOGNIZED` naming the variable, the value, and the accepted set — `NewJSONLoggerAtLevel` is gone and all four sites share the one parse path. L15 and L16: the compose one-shots now hold one `SignalEscalation`. The run function creates it and a deferred call retires it after `coord.Drain` returns, so it spans boot, wait, and drain with no hole. `Arm` is idempotent, `watchBootSignal` arms before it cancels, and the inner `escalatorDone` is gone. L17: `waitDrainTimeout` is `serverkit.CLIChildGrace`. L18 and L19: the stage-6 paragraph's `signal.Notify` claim is narrowed to production files under `cmd/` and `lib/`, and D7's closing sentences defer to the state set F2 settled.

### Stage 8 — Build and release

Every build this tree runs disables CGO. The four host targets a developer invokes — `build`, `build-all`, `cli`, and the containerized `build-docker` — set `CGO_ENABLED=0`, joining the eighteen Dockerfiles and the goreleaser build that already did. `TestEveryBuildInvocationInTheTreeDisablesCGO` is the widened fitness check. It walks the tree for Dockerfiles and reads out of each one every line that compiles Go. It fails a line missing the flag, and it fails when the number of Dockerfiles compiling Go is other than the eighteen it enumerates. It then reads the Makefile the same way, and reads the goreleaser build's environment. I probed it by dropping the flag from the `cli` target, and it named the line. I removed the CGO arm from `TestImagesAreTwoStageStaticNonRoot`, which checked three Dockerfiles.

Verification runs on per-run image tags. `.ok-workspaces/bin/run-tag` prints `run-<12 hex>`, fresh on every call; the Makefile mints one lazily per invocation, exports it as `RIMSKY_IMAGE_TAG`, stamps it on every image `core-images`, `service-images` and `test-images` build, and passes it as the all-in-one's base-image argument and as the fake-claude image's base tag. A tag already in the environment takes precedence, so a build in one process and a suite in another share one run's images. I deleted `tools/image-src-tag.sh`, the `org.rimsky.src-tree` label, the `SRC_TAG` variable and the `check-image-freshness` target. `make reap-images` now prunes by age alone: a per-run tag is never reused, so no tag is worth keeping, and I deleted the keep-list that spared `:latest` and `:$(VERSION)`. `code:test/support/imagetag/imagetag.go::Ref` reads the variable and appends it, with no derivation and no fallback; unset, it fails naming the variable, the run-tag script, and the build command. Its unit tests pin four properties: the tag resolves, an unset tag fails loudly naming the variable and the build command and never offering `:latest`, the missing-image error names both and wraps the docker failure, and the run-tag script prints a fresh `run-<12 hex>` on each of five calls. CI's services shard mints a tag, builds every image the suites consume under it, and exports it to the test step.

Every published tag is a multi-platform index. `BUILDX_PUSH` carries `--platform $(PUBLISH_PLATFORMS)` — `linux/amd64,linux/arm64` — alongside the SBOM and provenance flags it already had. The push order that already put `rimsky` before `rimsky-all-in-one` is now required, because the all-in-one resolves its base as an index. `make verify-published-platforms` closes the release chain: `tools/verify-published-platforms.sh` reads each pushed tag's raw manifest back through `docker buildx imagetools inspect` and fails naming the tag and the platform it lacks. `TestReleaseChainOrder` carries the new closing step and `TestEveryPushedTagIsAMultiPlatformIndexAndTheChainReadsItBack` pins the matrix, the flag, the target and the read-back. D16 records what the local registry proved and what only a real release can.

I applied the corpus deltas for `decision:release-chain` and `decision:release-distribution` verbatim from the sidecar, and the decisions TOC carries both rewritten lines; its 237 lines match its 237 files.

The proof: `TestEveryBuildInvocationInTheTreeDisablesCGO`, `TestReleaseChainOrder`, `TestEveryPushedTagIsAMultiPlatformIndexAndTheChainReadsItBack` and the `test/support/imagetag` suite pass with `-count=1 -timeout 0`; `go build ./...` passes in all four modules; `make lint` and the plumbline lint are clean. I exercised the per-run tag path for real: I minted one tag, built all nineteen images under it, exported `RIMSKY_IMAGE_TAG`, and ran the services harness unit tests and a services scenario against that tag. I exercised the loud-failure path too: an unset variable and a tag naming no image each failed naming the variable and the build command.

Five reviewer findings from stage 7 landed here. L20: `SignalEscalation.ArmOnFirstSignal` watches the signal channel for the whole drain that follows a successful wait, logs the first signal it sees, and arms the escalator, so the operator's second interrupt hard-exits; The success arm of `waitOneShotToTerminal` installs it, and that arm is the one path that armed nothing and left the channel unread. I built the reviewer's candidate (b) rather than (a). Arming unconditionally would make the first interrupt during a success drain a hard exit, and every other path drains on the first interrupt. `TestArmOnFirstSignal_ObservesTheFirstSignalDuringDrainAndHardExitsOnTheSecond` drives it in a subprocess: the first signal is observed off the returned channel — a bare receive, no wall clock — and the second exits 130. L21: both claim producers' default-port constant blocks carry the `@decision: default-port-allocation` citation, so all ten port-declaring bundled services carry it. L22: the synthetic-supervisor test decodes `callback.port` into a `*int` and fails when the key is absent, so it distinguishes the explicit zero from an omission, and a new table test in `lib/control/launch` decodes both YAML texts through the real config type and asserts the explicit zero stays ephemeral while the omitted key takes the core-block default. L23: `TestEveryCoreDefaultDeclaredInThePortsPackageIsEnumeratedByCoreDefaults` parses the `ports` package's exported integer constants and fails any that `CoreDefaults()` does not enumerate, and fails a count mismatch in the other direction; I probed it by adding a fifth constant and watched it name both faults. L24: the unreachable empty-name branch in the image-name test is deleted.

### Stage 9 — Test determinism machinery

Every wait the wall-clock lint admits now declares its class. The marker is `//nolint:testwallclock-<class>` followed by a justification, on the wait's own line or the line above, with three classes: `outcome` (the loop exits only on success), `pacing` (the delay is not a verdict input at all) and `ordering` (the pass depends on catching a transition). The lint fails an `ordering` wait and names the event-log tail as what to block on instead. It fails a wait carrying no class. It fails a marker whose class it does not know, and one carrying no justification. It refuses an `outcome` claim on a construct that fails when its deadline expires — `require.Eventually`, `for time.Now().Before(...)`, `for time.Since(...)` — because only the `case <-time.After` arm can pace a loop rather than end it. The retired unclassified marker `//nolint:testwallclock` fails by name, and I swept the tree's three sites onto the new vocabulary: the services harness's health poll is an `outcome` wait, and the compose stub's simulated delay and shutdown grace are `pacing`.

The ratchet now separates a backlog from a bad declaration. `scan.Violation` carries a kind, and only the unclassified backlog is baselineable: an ordering-dependent wait, an unknown class, a missing justification, an outcome claim on an expiring construct, and the retired marker each fail the gate at once and can never be recorded. `CountsByFile` counts the backlog alone, and `go run ./tools/wallclock-lint` refuses to write a baseline while any non-baselineable violation stands. That split is what keeps the lint green across stages 10 to 12. Today's 234 unclassified sites sit in the baseline exactly as before, and this stage recorded no new baseline, because the count did not move. Each group's conversion drops its files' counts, so each of those stages re-records the baseline and the second ratchet arm stays satisfied. Stage 12 drains the last group, and the baseline empties. A new ordering-dependent or unclassified wait fails the gate at any point in that sequence, rather than waiting for review.

The proof is two suites. `tools/wallclock-lint/scan/scan_test.go` is the scanner's contract suite: an unclassified wait fails, an outcome wait passes on its own line and on the line above, an ordering wait fails and names the event-log tail, a pacing marker admits a wait that is not a verdict input, an outcome claim on each of the three expiring constructs fails, an unknown class and a missing justification each fail, the retired marker fails and its message names all three classes, and only the unclassified kind is baselineable. It also pins that the scanner skips its own package, because every marker and construct in that file is input data rather than a wait. `test/plumbline/wallclock_ratchet_test.go` keeps the baseline arm and grows the classification arm, which fails on any non-baselineable violation anywhere in the tree; I probed the arm by marking a real test file `ordering`, and it named the file, the line and the event-log tail.

The project's determinism rules now state only what the plumbline testing standard leaves to the project. The section opens by pointing at `.ok-plumbline/docs/testing.md` and naming the subjects the standard settles, without repeating a rule of it. It then states this project's own seven: the class marker and its vocabulary, the `tools/gotest-guard.sh` backstop with `RIMSKY_TEST_NO_PROGRESS_SECS`, `-timeout 0` and its inconclusive exit code, concurrency caps that name their contention, the `-race` ban, the `Clock` abstraction, per-test isolation, and deterministic seeds. Each rule appears once.

I applied the corpus deltas for `decision:polling-audit` and `decision:test-wallclock-lint-ratchet` verbatim from the sidecar and rewrote both TOC lines; the decisions TOC's 237 lines match its 237 files.

Verification: `go run ./tools/wallclock-lint` reports the same 234 violations over the same 115 files and rewrites an identical baseline; `test/plumbline` and `tools/wallclock-lint/scan` pass with `-count=1 -timeout 0`; `go build ./...` passes in all four modules; `make lint` and the plumbline lint are clean.

Three reviewer findings from stage 8 landed here. L25: the CLAUDE.md release-flow paragraph names the closing `verify-published-platforms` step, the `--platform linux/amd64,linux/arm64` flag, the push ordering the index resolution requires, and the read-back; the image-builds paragraph names the run tag as the all-in-one's local base and the registry version as its push-path base. L26: `test/plumbline/release_platform_verify_test.go` drives the verify script over a stub `docker` on `PATH` and four canned payloads: a two-platform index carrying attestation manifests and both JSON key orderings, a single-platform index, a bare manifest that names no platform, and a tag the registry does not hold. The first passes. Each of the other three fails and names the tag it read and the platform it lacks. A fifth case gives the script one good image and one bad one, and proves the good one does not hide the bad. The suite needs no registry and no network. The static-text assertions over the script are gone; what remains is the Makefile wiring that points the target at the script the suite drives. L27: `goCompileLines` joins backslash continuations before scanning, so `RUN CGO_ENABLED=0 GOOS=linux \` followed by `go build` reads as one invocation, and a Dockerfile setting `ENV CGO_ENABLED=0` at file level satisfies the rule for every compile in it. I probed all three shapes: the continuation form and the file-level `ENV` form pass, and a Dockerfile with neither is named.

### Stage 10 — Convert wall-clock idioms, group 1 (`test/`)

The lint changed first, because it defines what a conversion is. It reads a fifth construct and refuses two claims it used to admit. `scan.Violation`'s `KindNotAnOutcome` is now `KindInadmissible`: a marker of **any** class on `require.Eventually`, `for time.Now().Before(...)`, or `for time.Since(...)` fails, because each of those ends the test when its deadline expires and no class claim can make that not a verdict. A `case <-time.After(...)` is read by its arm's body — `selectArmEndsTheTest` reads from the `case` line to the next `case`, `default:`, or closing brace and refuses the claim when the body calls a fail, a skip, a `require`/`assert`, or returns a timeout error — so only an arm that lets the loop carry on is admissible. A new `sleep` detector reads `time.Sleep(...)`, which brings every sleep in test code under the same declaration rule. Adding the detector raised the population from 234 sites over 115 files to 471 over 181, recorded as a new baseline before the drain began. `tools/wallclock-lint`'s `-grow` flag and its guard are deleted: the amended decision empties the baseline and stages 10 to 12 only shrink it.

The `test/` group then went from 218 sites to zero. The conversion has one idiom and two instruments. `test/support/awaited.Until(t, awaited, ready)` is the poll-until-success helper. It loops on `ready()` and returns only on success, so no elapsed-time value decides the verdict. It carries its own contract suite. Every `require.Eventually` in the tree (50 sites) and every deadline-bounded poll (54) became one of these, and the sleeps inside those loops went with them. The harness's typed waiters (`WaitForNodeState`, `WaitForEventCount`, and their four siblings) kept their own loops, because each reports its own observable when a run hangs; their cadence sleeps carry `outcome` markers.

The second instrument serves the sites that watched a fixed window and required nothing to happen in it. The scheduler now exposes its own progress: `scheduler.Handle.TicksCompleted()` counts completed ticks, `config.SchedulerHandle` carries it, and `Harness.WaitForSchedulerQuiescence` blocks until the scheduler completes three consecutive ticks over an unchanged world — the fingerprint reads run rows, in-flight run rows, frames, open frames, undelivered messages, and events, and any change restarts the count. Seventeen such windows became that call plus one assertion. Where the test itself holds the world still — a stub `HoldUntil`, a paused instance — the fingerprint stops changing while the gate holds, so the test's own release bounds the wait rather than a clock.

Sixteen markers remain across the group. Nine are `outcome`, each on the poll cadence of a loop that exits only on success. Seven are `pacing`: the stub's script-declared delay, the stub's best-effort probe backoff, the compose stub's two, and three teardown graces that kill a child process and reach no verdict.

Two conversions needed a new observable in the product. `test/support/executors/stub` gained a `Holding()` counter, so `TestHoldUntilBlocksUntilSignal` waits for the dispatch to enter the hold, checks without blocking that it has not returned, releases the hold, and takes a bare receive; it used to sleep 80ms instead. The harness's own 90-second `context.WithTimeout` is gone, because it fed elapsed time into every scenario's verdict, and `HarnessOpts.Deadline`, which nothing set, went with it.

The reviewer's fifth line landed here too: `test/plumbline/build_chain_test.go` now tracks the current `FROM` stage, so a runtime-stage `ENV CGO_ENABLED=0` no longer exempts a builder stage's compile, and `TestAStageLevelCGOEnvNeverExemptsAnotherStagesCompile` pins all three shapes over a synthetic two-stage body.

Verification: `go run ./tools/wallclock-lint` reports zero violations under `test/` and re-recorded the baseline at 253 over 83 files, all of them in the stage-11 and stage-12 trees; `test/plumbline`, `tools/wallclock-lint/scan`, and `test/support/awaited` pass; the whole of `./test/...` passes against real Postgres with `-count=1 -timeout 0` through the progress guard, as do `./lib/runtime/scheduler` and `./lib/control/config`; `go build ./...` passes in all four modules; `make lint` and the plumbline lint are clean; `go vet -tags smoke ./test/smoke/all/` builds.

**The three idioms stages 11 and 12 reuse.**

- `awaited.Until(t, awaited, ready)` in `test/support/awaited` replaces a `require.Eventually` or a deadline poll. It returns only when `ready()` reports success. Tests under `lib/services/test/` import it, because that module's `go.mod` requires the root module and the isolation lint exempts its test tree. A shipped `lib/services` package cannot import it. Neither can `lib/protocols`, whose `go.mod` names no rimsky module. A test in either place writes the same poll-until-success loop in its own package.
- `Harness.WaitForSchedulerQuiescence()` in `test/support/scenario` replaces a fixed window a test watched to prove nothing happened. It returns once the scheduler completes three consecutive ticks over an unchanged world fingerprint. It serves a test driving the root scenario harness. A test driving another stack needs the same wait, built on the progress that stack exposes.
- `eventwait.WaitForEvent(ctx, t, db, eventwait.Matcher{...})` in `test/support/eventwait` blocks on the event-log tail. A test whose pass depended on catching a transition waits on the durable event row instead of sampling for it.

### Stage 11 — Convert wall-clock idioms, group 2 (`lib/services`)

The `lib/services` tree went from 133 sites over 42 files to zero. The baseline now records 119 sites over 41 files, all of them in stage 12's trees. The conversion reuses the stage-10 idiom on both sides of the isolation boundary. Tests under `lib/services/test/` import `test/support/awaited`. A shipped package's test cannot import it, so `lib/services/internal/awaited` carries the same poll-until-success helper inside the module, with its own contract suite. F4 records that call. Every ad-hoc deadline poll in the module became `awaited.Until`, and the sleeps inside those loops went with them.

Twelve sites asserted that nothing happened over a fixed window. Each one now waits on progress its own stack exposes. The sensor-http scenario counts the sensor's polls at the fake upstream, so it asserts that the message count holds while the sensor completes five more polls. `requirePublisherMessageCountStableWhile` takes that progress as a `sensorProgress` value, and the object-store scenario supplies the same shape from its own `last_poll_at` column. The sensor-cascade scenario builds `waitForDispatchQuiescent` on the poll counter, restarting the count on any change, the same shape as `WaitForSchedulerQuiescence`. The webhook scenario needs no window: the sensor acknowledges only once its outcome is definitive, so a 404, a 401, and a dedup ack each settle the count as they land. Those three checks are single reads now, and so are the idempotency scenario's counts and the scopes-conflict row count.

Four sites needed a different anchor rather than a different clock. The sensor-http restart proof used to sleep two poll intervals to show that nothing polled the upstream while the sensor was down. The fake upstream now records any poll that arrives while the test holds the sensor down, the test does its pre-restart bookkeeping inside that window, and the assertion reads the recorded count. The session-resume scenario's `waitForNodeRunsQuiescent` is gone. The sub-worker dispatch is the caller-delegate branch of the same cascade pass that would have enqueued a fourth worker dispatch, so the test waits for that dispatch and then reads the worker's row count. The held-swap abandon path waits on the `claim_resolution.abandon` event, which rimsky writes once the producer's Abandon has run. The two executor timeout tests stopped racing a fixture sleep against a client deadline: the fixture server blocks on the request context, so the product's own timeout ends the request.

The stage left two `pacing` markers and no `ordering` sites. `sleepUntilPast` in the sensor-cron restart scenario advances the real clock past a cron window that the sensor reads from the host clock and no test can drive. An attempt budget bounds the harness's container-boot backoff, so elapsed time decides nothing there either. Five harness waiters gained `outcome` markers on their poll cadence.

The proof: `go run ./tools/wallclock-lint -list` reports zero violations under `lib/services`, and the re-recorded baseline drops 42 files, leaving 119 sites over 41. `go build ./...` passes in all four modules, `make lint` and the plumbline lint are clean. The services module's shipped-package suites pass with `-count=1 -timeout 0` — `claim_producers/...`, `executors/...`, `sensors/sensor-http`, and the new `internal/awaited`. The whole services scenario suite and the openlineage subscriber suite pass against a freshly built image set under one minted run tag, and the root `./test/scenarios/...` and `./test/support/...` suites pass against real Postgres, which is the regression for the six reviewer fixes below.

Six reviewer findings from stage 10 landed here. L33: `go run ./tools/wallclock-lint` reads the recorded baseline before it writes, and exits non-zero naming every file whose count rose rather than re-blessing it; there is no override flag. I probed it by lowering one file's recorded count and watched it name the file and refuse. L34: the three elapsed-time verdicts outside the lint's population are gone. I deleted the breakpoint overflow test's `testBudget` assertion, a hang backstop sitting inside a verdict. The per-binding host-agent test now asserts that the `terminal/error/spawn_failed` payload names the binding's own `within 3s`, the text the readiness poll writes, and it reads that payload through a new `nodeEventPayload` fixture helper. Writing that assertion exposed two bugs, which D35 records. The stub's cancellation test asserts that the RPC came back `DeadlineExceeded` rather than measuring how long it took. L35: every heartbeat log in the poll helpers now fires once, on the first poll that finds the condition unmet, and never again. That covers `awaited.Until`, `eventwait.WaitForEvent`, the seven waiters in the scenario harness, and the four in the services harness. A wait blocked forever now stops touching the progress guard's window, so the guard trips. L36: the claim-scope race scenario dials on `h.Ctx`. L37: the dead 60-second context in the retry-frame test is deleted. L38: `progressFingerprint` fails the test and names the query error. It used to return a string, and two equal read failures compared equal and read as quiescence.

### Stage 12 — Convert wall-clock idioms, group 3 (`cmd/`, `lib/runtime`, `lib/control`, `lib/protocols`, `lib/foundation`)

I converted the last 119 sites over 41 files. The recorded baseline is now empty, which is what `decision:test-wallclock-lint-ratchet` claims. `go run ./tools/wallclock-lint` reports zero violations over the whole tree and writes `{}`.

The conversion reuses stage 10's idiom on both sides of the module boundary. `cmd/`, `lib/runtime` and `lib/control` tests import `test/support/awaited`; `lib/foundation` is its own module and cannot, so `lib/foundation/internal/awaited` carries the same poll-until-success helper with the contract suite the other two carry, which D38 records. `lib/protocols` names no rimsky module at all, so its conformance test package writes the loop in-package as `pollUntil`.

Most sites were a fail-on-timeout select wrapped around a channel the product had already written to. Those became bare receives: 22 in the breakpoint evaluator, 14 in the CLI watch test, 8 in the executor callback-receiver conformance test, and the rest across the proxy, the MCP server, the unified stack and the publisher reconciler. Every ad-hoc deadline poll became `awaited.Until` with the condition it waits for named.

Ten sites asserted that something had **not** happened during a fixed window, which is the shape the sprint's two classes do not cover. Seven anchor on progress the product itself reports, then read the channel without blocking. That read is correct at that instant, because the anchor proves the awaited outcome has not landed. The other three are the CLI watch tests, which assert instead the exact number of activity readings the watcher consumed before it exited. Five more sites slept to let the product get far enough — the proxy's cancel-frame test, the compose wait loop's cancel, and the two follow-loop cursor tests among them — and each takes the same anchor. Six instruments carry all of it: the breakpoint evaluator's own overflow re-check, counted through a `persistence.Tables` wrapper in the test; the CLI stub server's per-poll instance-activity script, which drives the watch loop's idle confirmation rather than racing it; the fake agent's dispatch counter in the proxy; the sqlite advisory locker's contended-poll counter; the compose fake client's poll counter; and, in the callback-receiver tests, the receiver's own registration of the ack id. D36 and D37 record the calls.

Four fixtures stopped racing a timer against the product. The in-process executor's slow handler blocks on the request context. The host agent's non-binding stub child blocks on a signal, and so does the stub child's idle wait. The publisher kit's fake sleeper blocks on a channel the test closes. Each of the four used to return on its own after a fixed delay, and that return could beat the outcome under test.

Three `pacing` markers and five `outcome` markers remain in these trees. The `outcome` markers sit on the poll cadence of a loop that exits only on success. The `pacing` markers sit on the debug-override race prober, the fan-out semaphore's slot hold, and the stub executor's async-delivery retry backoff. No assertion reads any of the three. D40 records the call.

The sweep for elapsed-time verdicts outside the lint's population found and fixed 80 sites in these trees. Seventy-four were `context.WithTimeout` backstops bounding a call whose success the test asserts, across the proxy tree, the executor conformance scenarios, the host agent's spawn tests, the supervisor's shutdowns, the compose wait tests, the two registry-dial tests and two sqlite starvation tests; each is `context.WithCancel` now, so the progress guard is the only backstop. Three were `http.Client{Timeout: ...}` on a client whose call the test asserts succeeds, and one a `net.DialTimeout` to a port the product had already reported ready. Two were `time.Since` assertions — the in-process client's `elapsed > 1s` and the system clock's `Sleep` return — both redundant once the awaited signal ends the call. One was `require.WithinDuration` on `SystemClock.Now()`, bracketed between two wall-clock reads now, which is exact. The last was `TestInstanceTerminator_StopBoundedByBudget`, whose two `time.Since` bounds are gone; D43 records what it proves now. D42 names the six deadlines kept, each the input under test.

The ratchet test grew one arm: the recorded baseline must be empty. D41 records why the file and both arms stay.

Verification: `go run ./tools/wallclock-lint` reports zero violations and writes an empty baseline; `test/plumbline` passes, including the new empty-baseline arm; `go build ./...` passes in all four modules and `make lint` is clean. Every converted package passes with `-count=1 -timeout 0`: `./lib/protocols/...`, `./lib/runtime/...`, `./lib/control/...`, `./lib/foundation/persistence/...` against real Postgres and SQLite, `./lib/foundation/shared`, `./lib/foundation/internal/awaited`, `./cmd/rimsky/cli`, `./cmd/rimsky/cli/compose`, `./cmd/rimsky-host-agent-proxy` and `./cmd/rimsky-entrypoint`. I probed the watch conversion by setting `idleConfirmPolls` to 1 and watched all three idle tests name the read count they saw. The container-backed runs used one minted run tag with all nineteen images built under it: the CLI's context demo, and the five services scenarios the reviewer's three lines touch — the two demo scripts, the onboarding demo, the claim-producer observability dashboard, and the 9b producer probe.

Three reviewer findings from stage 11 landed here. L39: the claim ledger's `time.Since(*rec2.ClosedAt) > time.Minute` is gone; the test asserts the stamp exists and does not precede the record's own `OpenedAt`. L40: the claim-producer observability scenario reads the SSE stream on a cancel-only context, and `streamClaim` already returns on the `claim_terminal` event, so the terminal assertion now reports a stream that closed without one. L41: the three demo-script runners lost their `timeout` parameter and take the caller's context, and the conformance-9b probe's dial and run contexts are cancel-only.

### Stage 13 — Sensor and replica

I retired `concept:replica`. Its file is gone, the concepts TOC lost its line, and the TOC's 74 lines match the directory's 74 files as a set. `concept:sensor` carries the amended body from the sidecar. The deployment-tier replica posture leaves its Boundaries and its Adjacent list. One invariant replaces the single-replica one: rimsky addresses a sensor by its endpoint, coordinates nothing behind it, and relies on a deterministic per-window idempotency key to absorb a second firing. No file under `.ok-planner/design/` names the retired slug now. D45 records the one prose reference I repointed.

I deleted the sensor-cron replica-posture test. It carried four subtests: the single-fire case, the two-replica dedup case, a source-token scan, and a package-doc text check. The reframed test keeps the first two. D46 records the other two. `multi_replica_test.go` is now `deterministic_idempotency_key_test.go`, carrying `@concept: sensor` alone. `TestRefiringTheSameWindowAfterARestartIsAbsorbedByTheIdempotentSend` drives the case the amended invariant states: a sensor fires a window, restarts, re-subscribes, and fires the same window again; the deduping receiver sees two posts and accepts one, because both derive the same idempotency key. `TestDistinctSubscriptionsNeverShareAnIdempotencyKey` is the kept distinct-subscription case, and `TestOneFireWindowPostsOneEnvelopeNamingItsSubscription` is the kept single-fire case. One `subscribeCron` helper now builds every subscription, where three copies stood before.

All four sensors take one connection-string shape against one database. `TestEverySensorTakesOneStateDSNShape` in the services scenario suite runs two passes over one table of the four sensors, each row naming its image, its own `_STATE_DSN` variable, its table prefix, and the tables it owns. The first pass boots one Postgres on a private network and starts all four sensor images against that one DSN, each returning when its gRPC port accepts a connection. It then reads every `sensor\_%` table out of that database and asserts the set equals the union of the four rows' declared tables — the object-store sensor's second table, `sensor_object_store_seen_names`, among them — and that each table matches exactly one sensor's prefix, so nothing collides. The second pass gives each sensor the same variable pointed at a host that does not resolve and asserts it exits non-zero, names its state database, and names the host from its own variable, which shows it read that variable and not another's. It ports the held probe `assumption-sensor-state-dsn-uniform`.

`harness.RunImageToExit` is what the second pass needs. It starts an image on the network, blocks until the container exits, and hands back the exit code and the logs. The harness's existing exit waiter named the test-runner in every message; it takes the container's name as a parameter now and serves both callers.

I probed both halves of the scenario. Renaming one expected table made the first pass name the set it found against the set it wanted; changing the awaited log text made the second pass name all four sensors, and its output shows each exiting 1 with `ping state db: ... hostname resolving error` in its log.

Two reviewer findings from stage 12 landed here. L42: the sqlite deadlock guard's 45-second `time.AfterFunc` closed a channel every worker polled. Under load that timer fired, every worker returned early, and the bump count read as a deadlock. The timer, the channel and the select are gone. `wg.Wait()` blocks until every goroutine finishes, and `bumps == expected` proves the behaviour at any load. L43: `WaitForControlAPIReady` built a cancellable child context and then overwrote it, leaking the first cancel on the production path. It starts from `pollCtx := ctx` with a no-op cancel and creates the child only under the deadline branch.

Verification: `go build ./...` passes in all four modules, `make lint` is clean, the plumbline lint reports no violation over the six touched source files, and `go run ./tools/wallclock-lint` reports zero violations and leaves the empty baseline unchanged. `./sensors/sensor-cron` in the services module, `./lib/foundation/persistence/sqlite -run TestQueue_`, `./cmd/rimsky/cli/compose` and `./test/plumbline` pass with `-count=1` through the progress guard, and the new scenario passes against sensor images built under one minted run tag. The reviewer's out-of-population sweeps over what I wrote find nothing: no `time.Since` or `time.Now().Sub` in an assertion, no `time.NewTimer`, `time.NewTicker` or `time.AfterFunc`, and the two `context.WithTimeout` calls in the new harness file bound container teardown alone, whose error nothing asserts.

### Stage 14 — Ported experiments

Four services scenarios port four held probes. Each drives the shipped surface an end user has, and none copies the probe's idioms.

`TestDevLoopShortcutVerbsAreIndistinguishableFromTheirGroupedForms` builds the CLI binary and runs both spellings of the five dev-loop pairs as a user types them, against one zero-config all-in-one container. It compares the flag set each verb reports under `-h`, then the output and exit code of a success path and an error path for each pair. The read pair runs twice over one world and compares byte for byte, in human and JSON form. The four write pairs consume their subject, so the run seeds two interchangeable subjects and compares them with hashes, instance ids and timestamps normalised away. Exit codes are never normalised. A pair whose two spellings both print nothing fails, so no comparison passes on emptiness. The test drives the built binary, because four of the five shortcuts are one-line delegations inside the CLI package and only the binary's own dispatch table turns a typed word into that call. D47 records that call.

`TestComposePlanPreviewsExactlyWhatComposeUpApplies` drives one compose project through the four manifest states. Each round fingerprints the live world — templates, tags with their targets, instance keys — runs `compose plan`, re-reads the fingerprint to prove the preview mutated nothing, then runs `compose up --yes` and compares the plan's operations against the operations the apply reports, operation for operation and object for object. It canonicalises the two cosmetic differences the sprint names and nothing else: `tag-rm` reads as `tag-delete`, and both sides' hashes are truncated to the plan's width. I probed each canonicalisation by removing it. Each is load-bearing exactly where the sprint says: the tag difference in the fourth round, the hash difference on the first round's `deploy`. Each round also declares whether it expects changes, so the no-op re-apply proves a no-op and the other three cannot pass on an empty plan.

`TestReadOnlyRoleGrantsEveryReadActionAndNoWrite` mints a key through `rimsky auth create-key --role read-only`, which is where the role expands, and reads the grant back as exactly one `*:read` entry with no mode and no scope. It then derives the read population from the action registry rather than a list of its own: every action ending in `:read`, asserted at 18, each driven at its first registered route with the path parameters filled from one table. A route naming a parameter the table has no value for fails by name, so a new read action cannot slip through undriven. None of the 18 answers 401 or 403. The 404 and 400 the deliberately absent identifiers produce are not refusals, and the assertion says so. The same key is refused six writes with 403, and `instance:list-frames` and `instance:read-frame` are refused too, because the wildcard matches the whole verb. I probed the population count and the gate itself: with a `tag:read`-only key, 17 of the 18 routes come back refused, so the read-only key's clean sweep is a result rather than an ungated pass.

`TestAllInOneStateVolumeCarriesTheDeploymentAcrossAContainerReplacement` runs the all-in-one image with its baked defaults over a host directory mounted at the declared state volume. It registers a template, creates an instance under a fixed key, wakes the graph with an empty message and a typed one, runs to quiet, and surveys the deployment: template ids, instance ids with their keys, the instance's event kinds, its message types, and its per-node run summaries. It asserts `state.db` sits on the mounted directory, destroys the container, starts a fresh one over the same directory, and requires the survey to come back identical. A third container with nothing mounted holds no template and no instance, so the mount is what carries the history. I probed it by pointing the replacement at no mount, and it fails naming the instance the deployment no longer holds.

`harness.StartAllInOneZeroConfig` is the new harness entry point all four tests boot: the all-in-one image with no config injected, an optional host directory bound at `/var/lib/rimsky`, and a `Stop` that destroys the container mid-test. The existing `BringUpRimsky` always writes a `rimsky.yml` into the container, which is the opposite of what a zero-config claim needs. Three helpers in the scenarios package lost their single-caller names and serve the new tests too: `repoRoot`, `buildRimskyCLI`, and `requireInstanceIdle`. `captureRun` now captures stderr beside stdout, because a CLI verb's failure goes to stderr and a capture that drops it reports a bare exit code.

Writing the compose scenario exposed a product bug, and I fixed it. D48 records it: a manifest whose node uses `kind:` sugar plans every step against a hash the deployment does not assign, and `compose up` walked into an opaque `500 FOREIGN KEY constraint failed` from the tag step. `applyStep` now compares the hash the deployment returns against the hash the plan carries and refuses the apply naming both, the manifest path, and the reason. `TestApplyPlan_RefusesWhenTheDeploymentHashesTheSpecDifferently` pins it. F5 records the fork the residue leaves.

Four reviewer findings from stage 14's first fix round landed here. L47: every one of the five pairs now compares an undefined-flag case, and the four write pairs each compare a `-o json` success beside their human one, on their own seeded subjects. A comparison whose arguments ask for JSON also requires exit 0 and output that parses. A pair therefore cannot pass by failing the same way twice, which is how this round found the bug D50 records. L48: the two renderings emit in different orders, so the sort stays. D49 records it as the third canonicalisation and gives the reason, and the mismatch message now states that reason too. L49: the refusal test computes the hash the stub deployment assigns, asserts it differs from the planned one, and requires the message to name both hashes and the manifest path. L50: `captureRun` no longer swaps `os.Stderr`. No verdict reads stderr. Swapping a process-global across parallel tests swallowed the harness's own diagnostics.

Three reviewer findings from stage 13 landed here. L44: the per-table ownership loop ran after set equality had already passed and proved nothing further; it is gone, and `requireNoSensorPrefixCoversAnother` asserts the property that makes set equality carry the no-collision clause — no declared prefix covers another — over the table itself, whatever the database holds. L45: `requireEveryBundledSensorIsInTheTable` walks `lib/services/sensors/` and fails when the shipped set differs from the four the table declares, so a fifth sensor forces the table to grow. L46: the channel and goroutine wrapping `wg.Wait()` in the sqlite deadlock guard are gone; the test calls `wg.Wait()`.

Verification: `go build ./...` passes in all four modules, `make lint` is clean, the plumbline lint reports no violation over the fourteen touched source files, and `go run ./tools/wallclock-lint` reports zero violations and leaves the empty baseline unchanged. The whole services scenario suite passes with `-count=1` under one minted run tag with every image built under it, `./cmd/rimsky/cli/compose` and `./lib/foundation/persistence/sqlite` pass, and each of the four new scenarios carries a probe above. The out-of-population sweep over what I wrote finds nothing: no elapsed-time assertion, no timer, no sleep, and the only deadline contexts bound container teardown, whose error nothing asserts.

# Certification — Recommended intake drain

Status: certified with issues promoted

## Outcomes delivered

- Every CLI verb that dials the control API sends the resolved key, pinned by a discovered-population test; the compose verbs and the publisher conformance poll authenticate (`concept:rimsky`).
- The compose one-shot's progress printer selects as volume × format, and verbose output ticks each newly observed frame with its `frame_id` (`decision:progress-flags`).
- Compose names everything under the manifest's bare project prefix; the server reserves no prefix, and the `compose:origin` action, header, and guards are gone (`concept:tag`, `concept:control-api`, `story:compose-lifecycle`).
- The three unpermissioned control-api actions have MCP tools; the catalog lists by recorded posture, fail-closed (`story:mcp-transport`).
- Every node-run state write in both backends routes through the next-state switch and refuses an illegal pair; the release path is transactional and cannot report a release that did not happen (`concept:transition-reason`).
- A standalone validator that derives no role refuses to boot, naming the entry (`concept:validation`).
- The four sensor state stores run on native pgx and the standard-SQL adapter is lint-denied repo-wide (`decision:postgres-pgx-v5`).
- The migration runner records a digest per applied file and refuses a rewritten one by name (`decision:migrations-append-only-numbered`).
- The closed egress default is cited at its enforcement sites (`decision:destination-allowlists-default-closed`); the structural-root annotations cite the on-demand decision (`decision:structural-root-edges-derived-on-demand`).
- Every rimsky process drains and hard-exits on a second signal through one shared helper, and installs the JSON logger at the level `RIMSKY_LOG_LEVEL` names — an unrecognized value is reported, not swallowed (`decision:graceful-shutdown`, `decision:logging-slog-only`).
- Core listener defaults live in the 8080–8099 block, bundled defaults in 9000–9199, images declare exactly the ports their binaries open, and fitness checks pin both populations (`decision:default-port-allocation`, `concept:service`).
- Every build disables CGO and every image cross-compiles for `linux/amd64` and `linux/arm64`; the release chain pushes multi-platform indexes and verifies each pushed tag's platforms back from the registry (`decision:build-cgo-disabled`, `decision:release-distribution`, `decision:release-chain`).
- Verification runs on per-run image tags minted by the ok-workspaces script; `RIMSKY_IMAGE_TAG` is the harness's only resolver and a missing image fails naming the variable and the build command (`story:fresh-artifacts-per-run`).
- Every test wait is classified; the wall-clock lint's baseline is empty and the ratchet keeps it so; 471 sites across 181 files were converted, and the elapsed-time verdicts outside the lint's population were swept too (`decision:polling-audit`, `decision:test-wallclock-lint-ratchet`).
- The project's determinism rules defer their universals to the plumbline testing standard, each rule stated once.
- `concept:replica` is retired; a sensor's contract is the deterministic idempotency key per subscription window, and all four sensors take one state-DSN shape, proven end to end (`concept:sensor`).
- Four held audit probes are ported as scenario tests: the shortcut verbs match their grouped forms, `compose plan` previews what `up` applies, the `read-only` role covers every read action and no write, and the all-in-one's volume carries state across replacement.
- The surface intent names MCP tools public, the two public Go modules, and the three non-prefixed public environment variables.
- Fixed along the way: eight duplicate key-flag definitions converged; two host-agent spawn-deadline bugs; a compose `kind:`-sugar manifest now refuses with both hashes instead of an opaque 500; nine verbs that declared `-o json` and ignored it now honour it; a flaky verifier scenario fixed at its cause.

## Divergences

D1. The api-key universal test found drift the sprint did not name, and I fixed it in the same change. Seven auth verbs each defined the `-key` flag themselves, with three different help texts, and `resolveAuthEndpointAndKey` carried its own copy of the flag-then-environment-then-context precedence. The concept states one universal; the tree carried eight definitions of it. I converged them: `RegisterAPIKeyFlag` is the one flag definition and `ResolveAPIKey` the one precedence, and both `RegisterCommonFlags` and the auth verbs call them. The auth verbs' help text now names the environment variable, which it did not before although the code honoured it. The sprint's item asked only for a test. Keeping the duplication would have made that test assert a universal the tree does not have.

D2. The sprint does not say how the test discovers "every verb building a control-api client", so I chose the population rule. The test parses the CLI's own packages and walks the call graph from each verb, stopping at any other verb so a dispatcher does not inherit its subcommands' calls. A verb is a function named `Run<Name>` in the CLI or compose packages, `runAgent<Name>` for the three host-agent subcommands, or `runConformance<Name>` for the conformance verbs, which the concept names as exceptions and which therefore have to be in the population to be excepted. A verb is in scope when that walk reaches the client constructor. Two assertions follow, not one: every such verb must send a resolved key, and every such verb that owns its own flag set must define the key flag. The split exists because `RunRunRemote` dials the control API on flags its caller parsed. Requiring it to define a flag it does not own would force either a false exception or a narrower population, and a future flagless verb could slip through the narrower one.

D3. Stage 4 deleted `lib/services/test/scenarios/control_api_compose_prefix_guard_e2e_test.go`, which the sprint names. That file also hosted three helpers unrelated to the reservation — `mintAPIKey`, `deployScenarioTemplateAuth`, `tagListedAuth` — that `mcp_transport_parity_e2e_test.go` calls. I moved them verbatim into a new `auth_key_helpers_test.go` in the same package, so the deletion does not break an unrelated scenario. The three helpers the deleted file used only for itself are gone.

D4. The sprint says the compose scenario asserts the four verbs round-trip under the project prefix and that a foreign tag under that prefix is listed, diffed, and torn down. `TestCLICompose_UpThenDown` already drove the four-verb round trip, so I extended it with the foreign-tag case instead of writing a second scenario that would boot another stack to repeat the first half. The scenario now creates a tag under the project prefix through the plain client and asserts `status` lists it, `plan` reports it as drift, and `down` removes it.

F1. Architect ruling: promoted. `test/plumbline/cli_api_key_universal_test.go` exempts `compose.RunTemplateRun`. The amended `concept:rimsky` closes its exception list with "These verbs stand outside the rule, and no others do", and the ephemeral-run verb is not on it. The concept names the ephemeral-run verb and the compose one-shot as two distinct one-shot self-host modes, so the built reading stretches the compose-one-shot exception over a verb the concept names separately. The sprint closed that list so a test could check the rule mechanically, and an exemption the list does not authorize defeats the closure. The behavior itself is not in dispute. A stack the verb just booted holds no operator credential, and sending one would fail the run for an operator whose environment names a key for a remote deployment. The corpus can name a seventh exception, restate the exception as the self-host machinery both modes share, or stay closed on verbs while the self-host branch routes through the compose one-shot's entry point. Each landing is a durable corpus mutation. Reasonable owners choose differently, so the fork is genuine. The issue is `.ok-planner/issues/2026-08-19-212932-cli-api-key-exception-list-omits-ephemeral-run-self-host.md`. The built reading stands until the owner rules: the exemption carries its justification at the site, and no code moved.

D5. The sprint says the run-tree parent arm "derives its target from the aggregation policy inside the switch, so the switch keeps its state-and-reason shape". Two readings fit. (a) The aggregation policy's verdict reaches the switch as the reason — the caller maps the settled result to `aggregate_settled_success` or `aggregate_settled_failure`, and the arm returns fresh or failed — which keeps `NextState(state, reason)` exactly as it is. (b) The aggregation itself moves into the `cascade` package so the switch computes the verdict, which would need the children and the policy as arguments and so would change the shape the same sentence protects. I built (a). `Aggregate` stays in `lib/runtime` where the run-tree types live, and `cascade.AggregateSettledReason` is the one place the verdict becomes a reason.

D6. Routing the release writers reverses a contract an existing conformance case pinned. `DispatchReleaseClaimSkipsTerminalRun` asserted that releasing an already-settled run is a silent no-op, for the delayed duplicate-acquisition race. The amended `concept:transition-reason` says a writer whose pair the switch rejects fails instead of writing, so the case is now `DispatchReleaseClaimRefusesTerminalRun`: both release verbs fail with the illegal-transition sentinel and neither reverts the row. The four callers already logged and continued on a release error, so a rare race now leaves a warning where it left silence. No caller treats it as fatal.

D7. The parent aggregate arms admit a settled parent. I first restricted them to the in-flight set, on the reading that a settled run is finished; `TestPropagateFromChildState_SettledParentReprojectedByLateChildTransition` pins the opposite, and the retired `parentAggregateOK` sentinel was permissive from every state for exactly that reason. A late child transition must be able to re-project a parent's aggregate verdict, so both arms admit `fresh` and `failed`. Stage 6 narrowed the rest of the set: F2 records where the boundary landed, and `aggregatingParentState` is the one place that names it. `NextState` still refuses the aggregate reasons for a leaf run from any state.

D8. The migration-digest conformance case cannot rewrite the file the sprint's wording describes: the runner reads an embedded FS the conformance suite does not own, and the backend constructors are unexported. The case mutates the recorded digest through the suite's raw-exec instead, which is the same comparison from the other side, and it additionally asserts that every applied row carries a digest after a boot and that a row whose digest is cleared is backfilled rather than refused. The runner's own unit tests cover the file-side rewrite directly, over a `MapFS`.

D9. The host-agent proxy stopped its two gRPC servers with a bare `GracefulStop()` and no budget, so a stuck stream held the shutdown forever. `decision:graceful-shutdown` opens by saying that on every path anything still running when the grace expires stops holding the shutdown, but its three-value sentence names three deployed core paths and the proxy is not one of them: the entrypoint, the standalone role processes, and the supervisor's dispatch wait. The proxy ships as one of the four distributed core images, so I gave it the deployed-core thirty seconds through `serverkit.GracefulStop`. The alternative was to add only the escalation and leave the unbounded stop, which would have left one shipped process outside the decision's opening claim.

D10. The four long-running CLI read verbs watched `os.Interrupt` alone through `signal.NotifyContext`. Routing them through the shared helper widens each to SIGINT and SIGTERM, because the helper notifies on both and the decision makes no per-path distinction. The effect is that a SIGTERM to `rimsky watch`, `rimsky messages`, `rimsky instances events`, or the `rimsky run` wait now drains and returns its exit code where it previously killed the process outright. I judged one signal set across every entry point more plausible than a per-verb set the decision does not describe.

F2. Architect ruling: determined. `NextStateParent` admits the aggregate reasons from `stale` beside `running`, `held`, `fresh`, and `failed`, and refuses them from `pending` and `parked`. `CreateRootNodeRun` inserts a sub-graph root in `stale` in both backends and creates its children immediately. The root therefore carries its children's aggregate verdict without ever passing through `running` or `held`. The switch models the states the runtime produces. The alternative moves the insert state that a dozen queue predicates and reaper cutoffs read, and no work item in this sprint authorizes that move. The sprint asks that every state write route through the switch, which it now does. I changed nothing. `TestNextStateParent_AggregateSettlementNeedsAParentThatCanCarryChildren` pins both halves, and so do `TestPropagateFromChildState_LeafRoot` and its four siblings in `lib/runtime`. Reviewer line L10 named a four-state list. The finding stands and the fix landed: the arms consulted nothing before and refuse two states now. Only the boundary differs, by one state.

D11. The supervisor's callback port had no built-in default: `callback.port` absent meant zero, and zero means an operating-system-assigned port. Giving it 8081 as the decision requires would have turned both CLI self-host launchers' deliberate `port: 0` into a fixed 8081, and two concurrent `rimsky compose run` invocations would then collide. I made the YAML field a `*int` so an absent key and an explicit zero are different states: absent takes 8081, explicit zero stays ephemeral. The alternative was to leave the supervisor out of the core block, which the decision's first sentence forbids.

D12. The `docs/` tree carries the moved ports in about thirty places — port tables, the collision notes, the proxy and all-in-one sections. I left every one. Those files open with a `/document` provenance stamp naming the release they describe, the ok-planner rules call them a snapshot allowed to go stale, and the next documentation run rewrites them whole from the audit's records. Editing them here would put this sprint's numbers under another release's stamp. The historical release notes under `releases/` stay for the same reason: they record what shipped.

D14. The per-run tag item depends on `/ok` converging the ok-workspaces run-tag payload into this tree, and that convergence has not happened. I made three calls, so that the build works now and the convergence overwrites what I wrote rather than adding to it. The profile declares the path: `.ok-workspaces/config.json` loses its `srcTag` block and gains `"runTag": {"path": ".ok-workspaces/bin/run-tag"}`, which is the suite's own default, so a converge reading no `runTag` key and a converge reading this one materialize the same file at the same path. I materialized that file myself from the suite's canonical `scripts/run-tag`, stamped v18.8.0 to match every other materialized file here; the converge overwrites it with identical content. I deleted `tools/image-src-tag.sh` rather than leave an orphan for the converge to retire, and I moved its two by-path exemptions — the plumbline ignore entry and the licensing exemption — onto the new path. I left `.claude/rules/ok-workspaces-cheatsheet.md` alone: it is suite-owned and stamped do-not-hand-edit, so its third rule still describes the content-addressed script this tree no longer carries. The converge rewrites that file.

D15. The sprint says the CI services shard passes the base-image argument at the run's tag. Its inline `docker build` lines built three of the nineteen images the services suites consume, which is the gap `ci-services-shard-builds-unresolvable-image-tags` names, so threading the tag through those three lines would have left the shard unable to resolve the other sixteen. The step now mints the tag and runs `make core-images service-images test-images` under it, and the base-image argument reaches the all-in-one at that tag through the `core-images` recipe, which is the one definition of how that image is built. The alternative was to copy nineteen build invocations into the workflow, which duplicates the make targets and drifts from them.

D16. I cannot push to Docker Hub from here, so the multi-platform push and the closing verify are proven against a throwaway local registry rather than the release path. Proven: `docker buildx build --push --provenance=mode=max --sbom=true --platform linux/amd64,linux/arm64` publishes an OCI index carrying a `linux/amd64` manifest, a `linux/arm64` manifest and the two attestation manifests; a second image built `FROM` that index for both platforms resolves it as an index and publishes both, which is the ordering constraint the all-in-one rests on; and `tools/verify-published-platforms.sh` passes both of those, fails a tag pushed for one platform naming the tag and the platform it lacks, and fails a tag absent from the registry. Not proven: that Docker Hub accepts the same invocation for the fifteen published images, and that the chain's step ordering holds end to end. Only a real release exercises those.

D17. The CGO check's population is every `go build` and every `go test -c` invocation in the tree. It excludes `go install`, which appears three times in the Makefile and installs third-party developer tooling — golangci-lint, the protoc plugins, gotestsum — rather than building anything this tree ships. `decision:build-cgo-disabled` governs the binaries this project produces, and forcing the flag onto a tool install would change what a contributor's tooling links, not what rimsky ships.

D18. The class marker is `//nolint:testwallclock-<class> <justification>`. The sprint leaves the shape to me and requires a form the plumbline comment-hygiene lint accepts. I probed the lint binary on a sample file first. It rejects `//testwait: outcome — …` and `//testwait:outcome …` as prose, and it accepts `//nolint:testwallclock-outcome …`, because it exempts a lint-suppression directive. The shape also earns its place. The marker is a lint directive, so the exemption is honest rather than a loophole. It extends the vocabulary this lint already owned instead of opening a second one. And the retired unclassified spelling fails by name, so the old idiom cannot return.

D19. Stage 8's `imagetag.Ref` panicked when `RIMSKY_IMAGE_TAG` was unset, and a panic in a package-level helper aborts the whole test binary: a plain `go test ./cmd/rimsky/cli/` lost every one of that package's other tests to one container-backed test. Before the per-run tag, that case failed as a single test with the build command named. I restored that shape without weakening the contract. An unset variable resolves to the self-describing tag `unset-RIMSKY_IMAGE_TAG`, which no registry holds. The failure then lands where the missing-image failure already lands: the caller's `t.Fatalf`, one failing test, and a message naming the variable, the build command and the run-tag script. A raw docker error names the variable too, because the tag carries it.

D13. The `@decision:` citation for the port blocks sits at five sites — `lib/foundation/ports`, the three core listener sites, and each bundled service's constant block — rather than at one. The decision governs two populations and the check enumerates both, so a single citation would leave nine of the ten bundled services with no link to the rule that governs their number.

D20. The scenario harness gave every test a 90-second `context.WithTimeout`, and nothing set `HarnessOpts.Deadline` to change it. Converting a poll to block forever is not a conversion while that context can cancel the queries the poll runs, so the deadline is now a `context.WithCancel` retired by `t.Cleanup`, and the unused option field is deleted. The sprint names the 234 lint sites, not this one; I read the item's "no elapsed-time value decides the verdict" as covering the harness the converted tests run inside.

D21. The conversion has one idiom for an ad-hoc condition — `awaited.Until` — and leaves the harness's six typed waiters their own loops. A single helper would have cost each of them the observed-state line it logs when a run hangs, which is the only diagnosis a blocked-forever wait offers. The split is by job, not by dialect: a named harness wait for a harness-known observable, `awaited.Until` for a condition a test states itself.

D22. Seventeen sites asserted that nothing happened during a fixed window. The sprint's two classes do not cover them: there is no positive product event for "the scheduler looked and did nothing", and the `ordering` class's event-log tail records transitions that by hypothesis never occur. I made the scheduler expose its own progress instead — a completed-tick counter on its handle, widening `config.SchedulerHandle` — and built `WaitForSchedulerQuiescence` on it: three consecutive ticks over an unchanged world fingerprint. The verdict then rests on the product's progress, not on elapsed time; a slow machine takes longer to complete three ticks and reaches the same verdict. I added no log event for the tick: the events standard's sites are state transitions, external-input branches, boundary crossings, retries, and caught errors, and a scheduler loop iteration is none of them — the boundaries inside the tick already emit.

D23. Several converted loops carried an `if !converged { ... t.Fatalf(...) }` block that dumped stub observations and SQL rows when the deadline expired. A loop that blocks until success never reaches that block, so I deleted those blocks and the helpers they alone used, `strDeref` in the fan-out overlay scenario among them. A hung run now reports the `awaited.Until` log line naming what the test waits for, beside the harness waiters' own observed-state lines.

D24. `TestPerInstanceOrderingInvariant_Concurrent` is the one site I judged genuinely ordering-dependent. It sampled the running-frame count every 20ms for five seconds and asserted at most one, which is exactly the shape the `ordering` class refuses: a sampler can pass through a window in which two frames overlapped. It now waits for every frame to end and then reads the durable record — a self-join over `rimsky_frames` counting pairs whose `[started_at, ended_at)` intervals overlap — and asserts zero. That is strictly stronger than the sampler and cannot miss a window.

D25. `TestHoldUntilBlocksUntilSignal` proved "Execute has not returned" by racing an 80ms timer against the result channel. The stub now counts dispatches blocked inside a `HoldUntil` (`Stub.Holding()`), so the test waits for the dispatch to enter the hold, reads the result channel non-blockingly — correct at that instant, because the dispatch is provably inside the hold — releases, and takes a bare receive. Its companion 2-second fail-on-timeout receive is a bare receive now.

D26. `awaitStableDispatchCounts` in the multi-hard-dep scenario waited for the stub's dispatch count to hold still for two seconds. It is `WaitForSchedulerQuiescence` plus a read now. That change exposed a second wall clock the old helper had been hiding: the boot phase read c's attribute row immediately after the helper returned and relied on those two seconds for the row to land. The boot phase now waits on the row itself — c carrying both upstreams' first-fire values — before reading the counts.

F3. Architect ruling: determined. `time.Sleep` belongs in the wall-clock lint's population. `decision:test-wallclock-lint-ratchet` says a per-site suppression marker exists for sleeps that are genuinely not verdict inputs. A test writes fixture pacing as `time.Sleep`, so without the sleep detector that sentence names no site at all. `decision:polling-audit` says no wait in the suites is unclassified, and a sleep in a test is a wait. The competing reading takes the ratchet's sleep sentence for the `case <-time.After(...)` arm, which restates the fail-on-timeout select the same Choice already forbids and leaves the sentence with nothing of its own to say. It also leaves one hole open: a converter replaces a banned deadline poll with a sleep and an assertion, and the lint reports nothing. Closing that hole is why the decision exists. The site count the sprint quotes describes the population before the detector, and the larger population makes the decision's own claim true rather than adding scope. I changed nothing. The tree carries the detector, and the drain is complete. The reviewer raised this as L30.

D27. Twelve sites asserted that nothing happened over a fixed window, and the sprint's classes cover none of them. Each one takes the progress its own stack exposes rather than a shared source, because a containerised stack offers no single scheduler handle to read: the sensor's poll count at the fake upstream, the object-store sensor's `last_poll_at` column, the webhook sensor's synchronous acknowledgment, and a settled node terminal. `requirePublisherMessageCountStableWhile` takes that progress as a `sensorProgress` value so the shape is one helper with three suppliers, not three helpers.

D28. The sensor-http restart proof's "nothing polled the upstream while the sensor was down" is now a recorded fact rather than a window. The fake upstream counts every poll that arrives while the test holds a `sensorDown` flag. The test sets the flag after `Stop` returns, when the container is already terminated, so no in-flight poll lands late. It clears the flag before `Restart` begins, so the new container's first poll cannot fail the assertion. The down window carries the pre-restart message count and the durable-watermark read, both real round trips, and a slower machine lengthens the window and strengthens the proof. I moved the watermark read from before `Stop` to inside the window for that reason. Five sensor polls have completed since the emit by then, so the row is written.

D29. The session-resume scenario's `waitForNodeRunsQuiescent` is deleted rather than converted. It proved that the cascade enqueues no fourth worker dispatch, and the sub-worker dispatch is the caller-delegate branch of the same cascade pass over the third terminal. Waiting for that dispatch and then reading the worker's `node_run` count is the same proof with no window: the pass that would have enqueued the fourth has committed by then.

D30. The held-swap abandon path waits on `claim_resolution.abandon`, the event rimsky appends against the acquirer's node once the producer's Abandon verb has run. I tried `lock_released` first: the acquirer emits it with `action=held_marked` when it settles while still holding the claim, and never emits it again on the held path, so the give-up leaves no `lock_released` record. The other candidate was the producer's on-disk sentinel directories, which would couple the scenario to the filesystem store's private layout.

D31. `TestExecute_Timeout_ReturnsTimeout` in http-node and `TestExecute_TimeoutClassifiesAsTimeout` in verifier-http raced a fixture sleep against a shorter client deadline. Their fixture servers block on the request context now, so the handler returns exactly when the product's timeout fires and the two durations cannot cross. The caller's deadline stays: it is the input under test, not a verdict, and a slow machine still reaches the same class.

D32. `TestPattern_RingMode_LiveDiscovery` slept 2ms between commits. The ring position is a nanosecond stamp with a name tie-break, and the assertion — that a folder added after four commits is discovered within the next six opens — holds under any ordering of the first two folders. I deleted the sleep rather than marking it.

D33. `countInstanceMessages` in the idempotency scenario and the acquired-row count in the scopes-conflict scenario each waited for a count to hold still. A synchronous event the test already observes settles both: the message POST returns once rimsky has persisted the message, and the contender node reaches terminal failed before the test reads the row count. Both are single reads now, and the scopes-conflict loop that waited for a second row to appear so it could fail is gone with them.

D34. The openlineage subscriber test carried its own `waitNodeTerminal`, a copy of the services harness's `WaitForNodeTerminal`. Converting it would have left two blocking waiters for one observable, so I deleted the local copy and pointed the call at the harness. `dumpOLArrivals` in the openlineage scenario went with the deadline it served: a wait that blocks until success never reaches a dump.

F4. Architect ruling: determined. The tree keeps one helper package per module. `lib/services/internal/awaited` holds the poll-until-success loop, carries its own contract suite, and serves the eight shipped packages that cannot import `test/support`. The second option copies one behavior into eight packages, and the project's strict-DRY rule forbids copying what must change together. The third option has a shipped package's `_test.go` import `test/support/awaited`, which contradicts the module layout this repo documents: the services module requires the root module for its own out-of-tree test tree, and a shipped package's in-package test is not that tree. `protocols-purity` denies the protocols module any test infrastructure, so the fourth placement stays closed. The shape mirrors what D38 settled for `lib/foundation`. I changed nothing. Seven shipped-package test files import the in-module helper, and nothing else does.

D35. Writing the assertion that the spawn-failure event names the binding's own `timeout_seconds` exposed two bugs, and I fixed both. First, the host agent spent the readiness deadline once per port attempt: `SpawnService` retries `maxSpawnPortAttempts` times on any bind failure, so a binding asking for 3 seconds waited 9 before the agent reported anything. The port retry exists for a picked-port collision, which the child reports by exiting at once, so a child that stays alive without binding now fails on the first attempt — `errReadyDeadlineExpired` ends the loop, and `TestSpawnService_ReadyDeadlineIsSpentOnceNotOncePerPortAttempt` pins that one port is picked and the message names the deadline the caller asked for. Second, the host-agent proxy waited exactly the readiness deadline it had just granted the agent, so its own `spawn ack timed out` always raced the agent's report and usually won; the operator read the proxy's timeout instead of the reason. The ack deadline is now the readiness deadline plus `spawnAckReportGrace`, derived in one place by `spawnDeadlines`, and `TestSpawnAckDeadlineOutlastsTheReadinessDeadlineTheAgentIsGiven` fails if the two ever meet again. The proxy's own timeout test now drives an agent that never acks, the one case that deadline exists to catch, and the fake agent's spawn delay is gone with it.

D36. Ten sites proved a negative over a fixed window: the evaluator has not returned, the watcher has not exited, the proxy has not answered. The sprint's classes cover none of them, and stage 10's `WaitForSchedulerQuiescence` serves only a test driving the root harness. Each site now waits on progress its own subject reports. Seven then read the channel with a `default:` arm; the three CLI watch tests read the exact number of activity readings the watcher consumed, which D37 explains. The instrument differs by subject, because the subjects share no scheduler: a `persistence.Tables` wrapper counting the breakpoint evaluator's overflow re-checks, an activity script in the CLI's stub control API, a dispatch counter on the proxy's fake agent, a contended-poll counter on the sqlite advisory locker, a poll counter on the compose fake client, and the callback receiver's own wait-map entry. Three of the six live in the test, two are new fields on code the tests drive, and one is state the receiver already kept. D39 records the two new fields and the third product change beside them.

D37. `TestRunWatch_IdleConfirmationResetsOnTransientBusy` was ordering-dependent in the strict sense: it flipped the instance to busy inside a 15ms window between two 20ms polls, and a sampler that arrives late reads the wrong world. The event-log tail does not help — the watcher reads a REST surface, not the event log. I made the stub drive the cadence instead: `InMemoryState.SetInstanceActivityScript` hands the watcher one activity reading per idle check, and `InstanceActivityReads` reports how many it consumed. The three idle tests now assert the exact read count at exit — 4 for the reset case, 5 for the two blocking cases — which is strictly stronger than the windows: an exit at read 1 means one idle read confirmed idle, and an exit at read 3 means the busy reading between the two idle ones did not reset the count. The alternative was to keep the window and widen it, which only lengthens the fuse.

D38. `lib/foundation` is its own module and does not require the root module, so its tests cannot import `test/support/awaited`. Eight sites across three foundation packages needed the poll-until-success loop. The options are: (a) `lib/foundation/internal/awaited`, one helper per module, which is the shape F4 settled for `lib/services`; or (b) the loop written out in each package. I built (a), for F4's reasons, and it carries the same contract suite. The tree now holds three copies of one small helper, one per module the isolation boundaries force apart. A reasonable owner could prefer to put the helper in `lib/protocols`, the one package every module may import — but the protocols module is the public contract surface and carries no test infrastructure, which `protocols-purity` enforces, so that would trade one duplication for a boundary violation.

D39. Three product files gained an observable so a test could stop watching a clock. `advisoryLockerImpl` counts the passes on which it finds the migration lock contended. `clitest.InMemoryState` — test-support machinery, not shipped code — carries the activity script. `compose.WaitForControlAPIReady` reads a non-positive deadline as "until the context ends", which is what a test with a cancel-only context needs; the two production callers keep their ten seconds. Each is the testing standard's own remedy: where a test needs a signal the product does not emit, add it. I added no log event for any of them, on `decision:polling-audit`'s reading that a poll iteration is not one of the events standard's sites.

D40. Eight class markers remain in these trees. Five are `outcome`, on the poll cadence of a loop that exits only on success: `awaited.Until` in the foundation module's copy, `pollUntil` in the protocols conformance package, the entrypoint's file wait, and the two conformance touch loops that repeat an idempotent write until the store's `updated_at` advances. Three are `pacing`. The debug-override prober's 250µs delay widens the interleaving it probes, and both the 200 and the 409 branch are asserted, so no verdict reads it. The fan-out semaphore's 5ms slot hold makes overlap observable, and the verdict is an upper bound on the observed concurrency, which more overlap only makes harder. The stub executor's 10ms retry backoff paces a delivery loop whose exit is a 2xx or an attempt budget, never elapsed time.

D41. The reviewer asked whether the baseline file and the ratchet arm still earn their place now that the baseline is empty. They do, and I made the emptiness itself the assertion. `decision:test-wallclock-lint-ratchet` says in one sentence that the gate fails on any violation and that its recorded baseline is empty; the file at `{}` is what makes the second half checkable. `TestWallclockVerdictRatchet` now fails when the baseline records any file, so a future run of `go run ./tools/wallclock-lint` cannot re-bless a backlog and pass. The writer was already sealed from the other side — L33's growth check refuses any file whose count rises above the recorded zero — so the two halves close the loop. Deleting the file and reducing the gate to "no violations" would drop the claim the decision makes about the baseline and would leave the writer free to recreate one.

D42. Six deadline contexts stay because each is the input under test rather than a backstop: the executor conformance's 100ms `AwaitTerminal` cancel case, the sqlite advisory locker's 100ms cancel case, the in-process client's 50ms caller deadline, the fan-out semaphore's 30ms acquire, the host agent's 50ms caller cancel during the port wait, and the peer-auth loader's one-second startup budget against an unreachable enroll endpoint. Three more stay because they bound teardown, whose error nothing asserts: the container terminate in the CLI's demo test and the two control-API shutdowns in `lib/control/config`'s cleanups. Every other deadline in these trees bounded a call whose success the test asserts, so its expiry was a load-dependent verdict, and those are cancel-only now.

D43. `TestInstanceTerminator_StopBoundedByBudget` asserted `time.Since(start) < 100ms` and `< 1s`. Neither bound is provable without a clock, and both are the banned shape. The test is `TestInstanceTerminator_StopReturnsOnTheLoopsExitNotOnItsBudget` now. It proves the two things `Stop` actually owes: a `Stop` on a terminator whose loop never ran returns, which a blocking `Stop` would not; and a `Stop` after the loop's context is cancelled returns because the loop closed its `done` channel, which the test reads without blocking after `Stop` comes back. A `Stop` that came back on the ten-second budget instead would leave `done` open and fail.

D44. The retention conformance case spaced four frame terminations 20ms apart so a host-clock `time.Now()` would land between f1's and f2's database `ended_at`. The cutoff now comes from the store's own recorded stamp: the case reads f2's `ended_at` back and uses it as the cutoff, so the prune's `ended_at < cutoff` predicate selects the fixture frame and f1 exactly. It asserts f1's stamp precedes f2's and says why if it does not. Both backends stamp each terminate from a clock of microsecond resolution or finer, in its own transaction, so the ordering follows commit order rather than a delay. The host-to-container clock skew the sleeps paid for leaves the verdict entirely.

D45. Retiring `concept:replica` left one prose reference in the corpus that no delta amends: `decision:single-process-mode` cited it in the parenthesis explaining what gates the memory blob backend. I repointed it to `concept:blob-backend` rather than deleting it. That concept carries the invariant the sentence points at — the in-memory backend is legal only in the single-process deployment mode and is startup-rejected in any per-role process — so the reader still reaches the claim, and the decision's own commitment does not move. Deleting the reference would have left the sentence naming a gate with nothing behind it. `docs/concepts.md` also names the retired slug; that tree is `/document`'s release-stamped snapshot, a record allowed to go stale until the next documentation run, so I left it, as stage 7 left the same tree.

D46. The deleted replica-posture test carried two subtests that were about neither the replica posture nor the sensor's idempotency key. `no_coordination_primitive_in_source` scanned the package's source for advisory-lock and leader-election tokens, and `package_doc_omits_retired_divergence_prose` asserted a phrase is absent from a package doc comment. Both check the existence of static text or code, which the testing standard says proves nothing. Both existed to pin a posture the retired concept stated. I deleted them with the file rather than carrying them onto `concept:sensor`. The amended invariant's own claim replaces them, and the reframed test drives that claim through the product: the deterministic key makes a second firing harmless, not the absence of a token.

D47. The sprint says the shortcut test drives both spellings against one stack, and does not say through what. Driving the CLI package's exported functions would prove nothing: `RunDeploy`, `RunUndeploy`, `RunInstantiate` and `RunRmInstance` are each a one-line call to the grouped verb's function, so four of the five pairs would be equal by construction and the fifth, `RunLs`, would still skip the dispatch table that turns the typed word into the call. The test builds the `rimsky` binary and runs it, which is what a user has. The cost is one `go build` per run, which the onboarding scenario already pays.

D48. Writing the compose scenario against a `kind: attribute_passthrough` template made `compose up` fail with `500 template_tags.insertIfAbsent: constraint failed: FOREIGN KEY constraint failed`. The cause: the control API applies four canonicalisations before it hashes a registered spec — frame-resolution defaults, kind sugar, send-message sugar, and the aggregation-policy default — and the last three consult the deployment's kind-alias map. Compose's resolver applies only the first, so a manifest naming a node by `kind:` plans its tag, deploy and instance steps against a hash the deployment never stores. `applyStep` now compares the two and refuses, naming the manifest path, the hash the deployment assigned, the hash the plan carried, and the fix a manifest author can make, instead of proceeding into the constraint error. The scenario itself uses a template that names its executor, as the probe did, so it measures plan against apply rather than this bug.

F5. Architect ruling: promoted. D48's refusal removes the crash and leaves compose unable to apply a `kind:`-sugar manifest at all. The corpus does not settle what a compose template's identity is. `concept:template` names key order and whitespace as what canonicalization absorbs, and it pins the canonicalization library as a standing commitment. The register route folds three deployment-dependent canonicalizations into the hashed bytes. `story:compose-lifecycle` promises reconciliation for a manifest the register route accepts. Three landings stay open: move the deployment-dependent canonicalizations out of the hashed bytes, have the deployment answer with the canonical hash and resolve each manifest template through it, or rule that a manifest declares templates in already-canonical form. Each landing is a durable corpus mutation. Reasonable owners choose differently, so the fork is genuine. The issue is `.ok-planner/issues/2026-08-19-212933-compose-template-hash-depends-on-deployment-configuration.md`. The built reading stands until the owner rules: `applyStep` refuses and names both hashes, and `TestApplyPlan_RefusesWhenTheDeploymentHashesTheSpecDifferently` pins it.

D49. The plan and the apply emit their operations in different orders. The comparison therefore reads each rendering's three object-kind groups and compares them one kind at a time, in emitted order — a third reconciliation beside the two canonicalisations the sprint names. `EmitPlan` groups the plan's steps by object kind, so a reader sees Templates, then Tags, then Instances. `ApplyPlan` walks the same steps in execution order. The first round plans register, deploy, tag, create and applies register, tag, deploy, create. The fourth plans undeploy, template-delete, tag-delete and applies undeploy, tag-delete, template-delete. Comparing in emitted order would fail on that grouping alone and say nothing about whether the preview matched the apply. `EmitPlan` is a stable partition of `plan.Steps` by kind, so regrouping recovers the apply's order within each kind: a deploy before its register, or two instance-creates swapped, still fails. The test reads the plan's groups from its `Templates:`, `Tags:` and `Instances:` headers, maps each applied verb to its kind, and fails an applied verb it cannot map. The mismatch message names the kind and states the reason.

D50. The `-o json` comparison for the write pairs found a bug, and I fixed it. `rimsky instance delete` declared `-o json` through the common flag set and printed `<ref> deleted` whatever the format. Seven verbs did the same: `template rm`, `tag rm`, `asset delete`, `admin reset`, `tag create`, `tag mv`, and `compose up`. Tightening the check in the third fix round found one more, `compose down`, for nine in all. The three removals and `asset delete` now emit `{"ref": …, "removed": true}` through one `reportRemoval` helper. `tag create` and `tag mv` emit the tag object `tag get` already emits. `admin reset` emits `{"node": …, "reset": true}`. `compose up` and `compose down` each emit their project, their applied count and the instances the apply created, and send their per-step narration to stderr so stdout stays one document. Each verb keeps the exact human line it printed before, `asset delete` included. `TestEveryVerbDeclaringTheOutputFormatFlagReadsIt` replaces the three-name table: it walks the CLI's verbs, takes the 39 that register the common flag set, and fails any that never names `FormatJSON` on its own success path. `TestComposeUpUnderJSONWritesExactlyOneRecordToStdout` and `TestComposeDownUnderJSONWritesExactlyOneRecordWithAnEmptyInstanceArray` pin the two compose records. Each decodes the record, then reads everything the verb wrote after it. A remainder that is not whitespace fails the test, so a narration line and a dropped `return` both surface as trailing text. A second decode would instead read a plain-text line as a syntax error and pass.

D51. The output-format walk had to stop at the shared error reporters. `reportError` calls `ReportDryRunPreview`, which reads the active format, so a traversal following every callee finds `FormatJSON` reachable from every verb and passes vacuously. My first version of the check did exactly that and reported all 39 verbs clean. The walk now stops at `runWithCommon`, `RegisterCommonFlags`, `reportError`, `ReportDryRunPreview` and compose's two error reporters, so it measures the verb's own success rendering. I probed it by reverting each fix in turn, and it named the verb each time.

D52. `TestVerifierSeverityPartition` failed when I ran the whole services scenario suite for this round, and I fixed the test at its cause. It waited for the node's run summary to report a failed count, then asserted in that same read that some event carried the blocking check's `error_class`. The summary and the event settle in separate writes, A read that catches the summary first sees no class yet, and the assertion then fires on that sampling instant. The wait now also requires an event carrying an `error_class` before it judges which class that event carries, so the verdict reads a signal the product has emitted rather than the gap between two writes. The test passed three consecutive runs alone and once more inside the full suite.

D53. The output-format check first accepted the identifier `Format` as evidence that a verb reads the resolved format. `Format` is also the type and the field name, so a verb that handed `common.Format` to a helper which discards it passed. The predicate now requires `FormatJSON`, the constant a verb can only name to branch on. That tightening found `compose down`, which declared `-o json` and rendered one fixed line. I re-ran the probe over all nine fixed verbs. Reverting each one in turn makes the check name that verb, and the whole population of 39 holds.

D54. `TestClockControllableClockMultipleSleepersResolveInDeadlineOrder` asserted the order in which two woken goroutines appended to a shared slice. The controllable clock closes each sleeper's channel in deadline order, but it does not schedule the goroutines those closes release, so the append order is the Go scheduler's and not the clock's. I renamed the test to `TestClockControllableClockAdvanceWakesEverySleeperItsDeadlinePassedAndNoOther` and moved it onto the contract the clock owns: an advance wakes every sleeper whose deadline it passed, wakes no other, and each woken sleeper reads a clock at or after its own deadline. I also deleted the `sort.SliceStable` in `flushDueLocked`. `advanceTo` steps to the earliest pending deadline each round, so every batch it flushes is one instant and the sort can never reorder anything. Keeping it claimed an ordering guarantee the type does not make.

D55. `ReleaseClaim` and `ReleaseClaimWithDisposition` now run their state read and their compare-and-set write in one transaction on both backends, and both check rows-affected. Postgres reads through `claimedRunStateForUpdate`, which takes `FOR UPDATE`, matching what `UpdateAggregateState` already did. SQLite reads inside `Tables.Transaction`, whose `_txlock=immediate` connection serialises it against every other writer. A compare-and-set that matches nothing now returns `persistence.ErrNotFound` naming the run, where it used to return nil and leave the caller recording a release that never happened.

D56. The contended case is in the persistence conformance suite for both backends as `ReleaseClaimReadsTheStateTheCompetingWriterCommitted`, in `release_claim_contended.go`, covering both release verbs. The test opens a transaction, settles the claimed run into a terminal state, holds the transaction open, starts the release against the locked row, then commits. Under the fix the release reads the committed terminal state and returns `cascade.ErrIllegalTransition` with the claim untouched. It does so on every interleaving, because the row lock orders the release after the commit. The test does not reliably catch the pre-fix shape, and I want that on the record. I reproduced the old read-then-write shape behind the same test and it passed twenty runs on SQLite and five on Postgres. The settle always commits before the unlocked read reaches the database. Catching that shape needs the settle to commit strictly between the read and the write, and no external caller can drive that window without a hook inside the queue. I judged a test-only hook worse than the gap. The suite therefore pins the post-fix contract, the code holds the atomicity, and this note records the limit.

D57. The protocols module's copy of the poll-until-success helper now has its own file and its own contract suite. I moved the inline `pollUntil` out of `await_terminal_test.go` into `lib/protocols/conformance/executor/awaited_test.go` and gave it the same two contract tests the other three copies carry. No module can host a suite the copies share. `lib/foundation`, `lib/protocols` and `lib/services` are separate modules, `lib/foundation` does not require the root module, and the only package every module may import is `lib/protocols`, which `protocols-purity` bars from carrying test infrastructure. A per-module contract suite is therefore the only form left, which is what F4 and D38 already settled for the helper itself.

D58. `serverkit`'s unrecognized-level warning read `LOGGING.LEVEL.UNRECOGNIZED`, the only upper-case dotted kind in the tree. Every other log site in the tree, its two neighbours in the same package included, writes a prose message and structured fields. I made it read `unrecognized log level; using the default` and moved the test's assertion with it. Whether the tree should adopt event kinds at its log sites is a tree-wide question for `/audit`. One site does not convert the tree, and the uniformity rule forbids a second idiom beside the first.

D59. All eighteen Go-compiling Dockerfiles now cross-compile. Each builder stage reads `FROM --platform=$BUILDPLATFORM`, declares `ARG TARGETOS` and `ARG TARGETARCH`, and compiles with `GOOS=$TARGETOS GOARCH=$TARGETARCH`. Before this the arm64 image in every published tag ran the toolchain under emulation, which needs a binfmt handler `make buildx-builder` never installs. `TestEveryGoCompilingImageCrossCompilesForTheRequestedPlatform` walks the same Dockerfile population the CGO check walks and enforces all three parts. I proved the shape with `docker buildx build --platform linux/amd64,linux/arm64` over `dockerfiles/Dockerfile.go-base`: both builder stages ran on the host's arm64 and cross-compiled, one to amd64 and one to arm64.

D60. Deleting `WriteSyntheticSupervisorYAML` took its only test with it, and that test proved something worth keeping. The supervisor defaults the CLI writes and the ones baked into `dockerfiles/all-in-one.supervisor-config.yml` must agree, or a local run and the all-in-one image tune the supervisor differently. `TestSyntheticSupervisorDefaultsMatchTheBakedAllInOneFile` now compares the constant with the file directly, so the check survives the deleted function.

D61. Dropping http-node's fallback branch left `defaultHTTPPort` read only by the port audit's AST walk, which reads the source and not the symbol. `TestLoadOptsFromEnv_HTTPPortFollowsTheGRPCPortWhenTheOperatorSetsNeither` now reads both constants and asserts the loader hands back each one when the operator sets neither variable. That pins the relationship the deleted branch encoded, and keeps the constant reachable from Go.

D62. The publisher conformance fixture pushed its message on a 200ms ticker, which the `MessagePush` check then waited on inside a three-second budget. The finding proposed a push the test fires. The runner blocks the test for the whole run, so a test-fired push would have to race the runner's own sequence. The fixture pushes once from inside `Subscribe` instead, synchronously, before it answers. The message is therefore recorded before the runner reaches `MessagePush`, the wait returns on its first read, and the budget cannot enter the verdict. It also keeps the behaviour a cron publisher owes: a subscription produces a message. I removed the test's `MessageTimeout` override with the ticker.

D63. `TestAttachStateDB_...` asserted `time.Since(restored.LastPollAt) < time.Minute`. `last_poll_at` is stamped by the database's `now()`, so the assertion compared two clocks as well as measuring elapsed time. It now asserts the restored stamp is not before the row's own `started_at`, which the same database stamped, matching the claimledger treatment in this change.

D64. `decision:graceful-shutdown` named three thirty-second paths and the host-agent proxy was not among them, though the proxy's two gRPC servers stop under `serverkit.DeployedCoreGrace`. I read the deployed-core window as the determined one and amended the decision to name the proxy. The proxy ships as one of the four distributed core images, not as one of the eleven bundled-service images the ten-second window serves, and the calls it drains are dispatches on their way to a host agent rather than a service's own RPCs. Both the Choice and the Rationale carry the amendment, and the `decisions.md` summary line carries the same enumeration.

D65. The stub executor gave up on its async delivery after twenty attempts and said nothing. Every async scenario depends on that callback, so a dropped one failed the scenario without naming a cause. `postCallbackUntilAccepted` now loops until the receiver answers 2xx. A correct receiver accepts, so the loop returns on its first or second attempt. A broken one never accepts. The scenario's own wait for the outcome then never ends, and the run guard reports it. I moved the marker on the sleep from `pacing` to `outcome`, because the loop now ends on the receiver's answer rather than on a budget.

D66. I deleted eighteen inert `context.WithCancel` wrappers and their deferred cancels, and passed the parent context to each wrapped call. Nothing cancelled any of the derived contexts before the call it wrapped, so each one only added a node to the context tree. The coordinator asked me to judge the nineteenth, in `claim_producer_observability_dashboard_e2e_test.go`. Its cancel is inert too. `streamClaim` runs synchronously, breaks its scanner loop on the terminal event, and closes the response body on the way out. The deferred cancel therefore fires after the call it was meant to bound. The `ctx.Err()` branch inside `streamClaim` still reads the context the test passes, which is now the parent. I deleted that wrapper with the rest.

D67. `TestSensorHttp_BodyFilterAndDurableWatermark` set `sensorDown` after `sensor.Stop` returned, and `Stop` terminates the container synchronously. No poll could reach the upstream after that point, so `polledWhileDown` could never rise and the assertion at the end of that window could never fail. I deleted the assertion and both counters. Anchoring the window to progress the test drives does not make the claim checkable either. Setting the flag before `Stop` would open the window while the sensor is still alive, and a legitimate in-flight poll would then fail the test. The surrounding test still proves what the story owes — the watermark survives the restart and suppresses the re-emit.

D68. Three `awaited.Until` descriptions read the system at call time and printed the reading as though it were the observation that failed. The frame-count description ran four database queries to print four zeros before the wait began. The override-count description formatted a pointer to a slice that was empty at that moment. Its closure assigned a `lastCounts` variable that nothing ever read. The loop-sink description read `loopSink.Count()` before the wait. Each description now names only what the test awaits, and I deleted the dead variable.

D69. Seven `awaited.Until` descriptions stated the failure rather than the condition. That description is the only line a hung run prints, so it has to name what the test waits for. Each one now does, matching the rest of the conversion.
D70. I applied C24's `&&` split to `goCompileLines` as well, the Makefile arm of the same check, which the finding does not name. Both readers joined continuations the same way, so both under-counted a chain. Fixing one and leaving the other would have kept the hole on the side that reads `cd lib/foundation && CGO_ENABLED=0 go build ./...`. That line passes today, because the flag sits in the same link as the compile. A second compile appended to it would not. I also extended the parser's unit test with a two-link chain whose second link carries no flag, so nobody can revert the split silently.

D71. C26 asked for an internal-invariant error in place of `persistence.ErrNotFound`. I named the class `persistence.ErrInternalInvariant` rather than write a bare `fmt.Errorf` at each of the three sites. Both backends state the same class, and one sentinel states it once. Three plain strings state it three ways and drift. The alternative was a package-local error per backend, which gives one class two sentinels. Each site still writes its own message, because each names its own operation, its run, and the lock that makes the branch unreachable. No caller matches the sentinel, and no caller should: the branch fires only when the row read under the lock stops matching inside the same transaction.


## Findings fixed

- Test suites: 1 (the controllable-clock ordering test, fixed at the root).
- Sprint alignment: 1 judgment finding fixed by a mechanical corpus repair (the proxy named among the thirty-second shutdown paths); 5 claimed forks routed to the architect.
- Code review: 18 across three reviewers' full 469-file sweep, all fixed; verification passes confirmed each and added 3 more, also fixed.
- Mechanical floor (annotations, plumbline lint, catalog TOCs, workspaces discipline): clean on first pass and on every re-run.
- Loop subtractions: 1 repeat; 0 reversals.

## The finding ledger

The `## Certification ledger` section of this report holds the table: C1–C26, none open.

## Issues promoted

Two, both architect-confirmed intent forks, both made ruling-ready by `/verify-issues` and awaiting the owner's ruling:

- `.ok-planner/issues/2026-08-19-212932-cli-api-key-exception-list-omits-ephemeral-run-self-host.md` — where the corpus records the self-host loopback exemption (the fix would widen `concept:rimsky`'s closed exception list). Recommended ruling: restate the exception as the shared self-host machinery.
- `.ok-planner/issues/2026-08-19-212933-compose-template-hash-depends-on-deployment-configuration.md` — what a template's identity is a function of (the built refusal stands until ruled). Recommended ruling: the deployment owns the canonical hash and the validation route returns it.

`/verify-issues` also closed `hang-backstop-counts-retry-output-as-progress` as answered: the sprint's once-only heartbeat change already fixed it.

The close-out offer ends the walk: archive the sprint and commit the work, both on the owner's word.

## Certification ledger

| id | site | producer | round entered | outcome | repeats | rounds touched | note |
|---|---|---|---|---|---|---|---|
| C1 | `lib/foundation/shared/clock_test.go:99` — `TestClockControllableClockMultipleSleepersResolveInDeadlineOrder` asserts wake order `[0,1]`, observed `[1,0]` | test suites (`make test-foundation`) | 1 | fixed 1 | 0 | 1 | fixed: test asserts the clock contract (wakes every passed deadline, no other); inert sort removed (D54) |
| C2 | `## Divergences` D9 — `cmd/rimsky-host-agent-proxy/main.go` `GracefulStop(..., DeployedCoreGrace)`; `decision:graceful-shutdown` names three thirty-second paths and the proxy is not one | sprint alignment (A1) | 1 | fixed 1 | 0 | 1 | fixed: decision names the proxy among the thirty-second deployed-core paths (D64; corpus edit, judge re-checks) |
| C3 | `## Divergences` F1 — `compose.RunTemplateRun` exemption vs `concept:rimsky`'s closed exception list | sprint alignment (A2, CLAIMED FORK) | 1 | promoted `.ok-planner/issues/2026-08-19-212932-cli-api-key-exception-list-omits-ephemeral-run-self-host.md` | 0 | 1 | architect CONFIRMED: where the corpus records the self-host exemption is a durable corpus mutation; built reading stands until the owner rules |
| C4 | `## Divergences` F2 — aggregate arms admit `stale`; reviewer asked four states | sprint alignment (A3, CLAIMED FORK) | 1 | fixed 1 | 0 | 1 | architect OVERTURNED: the switch models the states the runtime produces; `CreateRootNodeRun` inserts `stale`; built reading stands, tree unchanged |
| C5 | `## Divergences` F3 — `time.Sleep` in the wall-clock lint's population | sprint alignment (A4, CLAIMED FORK) | 1 | fixed 1 | 0 | 1 | architect OVERTURNED: the ratchet decision names sleeps; built reading (detector) stands, tree unchanged |
| C6 | `## Divergences` F4 — one in-module `awaited` package vs eight in-package loops | sprint alignment (A5, CLAIMED FORK) | 1 | fixed 1 | 0 | 1 | architect OVERTURNED: one helper package per module, mirroring D38; built reading stands, tree unchanged |
| C7 | `## Divergences` F5 — compose template identity: refuse client-side vs deployment returns canonical hash | sprint alignment (A6, CLAIMED FORK) | 1 | promoted `.ok-planner/issues/2026-08-19-212933-compose-template-hash-depends-on-deployment-configuration.md` | 0 | 1 | architect CONFIRMED: template identity vs deployment canonicalization is a corpus tension; built reading stands until the owner rules |
| C8 | `lib/foundation/persistence/postgres/queue.go:358-411`, `sqlite/queue.go:399-455` — `ReleaseClaim`/`ReleaseClaimWithDisposition` read then write outside one transaction and return nil on zero rows matched | code review (R1; R9 repeat) | 1 | fixed 1 | 1 | 1 | fixed: read+CAS in one transaction, rows-affected checked, ErrNotFound on miss; contended conformance case added (D55, D56) |
| C9 | `lib/foundation/internal/awaited/awaited.go`, `lib/services/internal/awaited/awaited.go`, `test/support/awaited/awaited.go`, plus the inline `pollUntil` in `lib/protocols/conformance/executor/await_terminal_test.go:46-59` — four copies of the same poll-until-success helper | code review (R2; R11) | 1 | fixed 1 | 0 | 1 | fixed: protocols inline copy became a named test-scoped helper with the shared contract tests; cross-module suite impossible (D57) |
| C10 | `lib/protocols/serverkit/logging.go:42` — `LOGGING.LEVEL.UNRECOGNIZED` is the tree's only uppercase `SUBSYSTEM.NOUN.VERB` literal; neighbours in the same package use prose | code review (R3) | 1 | fixed 1 | 0 | 1 | fixed: prose message matching neighbours (D58) |
| C11 | `Makefile:359-363` `BUILDX_PUSH --platform linux/amd64,linux/arm64` with 18 Go-compiling Dockerfiles that hardcode `GOOS=linux go build` and no `--platform=$BUILDPLATFORM`/`TARGETARCH`; `make buildx-builder` installs no binfmt | code review (R4) | 1 | fixed 1 | 0 | 1 | fixed: all 18 Dockerfiles cross-compile; fitness arm enforces the shape; buildx multi-platform proof ran (D59) |
| C12 | `lib/services/executors/http-node/server_test.go:361` — `context.WithTimeout(…, 5s)` feeds the Execute request whose 200 the test asserts | code review (R5) | 1 | fixed 1 | 0 | 1 | fixed: context.Background() |
| C13 | `cmd/rimsky/cli/compose/synthetic_config.go:173` `WriteSyntheticSupervisorYAML` — dead (only its test calls it); the change edits the port it writes | code review (R6) | 1 | fixed 1 | 0 | 1 | fixed: dead writer and test deleted; constant-vs-baked-file check remains (D60) |
| C14 | `lib/services/executors/http-node/config.go:41-44` — the HTTP-port fallback branch computes `grpcPort + 1` in both arms | code review (R7) | 1 | fixed 1 | 0 | 1 | fixed: branchless fallback; relationship test pins the constants (D61) |
| C15 | `lib/control/config/claim_producers.go:445-465` — the validator-protocols default `[validation]` is refused by the new check, and the refusal message misdescribes an entry that declared no `protocols` key | code review (R8) | 1 | fixed 1 | 0 | 1 | fixed: absent list refused in its own arm; both messages pinned |
| C16 | `cmd/rimsky/cli/ctx_demo_test.go:134-147` — the container-log dump fires on the first poll of every passing run and never on a real failure | code review (R10) | 1 | fixed 1 | 0 | 1 | fixed: dump in t.Cleanup gated on t.Failed() |
| C17 | `cmd/rimsky/conformance_publisher_test.go:205` — the fixture publisher drives itself on a 200 ms ticker in the verdict path | code review (R12) | 1 | fixed 1 | 0 | 1 | fixed: fixture pushes synchronously from Subscribe; ticker gone (D62) |
| C18 | `lib/services/sensors/sensor-http/state_db_test.go:224` — `time.Since(restored.LastPollAt) > time.Minute` is an elapsed-time verdict | code review (R13) | 1 | fixed 1 | 0 | 1 | fixed: LastPollAt not before the row StartedAt (D63) |
| C19 | `lib/protocols/conformance/executor/scenarios/scenarios_test.go:103-113` — `postCallbackWithRetry` caps at 20 × 10 ms and silently drops the callback on exhaustion, marked `pacing` | code review (R14) | 1 | fixed 1 | 0 | 1 | fixed: loop-until-accepted; marker class moved to outcome (D65) |
| C20 | inert `WithCancel` wrappers left by the conversion: `sqlite/deadlock_guard_test.go:231`, `sqlite/max_open_conns_test.go:67`, `lib/runtime/supervisor_test.go:60,105,147`, `hostagent/mtls_test.go:313`, `scenarios_test.go:133,209,232,305,376,456,509,556`, `onboarding_demo_e2e_test.go:75`, plus `cascade_send_demo_e2e_test.go:49`, `frame_origin_audit_demo_e2e_test.go:44`, `conformance_9b/probe_test.go:47,59` | code review (R15 + addendum) | 1 | fixed 1 | 0 | 1 | fixed: all 19 inert wrappers deleted, the dashboard one judged inert too (D66) |
| C21 | `lib/services/test/scenarios/sensor_http_e2e_test.go:102-113` — `sensorDown` is set after the synchronous `Stop` returns, so `polledWhileDown` can only accumulate over one SELECT; the assertion cannot fail | code review (R16) | 1 | fixed 1 | 0 | 1 | fixed: unfalsifiable assertion and counters deleted (D67) |
| C22 | eager `awaited.Until` descriptions present wait-entry state as an observation: `serial_queue_each_invalidate_one_frame_test.go:37` (four DB reads), `attribute_overrides_match_overlay_unused_e2e_test.go:70` (dead `lastCounts`), `loop_counter_cap_e2e_test.go:139-140` | code review (R17) | 1 | fixed 1 | 0 | 1 | fixed: descriptions name what is awaited; dead variable deleted |
| C23 | seven `awaited.Until` descriptions state the failure, not the awaited condition (`…subgraph_e2e_test.go:93`, `…fanout_e2e_test.go:108`, `template_fan_out_e2e_test.go:134`, `template_sub_graph_delegation_e2e_test.go:153,159`, `frame_in_flight_blocks_next_serial_queue_test.go:36`, `host_agent_reap_test.go:47`) | code review (R18) | 1 | fixed 1 | 0 | 1 | fixed: all seven descriptions restated as the awaited condition |
| C24 | `test/plumbline/build_chain_test.go::dockerfileGoCompileLines` joins backslash continuations, so a chained `RUN` with six `go build` calls reads as one line and one `GOOS=$TARGETOS` occurrence satisfies all six; the CGO arm has the same hole | code review verification (R19) | 2 | fixed 2 | 0 | 1 | fixed: joined lines split on && in both arms; parser unit test extended; probe named the stripped call (D70) |
| C25 | `lib/control/config/publishers.go#202-207` re-derives protocols-less-validation inline while `claim_producers.go::validatorRoleDiscriminators#864` computes the same thing in the same package | code review verification (R20) | 2 | fixed 2 | 0 | 1 | fixed: dial site calls validatorRoleDiscriminators |
| C26 | `postgres/queue.go#382,417`, `sqlite/queue.go::releasedExactlyOneRow` — the new `ErrNotFound` branch is unreachable (FOR UPDATE / BEGIN IMMEDIATE hold the row), so it reads as an error contract callers cannot receive | code review verification (R21) | 2 | fixed 2 | 0 | 1 | fixed: rows-affected miss wraps new persistence.ErrInternalInvariant naming operation, run, and lock (D71) |
