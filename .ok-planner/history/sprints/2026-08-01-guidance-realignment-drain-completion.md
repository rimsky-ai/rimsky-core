# Certification — Sprint: Guidance-realignment drain

Status: certified clean

## Outcomes delivered

- The node-reset surface tells the truth: `decision:node-reset-clears-failure-marker` and the rewritten `story:node-admin` document the marker-clear reality (retry budgets are per-dispatch; no cross-run budget exists), the false retry-budget framing is retired, and all five code citations point at the corrected decision.
- `decision:network-binding` now covers the deployed reality: loopback default with a configuration-overridable bind, the wide bind named as the sanctioned split/containerized posture.
- `decision:release-distribution` states the four real channels and the two deliberate non-channels (Windows, network `go install`).
- The deleted registration-strictness mode is gone from the corpus: `ref-validation-mode`, `validation-names-the-mode`, `validation-error-names-mode` retired; `concept:instance`'s gate invariant no longer references it.
- The host-agent surface is corpus-true: `concept:host-agent` names the routing identity among the four stamped child env values, `story:anonymous-agents-isolated` is a live story proven end-to-end by the annotated isolation test, `decision:proxy-single-spawn-multiplexing` carries the multiplexing choice out of the concept, and `story:host-agent-anonymous-mode` is a pure statement.
- `decision:claim-producer-vocabulary-boundary` closes the rename-scope question so it cannot refile each audit cycle.
- Config-enforced decisions are provable and navigable: `decision:config-enforced-fitness-tests` realized by three grouped fitness tests (dependency-lint config, module manifests, Makefile/image chain) carrying 28 `@decision:` annotations; the wider annotation pass tagged 111 more decisions at their code enforcement sites, leaving exactly the 18 prose/process decisions uncited — a grep-verifiable boundary.
- The decision catalog is at choice altitude corpus-wide: all 233 live decisions carry genuine Alternatives (8 bare-default/changelog files retired), spec detail trimmed with every cut verified carried in code, historical narration restated present-tense.
- The story populations named by the four promoted story issues are pure statements: surface tails stripped, CLI-substance and parity stories rewritten (MCP parity landed in the new `decision:mcp-http-parity`), the 16-story mechanism tail rewritten with concept citations, missing and circular so-that clauses repaired.
- The intent-dossier debt is discharged: all 77 dossiers reconciled against the live corpus (19 code-verified folds, 4 judgment calls filed to the intake), `design/intent/` archived to `history/intent/`, the claim-scope naming symbols folded into `concept:claim-scope` with the fitness test repointed.

## Divergences

- Stories-sweep breadth: only the promoted populations were reduced to single-statement form; untouched stories keep the legacy three-block shape (the sprint ruled on the populations, not the catalog).
- `uuid-google` and `yaml-gopkg-v3` retired as content-free bare defaults; the choices remain carried by `go.mod` and the repo rules — one `git checkout` away if vetoed.
- Repo-wide license-header text changed: the three-line dual-license prose header became the SPDX expression form (`AGPL-3.0-or-later OR LicenseRef-FallGuy-Commercial` / `Apache-2.0`) across ~1700 files, forced by the v14.1.0 comment lint; licensing semantics unchanged, license tool updated in lockstep, root LICENSE files still govern.
- The wait-set per-round sender pinning was restored to `concept:wait-set` and `decision:substitution-deps-from-persisted-senders` — both had flatly denied a consultation the code performs; aligned to code with the restoring commit verified.
- Two cross-file prose corrections aligned artifacts with enforced reality (`synthetic-envelope-mechanism-retired` vs the harness-invalidate decision; `depguard-consumption-isolation`'s guard framing — lint wins).
- `TestPlumblineClean`'s config assertion updated for the retired `checks` key; the suite-owned src-tag script entered the plumbline ignore list with the nonconformance filed as an issue.
- Fixer calls (each reviewable): issue categories mapped per-issue to documented enum members rather than blanket `other`; the report's issue-count fixed as a wording clarification (the five-count was literally true; the sixth issue is planning-filed); the 20 stripped `status:` frontmatter keys restored from git-HEAD values (frontmatter-only corpus edits).
- No architect refutations — the fixer kicked nothing back.

## Findings fixed

- Sprint alignment: 4 findings (frontmatter dialect gap, report issue-count, commit hash in an issue candidate, invented issue categories) — all fixed in one cycle; re-review clean.
- Test suites: first `make test-all` failed on three counts, each fixed at the root (mid-run tree mutation rerun quiescent; stale config assertion updated; generated proto bindings restored via `make proto-gen`); quiescent rerun green — 160 package suites, zero failures, `make lint` clean.
- Mechanical floor: clean — 2206 citation pairs resolve; the only non-resolving strings are documentation examples in suite-owned instruction files and the ignored suite script.
- Code review: clean on first pass (independently re-verified the delta byte-matches, the Alternatives population, the annotation resolution, and the license sweep's completeness).

## Issues promoted

None by this certification run — the review-fix loop closed clean with no architect-confirmed forks and no cap escalation. The intake holds six open issues carrying this sprint's date, all filed before certification (five by execution's intent reconciliation and lint remediation, one by the planning ceremony); they are unverified raw filings awaiting the next `/verify-issues` pass and a future sprint.

---

# Completion report: Guidance-realignment drain

Execution record for `2026-08-01-guidance-realignment-drain.md`. Written as the work lands; finished by the closing ceremony.

## Status at hand-off to certification

All six work stages are complete: the 20 corpus deltas verified verbatim on disk (15 bodies byte-matched against the sprint, 5 retirement files absent), citations repointed and the isolation test annotated, the joint decisions pass and stories sweep landed, the three grouped fitness tests built and passing with the annotation pass covering every code-enforced decision, and the 77 intent dossiers reconciled and archived to `history/intent/`. `make lint` and `make test-all` are green (160 package suites, zero failures). Six intake issues carry this sprint's date; five were filed during execution — stage 6's four intent-reconciliation judgment calls (`breakpoint-overlay-visibility-across-matchers`, `proxy-same-identity-register-displacement`, `instance-key-idempotent-create-uncarried`, `subclaim-realized-write-semantics`) plus stage 7's suite-conformance report (`src-tag-script-violates-plumbline-lint`) — and the sixth (`cli-archive-sboms-after-first-release`) was filed by the planning ceremony before execution began. The remaining sections record the work in detail and every divergence and call made.

## Staging

Stages, in execution order: (1) apply the 20 corpus deltas + TOC updates; (2) repoint node-reset citations + annotate the isolation test; (3) joint decisions pass; (4) joint stories sweep; (5) grouped fitness tests + annotation tagging; (6) intent reconciliation and archive (after 3 and 4, per the sprint's stated dependency); (7) full build/test/lint verification; (8) finish this report; (9) `/certify-work` close.

## Work done

**Stage 1 — corpus deltas.** All 20 deltas applied verbatim: 10 amendments, 5 new artifacts, 5 retirements deleted. The three catalog TOCs updated to match (entries added/removed, stale summaries rewritten).

**Stage 2 — citations.** The five `node-reset-as-pure-retry-budget-clear` code citations repointed to `node-reset-clears-failure-marker`; `test/scenarios/host_agent_anonymous_multi_agent_isolation_test.go` gained `@story: anonymous-agents-isolated`. A repo-wide grep confirmed no other live references to the five retired slugs.

**Stage 3 — joint decisions pass.** All 234 in-scope decision files (the catalog minus the six sprint-final-form files) processed by 12 batched editor agents applying the three rules (missing Alternatives / spec altitude / historical narration), plus the same enumeration-boundary treatment applied to `concept:peer-auth`. Outcome: 8 retirements (`comment-drift-sweep`, `mechanical-cluster-sweep`, `merge-validator-warnings`, `build-go-version`, `claude-agent-attribute-only-session` (duplicate of `claude-agent-session-attribute`, content merged), `harness-first-ordering`, `uuid-google`, `yaml-gopkg-v3` — each a bare default or completed one-time event with no genuine rejected option; sanctioned corpus deletions per the sprint's work item), ~190 files edited, the rest already compliant. Every spec detail stripped was verified carried in code/tests before cutting (agents reported the carrying sites). decisions.md TOC consolidated: 8 lines removed, ~55 stale summaries rewritten. No dangling references to retired slugs anywhere live.

**Stage 4 — joint stories sweep.** Boilerplate delivery-surface tails stripped (`event-log-read`, `instance-lifecycle`, `lineage-admin`, `tag-management`, `template-lifecycle`, `runtime-diagnostics`); the three CLI-substance stories rewritten to outcome language (`one-shot-to-terminal`, `script-friendly-outcome`, `spawned-local-services` — their surface commitments already live in `decision:cli-verb`, `decision:exit-codes`, `decision:service-spawn-flag`, `decision:termination`, so no new decisions were needed there); the two parity stories rewritten as surviving pure statements (`mcp-transport`, `local-orchestrator-zero-config`), with the MCP parity commitment landed in the NEW `decision:mcp-http-parity` (the one place a surface commitment had no decision home); the mechanism-naming tail (16 stories) rewritten to outcome language with legitimate references converted to concept citations (`idempotent-mode-dedupes`, `iterative-workflows-converge` (now carries its previously-missing mandatory so-that), `cascade-defers-during-flight`, `all-upstream-gating`, `held-abandon-cascades-abandoned`, `held-commit-cascades-success`, `multi-hard-dep-rendezvous`, `sequenced-preserves-cascade-rounds`, `upstream-pull-on-invalidate` (circular so-that repaired), `portable-template-across-modes` (config-key mention stripped), `message-queue-coalesces-pending`, `uniform-attributes-delta-subscription`, `operator-invalidate-queues-during-flight`, `resume-preserves-snapshot`, `claude-agent-session-resume`, `executor-reads-dispatch-context`). Every touched story reduced to the canonical single-statement form. stories.md TOC summaries updated.

**Stage 5 — grouped fitness tests + annotation tagging (complete).** The annotation pass over the 129 then-uncited decisions finished across 7 batched agents: 111 decisions tagged with `@decision:` at their single most-specific enforcement site (Go code and tests; two land in `executor.proto`, which already carried citation tags), and 18 honestly classed prose/process — process, licensing, release-flow, and corpus-convention choices with no code enforcement site (`blessed-invariant-annotations`, `claim-producer-vocabulary-boundary`, `comment-hygiene-uniform-rule`, `config-flip`, `design-link-annotations`, `doc-residue-reshape-pass`, `implementation-language-go-plus-ts`, `migrations-no-compat-shims`, `parallel-cap-removal`, `pre-v1-break-freely`, `project-agnostic`, `release-dev-mechanical`, `release-distribution`, `release-formal-skill`, `release-notes-template`, `release-semver-from-diff`, `release-semver-sha-dot-joined`, `untagged-prose-by-module`). A post-pass grep confirms the uncited remainder equals exactly that prose/process set. Every edit passed the per-edit plumbline lint; agents reported clean builds on touched packages.

**Original stage-5 record.** Three grouped fitness tests built and passing under `test/plumbline/`: `depguard_boundaries_test.go` (the seven depguard rules + revive's disabled exported rule, parsed from `.golangci.yml`), `module_manifests_test.go` (five-module workspace tie with local replaces, the decided library pins, forbidden-logging-library absence), `build_chain_test.go` (Makefile targets, release-chain order, four core images, bundled-service images, two-stage/distroless/non-root/CGO-off image shape, version+channel tagging, hyphenless registry namespace, attestations, scout scan gate, race-gate split) — together carrying the `@decision:` annotations of 27 config-enforced decisions. The claim-scope naming symbols folded into `concept:claim-scope` as a canonical-naming-vocabulary invariant; `test/plumbline/claim_scope_naming_test.go`'s failure message repointed from the intent-dossier path to `concept:claim-scope`, and both its tests now carry `@concept: claim-scope`. Annotation tagging of the remaining 129 uncited decisions dispatched to 7 batched agents.

**Stage 6 — intent reconciliation and archive (complete).** All 77 dossiers walked (76 by 8 batched agents, claim-scope inline). Every fold was verified against code before landing; sprint-final-form artifacts were edit-fenced (divergences there became issues instead). Outcome: 19 folds across 20 live artifacts — among them the permission action-grammar invariant (`concept:permission`), the executor-agnostic schema invariant (`concept:persistence-database`), the claim-intent noun (`concept:claim`), the acquired-wins alias-collision rule (`concept:claim-co-holdership`), sub-claim-is-a-claim (`concept:claim-tree`), release-of-uncommitted≡abandon and external-dependent fail-fast (`concept:atomic-staging`), callback determinism (`concept:supervisor`), validate-without-persist (`concept:template`), source-file resolution boundary (`concept:rimsky`), api-key rotate refusals (`concept:api-key`), trailing-wildcard error vocabulary (`concept:error-policy`), permission-denied mode rows (`concept:event-log`), clone-never-re-fans-out (`concept:fan-out`), scratch settling-only repair (`concept:inertness`), stale last-outcome/creation-reason residue repairs (`concept:node-run`, `concept:attribute`, `concept:module-layout`), the corrected infra-terminal row (`concept:terminal-resolution`), the restored per-round sender pinning (`concept:wait-set` + `decision:substitution-deps-from-persisted-senders`, matching the code's pinned-sender resolution), workspace-wide verification (`concept:module-layout`), substitutable lock names (`concept:named-lock`), the coalesce multi-type validator warning (`decision:message-queue-mode-per-instance`), claim-terminal supervisor identity (`concept:lineage-record`), no-mid-subscription-reconfiguration (`concept:publisher-subscription`), and connection-level service entries (`concept:rimsky-yml`). 4 intake issues filed for genuine judgment calls (after de-duplicating two agents' same finding): `proxy-same-identity-register-displacement`, `instance-key-idempotent-create-uncarried`, `breakpoint-overlay-visibility-across-matchers`, `subclaim-realized-write-semantics`. Everything else was already carried or was dossier-staleness against deliberate later movement (each noted in the agents' reports). `design/intent/` (77 files) moved whole to `.ok-planner/history/intent/`; no live reference to the old path remains.

**Stage 7 — verification, plus a tree-wide lint remediation ("Fix Every Bug You Find").** Verification surfaced a pre-existing, tree-wide break: the v14.1.0 plumbline binary (refreshed by this morning's consented `/ok` converge) rejects the third line of the repo's canonical three-line license headers, so `TestPlumblineClean` failed with 1666 violations — none caused by this sprint's work. Root fix, not a suppression: the prose headers were swept to the SPDX forms the license tool already recognizes (`AGPL-3.0-or-later OR LicenseRef-FallGuy-Commercial` for the copyleft tree, `Apache-2.0` for the permissive tree) across 1718 files (1640 Go, 21 shell, 55 SQL, 1 TS, 1 JS), the license tool's stamp templates were carried along by the same sweep (its unit tests pass), two genuine prose comments were resolved per the tag-or-delete rule (a narration block deleted in `cmd/rimsky/cli/compose/run_test.go`; the docstring opt-in marker split from the package doc block in `lib/graph/attribute/substitution.go`), and the suite-owned `tools/image-src-tag.sh` (which ships lint-violating comments and a dead `@decision:` citation, and must not be hand-edited) was added to the project's plumbline ignore list with the nonconformance filed as `issue:src-tag-script-violates-plumbline-lint`. After the sweep: plumbline exits 0 over the whole tree, `make lint` (golangci over all five modules + license-lint) passes, `go build ./...` passes. `make test-all` run as the authoritative suite gate (the header sweep changes every file's content hash, so the content-addressed images rebuild). The first run failed on three counts, each fixed at the root: the context-demo test raced this session's own mid-run report edits (the tree hash moved between image build and test — rerun quiescent), `TestPlumblineClean`'s config assertion still demanded the `checks` key v14.1.0 retired (assertion updated: absent key is the current contract, a reintroduced key may never disable a check), and the header sweep had illegally touched the generated proto bindings (`make proto-gen` rerun so `gen/` is exactly regenerated output). The quiescent rerun passes: `make test-all` exit 0, 160 package suites ok, zero failures.

## Divergences and calls made

- **Stories-sweep breadth.** Most story files in the catalog carry the legacy three-block body (statement + two prose paragraphs). The sweep brought only the four promoted rulings' populations to the canonical single-statement form; untouched stories keep their legacy shape. Call: the sprint's work item names those populations, and a catalog-wide form reduction is a different (larger) compliance question the sprint did not rule on.
- **Retirement of `uuid-google` and `yaml-gopkg-v3`.** Both were content-free files recording bare defaults (one did not even name its library). The choices remain carried by `go.mod` and the repo rules; retirement follows the ruling's don't-fabricate-Alternatives test. Owner can veto at the certify walk — files are one `git checkout <sha> -- <path>` away.
- **Frontmatter dialect drift.** One editor batch (the `s*` decision files) stripped `status:`/empty-`aliases:` frontmatter keys per the strict template; the rest of the corpus keeps them. Repaired in certification's fix loop: the 20 files' original `status:` values (18 `as-is`, 2 `adopted`) were restored from git history, so every live decision again carries the key; no commitment touched.
- **License-header text changed repo-wide.** The lint remediation replaced the three-line dual-license prose header with the SPDX expression form in every source file. Licensing semantics are unchanged (the SPDX `OR` expression states the same dual license; `LICENSE.agpl`, `LICENSE.apache`, and `COPYRIGHT` at the repo root still govern), and the license tool already recognized the SPDX forms as valid before this session — but it is a notice-text change across ~1700 files the owner should be aware of at the walk.
- **Intent reconciliation restored one contested expression.** The wait-set per-round sender pinning: `concept:wait-set` and `decision:substitution-deps-from-persisted-senders` flatly said the wait-set is never consulted for sender data, while the code pins round-driving senders by the wait-set's recorded sender run. The reconciliation agent verified the pinning in code (with its restoring commit) and aligned both artifacts to it. This aligns expression with enforced reality, but it touches a decision's stated Choice — flagged here for the owner's attention rather than silently absorbed.
- **Two cross-file corrections beyond the three rules** (both align prose with enforced reality, no commitment changed): `synthetic-envelope-mechanism-retired`'s rationale contradicted `decision:test-harness-invalidate-node-retired` and was aligned; `depguard-consumption-isolation`'s rationale claimed defense-in-depth where the lint config states it is the primary guard — lint wins.
