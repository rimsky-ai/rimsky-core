{"id": "callback-hostname-split", "event": "open", "kind": "human", "category": "unclear", "artifacts": ["concept:executor", "concept:supervisor"], "summary": "Supervisor binds on `0.0.0.0`; the advertised callback URL must be peer-reachable — two distinct hostnames", "detail": "The supervisor's async-callback HTTP listener binds on `0.0.0.0`, but executors need a peer-reachable hostname to dial back. The peer-reachable host is set via `callback.advertise_host` (YAML) or `RIMSKY_SUPERVISOR_CALLBACK_ADVERTISE_HOST` (env). If empty, executors can't reach back. Two distinct hostnames in play; the YAML config doesn't make the asymmetry obvious. Operators may assume the listener bind value is the advertise value. Proxy-mediated executors do not sidestep the asymmetry: the `concept:host-agent-proxy` → supervisor callback hop has the same advertise-host requirement as any other executor → supervisor hop, so routing a dispatch through the proxy does not change the callback-reachability story for that hop. A separate hostname class joins the system on the dev-side path — the `concept:host-agent`'s local-listener address, used by spawned processes to POST callbacks back t Why it matters: A misconfigured deployment fails silently: the supervisor accepts dispatches, the executor accepts work, but the final-outcome POST never arrives, and the dispatch ages into orphan-reap. Diagnosis requires correlating supervisor logs, executor logs, and network policy.", "candidates": ["Require the advertised-callback-host setting (no default) and fail fast at startup if it is missing.", "Auto-derive the advertised host from the deployment environment.", "Add a startup self-probe that POSTs to its own advertised URL and warns if unreachable."], "at": "2026-07-22T02:37:13Z"}
{"id": "graph-runtime-scheduler-import-exception", "event": "open", "kind": "human", "category": "layering", "artifacts": ["concept:module-layout"], "summary": "Graph layer's runtime-import exception for scheduler sweeps", "detail": "The graph layer is otherwise pure (no runtime-layer imports), but a documented scheduler exception permits the runtime layer to be imported for sweep entry points so the scheduler tick can drive runtime sweeps. This leaves a single layering carve-out whose durability is not settled — the exception either belongs in the layering rule as a first-class allowance, or the sweep entry point belongs on the runtime side with the scheduler invoking it through a narrower seam.", "candidates": [], "at": "2026-07-22T02:37:13Z"}
{"id": "memory-blob-audit-gap", "event": "open", "kind": "human", "category": "durability", "artifacts": [], "summary": "memory-blob-audit-gap", "detail": "The memory variant of `concept:blob-backend` stores blob bodies in an in-process map; the persisted event log and node-run rows reference those bodies by handle. When the unified process exits, the in-process map vanishes — but the persisted rows survive, referencing handles that no longer resolve. For long-running unified deployments using the memory blob backend, \"blobs are ephemeral after process exit\" is the documented and intended semantic. The muddiness is what that means for the audit trail: an operator (or post-mortem tool) reading the persisted event log encounters blob handles that resolve to nothing, with no in-band indicator distinguishing structural absence (memory blobs never persisted) from data loss (a backend that lost its data). A reader holding only the persisted event log cannot tell which case they are looking at.", "candidates": [], "at": "2026-07-22T02:37:13Z"}
{"id": "per-cascade-source-mode", "event": "open", "kind": "human", "category": "scope", "artifacts": ["concept:cascade", "concept:wait-set", "concept:node-run"], "summary": "Per-cascade-source-mode generalization", "detail": "The cascade-mode policy is currently one value per receiver template-node, applied uniformly across every cascade source feeding that receiver. A generalization would let a receiver carry different cascade-mode values per upstream sender — for example, coalescing attribute cascades from a high-frequency sender while preserving every message cascade from a low-frequency one. The single-mode-per-node shape is sufficient for the workloads the design currently expresses, but it conflates \"what dedup do I want for this dependency?\" across heterogeneous upstreams. Whether the per-source generalization is the right scope expansion — and what its dispatcher / wait-set surface looks like — is undecided. Why it matters: Authors with mixed upstream cadences cannot express the natural pattern (\"most-recent for the noisy attribute upstream, sequenced for the audit-trail message upstream\") without splitting the receiver into two template-nodes. The split distorts the graph for a policy reason.", "candidates": ["Keep cascade-mode as one value per receiver template-node; document the workaround pattern for mixed-cadence authors.", "Extend cascade-mode to a per-(receiver, sender-node) map, with a node-wide default and per-source overrides.", "Extend cascade-mode to a per-(receiver, signal-type) map, keyed by the signal taxonomy rather than the sender node."], "at": "2026-07-22T02:37:13Z"}
{"id": "pre-v1-hash-instability", "event": "open", "kind": "human", "category": "unspecified", "artifacts": ["concept:template"], "summary": "Pre-v1 template hash bytes are not pinned across breaking changes; the post-v1 commitment is unspecified", "detail": "The current state: JCS library version is pinned in `go.mod` and the canonical-hash function is the registry's identity function. A transitive minor bump that changed canonicalization output bytes would invalidate every existing template id (annotated at the canonicalization package now living under the graph module's template layer). The repo-wide pre-v1 stance (\"no backwards-compat guarantee on the wire protocol, config shape, or persisted identifiers until v1 ships — a breaking change may require nuking a dev database\") now lives in the project's general pre-v1 rules rather than a template-specific CLAUDE.md note, and there is no in-tree `docs/concepts/template.md` (public docs are out-of-tree). The durable concept doc states only that the canonicalization-library version is pinned today; it does not state a post-v1 commitment. Will v1 freeze the JCS library version? The canonical-for Why it matters: Anyone building tooling that re-derives template hashes (subscribers, third-party diff tools) needs to know what to bind to. Post-v1 readers will hit a wall: the rule says \"pinned\" but historical practice says \"rebuild on breaking changes.\"", "candidates": ["State in `concept:template` the post-v1 commitment for hash byte stability: whether the canonical-hash function is pinned for the indefinite life of v1, whether the template id carries a version prefix that lets the canonical form evolve, or whether breaking hash changes ride a defined migration path."], "at": "2026-07-22T02:37:13Z"}
{"id": "stub-mode-runtime-only-gate", "event": "open", "kind": "human", "category": "unclear", "artifacts": ["concept:conformance", "concept:executor"], "summary": "`--require-stub-mode` is a runtime gate; an operator can mistakenly point conformance at a production endpoint without it", "detail": "The safety against accidental real-money LLM calls in conformance runs is a runtime flag, `--require-stub-mode`, gating an inline stub-mode probe run by the conformance CLI itself (the probe no longer lives in a separate sidecar binary). Without the flag, the probe doesn't fire, and conformance can run against any endpoint. Default behavior is permissive: no probe runs unless the operator opts in. The right default for a stub-only executor (`executors/stub/`); a footgun for any LLM-calling executor. Why it matters: A new operator running the executor-conformance binary against an arbitrary endpoint without the stub-mode gate hits real APIs. Cost: real money. The safety net only fires when the operator already knows it should.", "candidates": ["Reverse the default: the stub probe always runs unless the operator explicitly disables it.", "Detect known LLM-calling executors and require the opt-in for them.", "Add a documented protocol-level stub-mode declaration in the capabilities handshake so any LLM-calling executor self-declares."], "at": "2026-07-22T02:37:13Z"}
{"id": "stub-mode-signature-no-proto-surface", "event": "open", "kind": "human", "category": "unspecified", "artifacts": ["concept:conformance", "concept:executor"], "summary": "Stub-mode signature (`attributes_delta = {stub: true}`) is centralized only in the probe binary's source", "detail": "`rimsky conformance executor --require-stub-mode` asserts that a \"stub-mode\" executor responds to a probe Execute (`userdata: {stub_probe: true}`) with a settling outcome whose `attributes_delta` is exactly `{stub: true}`. The probe logic is duplicated: once in the shared conformance runner library's `probeStubMode`, and again inline in the CLI's own conformance command (the standalone probe sidecar binary that originally hosted this assertion is gone). Any executor that wants to be \"stub-conformant\" must hard-code this exact map shape. The agreement is not documented in `protocols/proto/v1/executor.proto` or in any in-tree protocol/conformance doc, and has no shared constant tying the two Go call sites together — each hard-codes the literal `{stub: true}` independently. There is no TypeScript executor in the tree to independently hard-code a second copy (all bundled executors, including Why it matters: The stub-mode probe is the gating mechanism for `--require-stub-mode` — the safety net that prevents `rimsky-executor-conformance` from issuing real LLM calls against a production endpoint. A protocol-defining handshake that lives only in test binary source code is the kind of thing that breaks silently when someone \"tidies up\" the literal: the only signal is \"conformance fails\" with no clear pointer to the agreed shape. This is the same shape as `async-callback-body-key` (the `type` vs `kind` i", "candidates": ["Give the stub-mode signature an explicit wire-protocol shape — a typed boolean field on the executor protocol — instead of asserting a free-form attribute map, so the handshake is part of the schema surface both sides depend on (see `concept:executor`).", "Document the stub-mode signature as part of the conformance concept's definition and the executor protocol's contract, so a conformant executor has an authoritative description of the expected shape (see `concept:conformance`).", "Centralize the expected stub-mode shape in one shared definition that both the conformance probe and the runner reference, so a rename or shape change has a single point of truth rather than independently hard-coded literals."], "at": "2026-07-22T02:37:13Z"}
{"id": "timeout-policy-asymmetry", "event": "open", "kind": "human", "category": "inconsistent", "artifacts": ["concept:frame", "concept:parked-state"], "summary": "Frame-timeout is purely advisory; park-timeout is destructive — sibling timeout disciplines with opposite policies", "detail": "Two timeouts in the system look similar but behave oppositely: - **`frame_timeout_ms`** (frame stuck warning): the scheduler emits a single `frame.stuck.observed` slog warning when `last_progress_at` falls outside the window. **No destructive action**. The frame stays `running`; nothing is failed. - **`max_park_duration`** (parked watchdog): when `parked_at + max_park_duration < now()`, the watchdog forces `parked → failed` with `error_class: \"park_timeout\"`. **Destructive**. Both are operator-facing safety caps measured against \"elapsed time\"; one observes, one kills. Documented separately and consistently in each home, but the policy asymmetry isn't surfaced anywhere as a deliberate choice. Why it matters: An operator who learns \"rimsky doesn't kill on timeout\" from the frame surface is surprised when a parked node fails on park-timeout. A future \"kill stuck frame\" policy proposal needs to know that the asymmetry was deliberate (one observes lack of progress; the other observes time-while-paused).", "candidates": ["Surface the deliberate asymmetry — frame-timeout observes, park-timeout kills — in a single \"timeout philosophy\" statement spanning the frame and parked-state concept definitions, so an operator does not generalize one timeout's behavior to the other (see `concept:frame`, `concept:parked-state`).", "Unify both timeouts as advisory by default, with an optional destructive mode, so the two disciplines share one policy shape.", "Unify both timeouts as destructive by default, with explicit per-template overrides, so the two disciplines share one policy shape."], "at": "2026-07-22T02:37:13Z"}
{"id": "unreachable-service-row-stall", "event": "open", "kind": "human", "category": "unspecified", "artifacts": ["concept:executor", "concept:claim-producer", "concept:supervisor", "concept:error-policy"], "summary": "Dispatch rows for unknown / unreachable services sit in queue with no error class", "detail": "When a dispatch row is claimed by a supervisor whose resolver cannot find the named executor, a synthetic `unresolved_executor` error class now fires and the run settles to a terminal error — that specific hosted-executor case (a misconfigured or dead executor a supervisor did attempt to dispatch to) is no longer a silent queue stall. What remains muddy: (1) the proxy-mediated case, where the user's agent simply isn't connected yet, is a normal transient state rather than a hard failure, and there is still no threshold-based escalation into an error/alert after repeated misses; (2) a row whose `executor_name` matches no supervisor's `accepted_executors` at all may still never be claimed by any supervisor in the first place, so the `unresolved_executor` path — which only fires once a supervisor has picked the row up — never triggers, and the row can still sit in queue indefinitely. Why it matters: In the proxy case, the user's agent not being connected is a normal transient state, and the rimsky operator may want to alert on it after a threshold. There is no mechanism for that today. A row no supervisor's `accepted_executors` ever matches has the same problem: it may never be claimed at all, so it never reaches the resolver check that now catches a claimed-but-unresolvable executor name.", "candidates": ["Extend `concept:error-policy` so that dispatch rows targeting unreachable services are escalated into a documented error class after a threshold, and capture in `concept:claim-producer` / `concept:executor` that an acquire-side reachability outcome is observable on the row. The threshold knob lives on the template per `concept:template`."], "at": "2026-07-22T02:37:13Z"}
{"id": "test-wallclock-verdict-dialect", "event": "open", "kind": "human", "category": "determinism", "artifacts": [], "summary": "Wall-clock test verdicts are still the dominant dialect despite the determinism rule retiring them", "detail": "~52 fail-on-timeout selects (select { case <-signal: ... case <-time.After(d): t.Fatal }) across every module (e.g. lib/runtime/breakpoint_eval_test.go, cmd/rimsky/cli/watch_test.go, lib/protocols/conformance/executor/callback_receiver_test.go); sleep-then-assert-nothing negative windows (test/scenarios/subscription_cascade_test.go:173, the whole test/scenarios/breakpoints/ suite); and ~200 deadline-bounded poll loops that t.Fatalf on expiry. No mechanical check backs the written rule, so the idiom re-enters with every new test. Load-dependent verdicts are a noise generator for continuous review.", "candidates": ["Mechanical sweep: fail-on-timeout selects become bare receives, deadline polls become success-only loops, negative windows gain real synchronization points; capped with a lint rule forbidding time.After in _test.go (per-site suppressions for legitimate fixture sleeps)"], "at": "2026-07-22T02:38:08Z"}
{"id": "store-claim-producer-vocabulary-split", "event": "open", "kind": "human", "category": "naming", "artifacts": ["concept:claim-producer"], "summary": "Retired 'store' noun still live in identifiers alongside 'claim producer'", "detail": "lib/control/config/claim_producers.go:372 names its result 'stores :=' inside the function that rejects the retired 'stores' YAML key; exported DialLifecycleSubscribers(ctx, stores RemoteClaimProducersConfig, ...); harness exports StartFilesystemStore/FilesystemStoreSpec/StartPostgresStore beside DialClaimProducer in one package; lib/services/test/scenarios/claim_producers/ declares 'package stores'; stores_redesign_smoke_test.go filename; example templates name producers fs-store/pg-store. A session grepping either noun finds both alive and mints new Store identifiers.", "candidates": ["Targeted rename sweep of claim-producer-context identifiers (harness exports, package name, file names, template fixture names, parameter/local names); scoped to spare legitimate 'store' nouns (object-store sensor, blob storage)"], "at": "2026-07-22T02:38:08Z"}
{"id": "endpoint-env-var-collision", "event": "open", "kind": "human", "category": "config", "artifacts": [], "summary": "Three near-colliding endpoint env vars; only one documented; ~30 more RIMSKY_* vars undocumented", "detail": "RIMSKY_CONTROL_API (CLI, cmd/rimsky/cli/run.go:146, undocumented), RIMSKY_CONTROL_API_URL (proxy/enroll, documented), RIMSKY_URL (host-agent's PROXY address despite the generic name, lib/runtime/hostagent/config.go:45, undocumented). Plus ~30 RIMSKY_* vars read in live code but absent from CLAUDE.md/concept docs/RELEASING.md (log controls, sensor state DSNs, openlineage vars, metrics addresses, RIMSKY_SUPERVISOR_CALLBACK_ADVERTISE_PORT).", "candidates": ["Rename RIMSKY_URL to name the proxy; fold the CLI onto RIMSKY_CONTROL_API_URL or rename the pair unambiguously", "Env-var registry (concept doc or generated table) backed by a fitness test that greps os.Getenv(\"RIMSKY_ and fails on any variable missing from the registry"], "at": "2026-07-22T02:38:08Z"}
{"id": "config-env-expansion-divergence", "event": "open", "kind": "human", "category": "config", "artifacts": [], "summary": "rimsky.yml parsed by two loaders with divergent env-expansion semantics; expandConfigEnv duplicated byte-identical", "detail": "expandBracedEnvRefs (lib/control/config/claim_producers.go:260, ${VAR} only, unset silently empty) vs os.ExpandEnv (supervisor loader and cmd/rimsky/cli/compose/synthetic_config.go:134, $VAR too) vs expandConfigEnv (unset hard error; byte-identical copies in filesystem and postgres claim-producer server opts.go). LoadSiblingBlocks re-parses the same sibling rimsky.yml with os.ExpandEnv that LoadRimskyConfigYAML parses with expandBracedEnvRefs: a bare $VAR yields different values by reader, no error either way.", "candidates": ["One expansion function, one policy (${VAR} only, unset is a hard error); service copies collapse into lib/services/internal/; compose consumes LoadRimskyConfigYAML instead of re-parsing"], "at": "2026-07-22T02:38:08Z"}
{"id": "blob-backend-spill-surface-drift", "event": "open", "kind": "human", "category": "docs-drift", "artifacts": ["concept:blob-backend"], "summary": "blob-backend concept doc claims four spill surfaces, its TOC line claims one; code has exactly two", "detail": "Doc body (blob-backend.md:11) claims attribute values, event payloads, parked payloads, and scratch spill; concepts.md TOC line claims attribute values only; code has exactly two surfaces (value_handle columns and rimsky_node_runs.scratch_handle written by lib/runtime/runner_terminal_scratch.go). No event-payload or parked-payload blob columns exist in any migration. The doc's own invariants section already matches the two-surface reality, contradicting its opening sentence.", "candidates": ["Rewrite the blob-backend concept's surface statement and its TOC line to the two real spill surfaces (attribute values, scratch)"], "at": "2026-07-22T02:38:08Z"}
{"id": "stale-tension-evidence", "event": "open", "kind": "human", "category": "docs-drift", "artifacts": [], "summary": "Three migrated tension issues carry stale evidence; timeout-policy-asymmetry describes machinery that no longer exists", "detail": "timeout-policy-asymmetry: both referents gone (no frame_timeout_ms anywhere; max_park_duration is an actively rejected config key; sweep_parked.go only wakes resume-ready nodes) - the tension dissolved, resolve rather than debate. stub-mode-signature-no-proto-surface: the duplication claim is false (CLI calls shared ProbeStubMode; single {stub:true} site) and the userdata framing is stale (field reserved); the no-proto-surface core remains. callback-hostname-split: the fails-silently framing predates the startup error for unset-advertise+wildcard-bind; the set-but-unreachable mode and two-hostname asymmetry remain.", "candidates": ["Resolve timeout-policy-asymmetry as dissolved (machinery removed)", "Re-open stub-mode and callback-hostname issues with refreshed evidence when resolving"], "at": "2026-07-22T02:38:08Z"}
{"id": "intx-idiom-residue", "event": "open", "kind": "human", "category": "uniformity", "artifacts": [], "summary": "X/XInTx method pairs survived the tx-last sweep in two spots; ~15 functions keep a now-meaningless InTx suffix", "detail": "Live pairs: MarkRevoked/markRevokedInTx and RevokeIfNotLast/revokeIfNotLastInTx (sqlite api_keys.go), postgres RevokeIfNotLast twin, getOrCreateInTx wrappers in both backends' deployment_ca.go. Judgment call: BlobBackend.Write/Read vs TxBlobBackend.WriteInTx/ReadInTx is an optional-capability split wearing the retired idiom's name. Residue: ~15 single-form functions keep the InTx suffix (most of lib/runtime, exported EmitTerminalSuccessAndDrainInTx, Harness.InTx). Copy-source hazard: a live pair reads as the house pattern.", "candidates": ["Collapse the api-key and deployment-CA pairs into single tx-taking methods; drop the InTx suffix from single-form functions; explicitly bless or fold the blob capability interface with non-echoing names"], "at": "2026-07-22T02:38:08Z"}
{"id": "image-freshness-latest-base", "event": "open", "kind": "human", "category": "build", "artifacts": [], "summary": "fake-claude-agent test image builds FROM :latest, the one hole in the staleness-unrepresentable guarantee", "detail": "lib/services/test/scenarios/claude_agent_fake_cli/Dockerfile.fake-claude-agent:37 builds FROM rimsky-executor-claude-agent:latest; the produced image is tagged :src-<tree-hash> but its base layer is whatever :latest points at, so a stale base yields stale executor code under a fresh-looking tag. Same Dockerfile carries a stale comment claiming inline testcontainers build. Six example READMEs still describe :latest harness resolution, contradicting the src-tag resolution CLAUDE.md documents.", "candidates": ["Parameterize the base (ARG BASE_TAG) and have test-images pass the src-tree tag; delete the stale comment; update the six example READMEs to describe src-tag resolution"], "at": "2026-07-22T02:38:08Z"}
{"id": "unconfigured-citation-tags-shell", "event": "open", "kind": "human", "category": "enforcement-gap", "artifacts": [], "summary": "@constraint:/@deliberate: tags in two demo shell scripts escape the plumbline lint", "detail": "examples/subscription-mounting-demo.sh (4 sites) and examples/cascade-send-demo.sh (7 sites) carry @constraint:/@deliberate: tags; neither is configured in .plumbline.json, yet plumbline exits clean, so shell scripts evidently escape the check. Proof that invented tags slip in where the hook does not look.", "candidates": ["Convert or delete the eleven tag sites", "Extend plumbline shell coverage upstream if intended"], "at": "2026-07-22T02:38:08Z"}
{"id": "lax-yaml-decoding", "event": "open", "kind": "human", "category": "enforcement-gap", "artifacts": [], "summary": "No strict YAML decoding anywhere: unknown config keys silently ignored at every boundary", "detail": "Zero KnownFields uses across all twelve yaml.Unmarshal sites. LoadRimskyConfigYAML hand-rejects exactly five retired keys; any other unknown key (typo, guess, stale example) is silently ignored in rimsky.yml, the supervisor YAML, and service configs. Failure mode: set a plausible-but-wrong key, observe no effect and no error.", "candidates": ["yaml.Decoder with KnownFields(true) at every loader, keeping the five hand-rejections only where their retirement-specific messages add value"], "at": "2026-07-22T02:38:08Z"}
{"id": "oversized-files", "event": "open", "kind": "human", "category": "granularity", "artifacts": [], "summary": "~40 files exceed 700 lines against the ~500-line guideline; monoliths shrink the space of conflict-free worktree packets", "detail": "Worst live-code offenders: lib/foundation/persistence/sqlite/nodes.go 1416 and postgres twin 1226, test/support/scenario/harness.go 1204, conformance/claimant_guard.go 1118, lib/graph/node/template_validator.go 1107, claude-agent agentrun.go 1034, controlapi/instances.go 1023. Test outlier lib/runtime/auto_terminal_test.go 2766. No mechanical check exists; the parallel worktree-wave workflow depends on file-disjoint packets.", "candidates": ["Opportunistic feature-seam splits, prioritizing the persistence nodes.go pair (same seams in both backends) and auto_terminal_test.go; no big-bang wave"], "at": "2026-07-22T02:38:08Z"}
{"id": "anonymous-agent-displacement", "event": "open", "kind": "human", "category": "security-hardening", "artifacts": ["cmd/rimsky-host-agent-proxy/state.go"], "summary": "Anonymous agents share one routing key, so any anonymous agent displaces any other and inherits its dispatch stream", "detail": "The proxy keys its connected-agent table by routing identity and overwrites on collision; every anonymous agent collapses to the single key 'anonymous' (register_auth.go whoami kind: anonymous -> anonymousRoutingIdentity; lib/runtime/hostagent/run.go AnonymousRoutingIdentity). Reachable only in no-auth mode, where there is no inter-agent security boundary to begin with; under real auth each agent carries a distinct key id. Accepted for the peer-auth track (security-remediation 2026-07-15, review finding); per-connection hardening operator-elected 2026-07-17 as a later pass.", "candidates": ["Route anonymous agents by a per-connection identity (agent label / connection id) so two anonymous agents cannot collide even in no-auth mode"], "at": "2026-07-22T04:20:59Z"}
{"id": "http-async-node", "event": "open", "kind": "human", "category": "feature-request", "artifacts": ["lib/services/executors/http-node/server.go", "lib/protocols/proto/v1/executor.proto"], "summary": "http-node cannot represent third-party async work; the AwaitAsyncCallback protocol is built but only claude-agent uses it", "detail": "GitHub issue 23 (open, rimsky-ai/rimsky-core) reports a node that POSTs to kick off a long-running job: the downstream acks 200 in milliseconds and works for 20+ minutes elsewhere, so http-node returns Success and the instance reaches complete while the data plane is still running or already dead. No failure to react to, no signal to retry on. Premise verified against the current tree: executeCore settles Success on any expected status. The mechanism to fix it already exists and is unused outside claude-agent -- AwaitAsyncCallback / AsyncCallbackBody, POSTed to ${callback_url}/v1/callback/{async_ack_id}, correlated on rimsky_node_runs.async_ack_id, durable across supervisor restart, deadline-enforced by max_quiet_period / max_runtime. Two findings from the decomposition. (1) The issue's proposed name 'webhook-node' is unavailable: 'webhook' already denotes the inbound direction (story:sensor-webhook translates external POSTs into messages), so the name would sit adjacent to a sensor doing the opposite thing. (2) Neither await-async nor park lets the cascade continue while the work proceeds -- concept:signal gates cascade-fire on a settling kind (terminal/success, terminal/error/<class>, attribute/<key>/changed) and excludes every transient/* signal before edge lookup; template registration rejects subscriptions targeting a transient leaf. transient/await_async is explicitly not a park (the node stays running) but that buys no subscriber visibility. Both are blocking semantics, which is what the reporter wants -- their complaint is that the node settles too early. A genuine fire-and-continue shape is a different design (settle Success promptly; completion arrives as an independent trigger via sensor-webhook or an attribute/<key>/changed signal) and is not a variant of this one. Close GitHub issue 23 when this ships.", "candidates": ["http-async-node as its own executor type", "mode: async on http-node, passing callback_url + ack id to the downstream and returning AwaitAsyncCallback"], "at": "2026-07-22T11:39:06Z"}
{"id": "http-node-global-timeout", "event": "open", "kind": "human", "category": "config-surface", "artifacts": ["lib/services/executors/http-node/config.go"], "summary": "http-node request timeout is a deployment-wide 60s env var with no per-node override", "detail": "RIMSKY_EXECUTOR_HTTP_NODE_TIMEOUT_MS defaults to 60000 and is read once in LoadOptsFromEnv; there is no per-node template field. A genuinely long synchronous endpoint -- a bulk export, a dataset download, any downstream that holds the connection until the work is done -- is killed at 60 seconds, and the only remedy raises the ceiling for every http-node in the deployment. Structurally identical to the claude-agent silence-timeout defect closed as GitHub issue 22, whose fix is the template to copy: per-node cli.silence_timeout_ms parsed in requestparse.go wins over the env-supplied default in runAgentReal. Blocks the download node in sketch:2026-07-22-download-node-and-byte-sink-claim-capability, which is a long synchronous dispatch by construction.", "candidates": ["Per-node timeout_ms attribute on http-node; demote RIMSKY_EXECUTOR_HTTP_NODE_TIMEOUT_MS to a deployment-wide default"], "at": "2026-07-22T11:39:06Z"}
{"id": "peer-auth-mtls-split-role-broken", "event": "open", "kind": "human", "category": "security-hardening", "artifacts": ["lib/control/config/peer_identity.go", "lib/control/config/supervisor.go", "lib/runtime/peer/credentials.go"], "summary": "peer_auth: mtls only installs an outbound identity on the supervisor; scheduler and control-api dial with no client cert or CA trust in a split-role deployment", "detail": "Verified v0.11.0/current tree. setupOutboundIdentity has exactly one caller, lib/control/config/supervisor.go:109 (the supervisor path); it calls peer.SetClientIdentity + peer.SetTLSRootCAs. The control-api loads the CA only to serve POST /v1/enroll and never installs its own outbound identity; the scheduler only validates the CA encryption key at load and never loads the CA. Unified topology works only because the supervisor's identity lands in process-global state (lib/runtime/peer/credentials.go SetClientIdentity/SetTLSRootCAs) shared by all three roles in one process. Impact: the v0.11.0 mTLS perimeter is effectively unified-topology-only; a three-container split with peer_auth: mtls + tls: required on internal peers gets TLS handshake failures from scheduler and control-api outbound dials.", "github": 25, "candidates": ["Install an outbound peer identity in the scheduler and control-api launch paths, not only the supervisor"], "at": "2026-07-24T00:55:18Z"}
{"id": "cli-distribution-channel", "event": "open", "kind": "human", "category": "release-process", "artifacts": ["RELEASING.md"], "summary": "CLI distribution — RESOLVED IN FLIGHT: prebuilt binaries via goreleaser (linux/darwin, amd64/arm64); go install unsupported by module layout; no Windows build", "detail": "Was: no release ships a rimsky CLI binary; build-from-source the only path, unblessed. Implemented this session (owner ruling: goreleaser owns the GitHub Release, approach A): .goreleaser.yaml builds linux/darwin x amd64/arm64 archives (rimsky binary + all license files) + checksums; the /release skill step 7 now runs `goreleaser release --clean --release-notes=releases/vX.Y.Z.md` instead of gh release create, keeping curated notes; Makefile cli-release replaced by cli-check/cli-snapshot (verified via `goreleaser release --snapshot` — 4 clean archives produced); cmd/rimsky/version.go gained a runtime/debug.ReadBuildInfo fallback so non-ldflag builds report the real version; RELEASING.md documents the install paths. Verified locally; the real `goreleaser release` publish is unverified (needs a tagged release, gated). Close GitHub 26 when the first release ships binaries. TWO BUGS surfaced and handled: (1) the CLI cannot cross-compile to Windows — it transitively embeds Unix-only syscalls (claude-agent clirunner Setpgid/Kill; filesystem store Stat_t); the old cli-release target claimed a windows_amd64 build that never compiled. Fixed by dropping Windows (Unix-only matrix, documented as deliberate). (2) go install github.com/rimsky-ai/rimsky-core/cmd/rimsky@vX.Y.Z does NOT work: the root go.mod pulls lib/* via replace => ./lib/*, and go install pkg@version ignores replace, so it fetches unpublished lib/*@v0.0.0 and fails. Documented as unsupported; earlier 'go install works today' claim was wrong.", "github": 26, "candidates": ["Ship binaries via goreleaser (DONE this session, publish step pending first release)", "Optional: add a Homebrew tap (goreleaser brews block + a homebrew-tap repo)", "Optional: add syft-based SBOMs on the CLI archives to match the image attestation posture", "To ever support `go install`: publish the lib/* sub-modules with real versions and drop the placeholder require+replace"], "at": "2026-07-24T00:55:18Z"}
{"id": "lock-scope-substitution-silent-failure", "event": "open", "kind": "human", "category": "diagnosability", "artifacts": ["lib/runtime/runner_acquire.go", "lib/runtime/runner.go", "lib/runtime/gate_evaluator.go", "proto:events.proto"], "summary": "Failed substitution in a lock_name:/scope: directive emits no event (invisible retry loop); template_resolution_failed.site is always hardcoded 'attribute'", "detail": "Verified two gaps. (1) buildLockSpecs substitution failure at lib/runtime/runner_acquire.go:428-430 logs a Warn and returns (acquisition{}, false, nil) — the candidate is silently skipped and retried every sweep forever; the failure never reaches the event log, classifyAttributeFailure, or error_types:. A bad directive presents as a node stuck stale with no event-log trace. (2) The site field of template_resolution_failed is hardcoded \"attribute\" at both emission sites (lib/runtime/runner.go:323 applyAttributeFailure, lib/runtime/gate_evaluator.go:143 routeSubstitutionFailureAtGate), while proto:events.proto TemplateResolutionFailedPayload.site documents an intended attribute|lock_name|scope taxonomy. No 'Site:' literal for lock_name/scope exists anywhere.", "github": 27, "candidates": ["Emit an event on lock-spec substitution failure and route it through classifyAttributeFailure", "Emit the documented site values (lock_name | scope) at their respective sites"], "at": "2026-07-24T00:55:18Z"}
{"id": "mcp-delete-route-unwired", "event": "open", "kind": "human", "category": "dead-code", "artifacts": ["lib/control/controlapi/mcp_route.go", "lib/control/controlapi/mcp/server.go"], "summary": "mcp.Server handles DELETE session-close (and advertises Allow: GET, POST, DELETE) but the router registers only GET/POST, so DELETE is unreachable in v1", "detail": "Verified: lib/control/controlapi/mcp_route.go:45-46 registers only r.Post(\"/mcp\") and r.Get(\"/mcp\"); chi intercepts unregistered methods before the mounted handler, so a real DELETE /v1/mcp gets chi's 405 (Allow: GET, POST, empty body). The MethodDelete branch and the handler's Allow: GET, POST, DELETE in lib/control/controlapi/mcp/server.go are reachable only from the unit test calling ServeHTTP directly. Consequence: no working clean-session-close in v1; abandoned sessions expire only via the 30-min idle timeout.", "github": 28, "candidates": ["Wire r.Delete in registerMCPRoute (makes the handler's Allow header true)", "Remove the dead DELETE branch and drop DELETE from the Allow header"], "at": "2026-07-24T00:55:18Z"}
{"id": "callback-host-dead-knob", "event": "open", "kind": "human", "category": "dead-code", "artifacts": ["lib/services/executors/claude-agent/opts.go"], "summary": "RIMSKY_EXECUTOR_CALLBACK_HOST is read into claude-agent Opts.CallbackHost but never consumed; the internal MCP server hardcodes 127.0.0.1", "detail": "Verified: lib/services/executors/claude-agent/opts.go:105 reads RIMSKY_EXECUTOR_CALLBACK_HOST into Opts.CallbackHost (field declared opts.go:64); no code in the package consumes the field — the only other reference is the struct declaration. The internal MCP server hardcodes 127.0.0.1 (agentrun.go). Setting the env var has no effect. (Distinct from the endpoint-env-var-collision row, which is about naming collisions and undocumented vars, not a read-but-unconsumed knob.)", "github": 29, "candidates": ["Honor the field (thread CallbackHost into the internal MCP server bind)", "Delete the env read and the field so operators aren't offered a no-op knob"], "at": "2026-07-24T00:55:18Z"}
{"id": "copying-md-apache-surface-drift", "event": "open", "kind": "human", "category": "licensing", "artifacts": ["COPYING.md", "licensing.yml"], "summary": "COPYING.md's Apache surface prose is wrong on both sides: cites a nonexistent cold-read/ carve-out and omits examples/", "detail": "Verified broader than filed. COPYING.md:56 describes the Apache surface as 'lib/protocols/, plus the cold-read/ carve-out'. No cold-read/ directory exists in the tree (the only live 'cold-read' references are in archived/discover records). licensing.yml — the mechanical per-file map — lists apache: lib/protocols/ AND examples/ (licensing.yml:26,28). So COPYING.md both invents cold-read/ and drops examples/. Align the prose to licensing.yml.", "github": 30, "candidates": ["Reword COPYING.md's Apache surface to match licensing.yml: lib/protocols/ + examples/, no cold-read/"], "at": "2026-07-24T00:55:18Z"}
{"id": "lifecycle-firing-site-doc-conflict", "event": "open", "kind": "human", "category": "design-corpus", "artifacts": ["concept:lifecycle-subscriber", "proto:lifecycle.proto", "lib/control/controlapi/instance_terminator.go"], "summary": "Lifecycle firing-site doc/code labeling mismatch — OWNER RULING 2026-07-24: status quo, by-design for now, revisit the protocol later", "detail": "Finding (docs-reconcile v0.11.0): concept:lifecycle-subscriber says main-scope run-scope-terminal fires 'synchronously within the request that performs the transition', while proto:lifecycle.proto and the code fire it from the poll loop — CloseAndFanOutRunScopesForInstance for a main scope is called from lib/control/controlapi/instance_terminator.go:139 (InstanceTerminator.tick). Separately surfaced this session: delivery itself is a synchronous outbound gRPC (s.OnRunScopeTerminal) made INSIDE a DB transaction under the per-scope advisory lock (lib/control/controlapi/lifecycle.go fanOutRunScopePeer), so a slow/hung subscriber stalls the firing path and pins a tx+lock — network I/O inside a DB tx. OWNER RULING (owner, this session): keep the concept doc's 'within the request' wording; treat synchronous-blocking lifecycle delivery as by-design for now; make NO doc or code change at this time; no separate ledger row for the blocking-delivery concern. The lifecycle-subscriber protocol is to be revisited later as a whole (trigger labeling AND the queue/outbox decoupling of delivery). Parked, not resolved.", "github": 31, "candidates": ["No action now (owner ruling: status quo / by-design); revisit the lifecycle-subscriber protocol later — reconcile the trigger labeling and consider queue/outbox delivery so a slow subscriber never blocks a rimsky-side tx"], "at": "2026-07-24T00:55:18Z"}
{"id": "signal-taxonomy-prose-enumeration", "event": "open", "kind": "human", "category": "design-corpus", "artifacts": ["concept:signal", "concept:transition-reason"], "summary": "Two concept docs, two conventions for a code-owned closed set: signal.md hand-enumerates the taxonomy leaves; transition-reason.md declines to and defers to the code", "detail": "Verified: concept:transition-reason states membership 'is owned by the state-machine code, not enumerated here' and that the runtime guard (not the type system) enforces the closed set. concept:signal hand-enumerates the closed taxonomy leaves (terminal/success, terminal/error/<class>, transient/*, attribute/<key>/changed) plus closed payload field names, which drifts silently against the code-owned taxonomy (lib/foundation/signal/taxonomy.go). Pick one convention repo-wide; the transition-reason posture composes better with generated references. Design-corpus change: rides /sprint.", "github": 32, "candidates": ["Adopt the transition-reason convention in signal.md: describe the taxonomy, defer the enumerated leaves to the code"], "at": "2026-07-24T00:55:18Z"}
{"id": "rimsky-yml-no-auth-invariant-stale", "event": "open", "kind": "human", "category": "design-corpus", "artifacts": ["concept:rimsky-yml", "lib/control/config/claim_producers.go"], "summary": "concept:rimsky-yml invariant 'No auth-related keys' contradicts the peer_auth: loader key added by the security perimeter", "detail": "Verified: concept:rimsky-yml carries an invariant that operators configure no auth mode / no auth knob in the yml (auth state is data-derived from the API-key ledger). But the loader accepts the deployment-wide peer_auth: none|mtls key (lib/control/config/claim_producers.go:346 yaml tag, :238 value validation, :585 mtls requirement). The invariant predates the peer-auth work. Narrow it — e.g. 'no credential material in the YAML' if that is the surviving intent — so peer_auth is not a contradiction. Design-corpus change: rides /sprint.", "github": 33, "candidates": ["Narrow the invariant to 'no credential material in the YAML' (or equivalent), acknowledging peer_auth: as a legal non-credential auth-posture key"], "at": "2026-07-24T00:55:18Z"}
{"id": "async-callback-no-bearer-under-peer-auth-none", "event": "open", "kind": "human", "category": "security-review", "artifacts": ["lib/runtime/callback.go", "lib/runtime/keepalive.go"], "summary": "Confirm intentional: async terminal callback POST /v1/callback/{async_ack_id} carries no bearer under peer_auth: none — ack id is the sole capability", "detail": "Verified the surface, not a defect. lib/runtime/callback.go:149 mounts POST /v1/callback/{async_ack_id} bare; the per-run routes /v1/runs/{run_id}/keepalive and /attributes (callback.go:151-152) are bearer-guarded via cancel_token (keepalive.go authorizeCancelToken). Under peer_auth: mtls the callback is bound to the dispatched executor's principal. Open question for the owner: is the bearer-less peer_auth:none posture intentional (unguessable ack id as capability token)? NOTE the filed remedy — 'a short code comment on the route mount' — conflicts with Plumbline's no-comments rule; the proof must be a mechanical check, not a comment. Alternative proof surfaces: a decision doc entry with a test asserting the route mounts unauthenticated under peer_auth:none, or moving the per-run bearer scheme onto the callback if the answer is 'not intentional'.", "github": 35, "candidates": ["Ratify as intentional; record a decision doc + test asserting the bearerless mount under peer_auth:none (not a code comment)", "If not intentional, extend the per-run cancel_token bearer scheme to the callback route"], "at": "2026-07-24T00:55:18Z"}
{"id": "module-mcp-transport-no-factories-in-image", "event": "open", "kind": "human", "category": "shipped-surface-gap", "artifacts": ["lib/services/executors/claude-agent/moduleloopback.go"], "summary": "claude-agent module/http-loopback MCP transport resolves only against in-process-registered factories; the published image registers none, so transport: module can never resolve there", "detail": "Verified: the module/http-loopback transport resolves against factories registered via claudeagent.RegisterMcpModule (moduleloopback.go:32). The only callers are the test-only fake CLI (lib/services/test/scenarios/claude_agent_fake_cli/.../main.go) and a unit test — the shipped rimsky-executor-claude-agent binary registers none. A template declaring transport: module against the published image can never resolve.", "github": 36, "candidates": ["Register useful bundled modules in the shipped binary", "Document the transport as embedder-only (build-your-own-binary)", "Reject transport: module in the published binary with an error naming the situation"], "at": "2026-07-24T00:55:18Z"}
{"id": "object-store-sensor-cloud-backends-absent", "event": "open", "kind": "human", "category": "design-corpus", "artifacts": ["concept:sensor", "decision:object-store-watching-model", "story:sensor-object-store", "lib/services/sensors/sensor-object-store/sensor.go"], "summary": "sensor-object-store ships filesystem + memory backends only; its decision doc asserts 'real object stores' that don't exist. RULING: keep the sensor and its pluggable abstraction, reword the decision doc to be filesystem-only-honest.", "detail": "Verified: the sensor is shipped code (story + decision both status: as-is), with a one-method ObjectLister abstraction (sensor.go:34), a listers registry keyed by config 'backend' (SetBackend, sensor.go:143), and two backends registered in main.go — memory (unconditional, a test fixture) and filesystem (only if RIMSKY_SENSOR_OBJECT_STORE_FS_ROOT set). No s3/gcs/azure code exists; an unregistered backend hits a generic 'not serviceable by this build' error at Subscribe. decision:object-store-watching-model over-promises: 'the local filesystem as one backend ... alongside real object stores' asserts cloud backends that don't ship. RULING (owner, this session): do NOT rename to sensor-filesystem-watcher (considered and reversed — the ObjectLister seam is one method, so s3/azure are cheap drop-in listers worth keeping the generalization for). Keep the sensor and abstraction as-is; the only change is a corpus delta rewording decision:object-store-watching-model to say the model is generalized/pluggable and admits real object stores as backends, while the current build registers filesystem only (memory being a test fixture, not a shipped store). This is the deliberate-boundary ruling issue 37 asked for. Rides /sprint (design-corpus edit).", "github": 37, "candidates": ["Reword decision:object-store-watching-model: generalized pluggable-backend model, filesystem-only in the current build, cloud backends admitted-by-design not-yet-implemented"], "at": "2026-07-24T00:55:18Z"}
{"id": "bundled-producers-no-data-processing-advert", "event": "open", "kind": "human", "category": "shipped-surface-gap", "artifacts": ["lib/services/claim_producers/filesystem/server/server.go", "lib/services/claim_producers/postgres/server/server.go", "lib/protocols/claimproducer/types.go"], "summary": "Neither bundled claim producer advertises data_processing, so lifetime: durable (asset) claims are not exercisable on the bundled stack", "detail": "Verified: declaredProtocols in both producers can only ever emit lifecycle_subscriber (filesystem, server.go:61) or that plus executor (postgres, server.go:62) — data_processing (claimproducer.ProtocolDataProcessing) is never appended. So the documented lifetime: durable asset shape (durable claim outliving its subgraph, available for co-holds and asset-presentation queries) cannot be exercised on the bundled stack. Referenced by sketch:2026-07-22-download-node-and-byte-sink-claim-capability as the reason the asset path is not reachable and why the download-node reporter reached for the executor surface.", "github": 38, "candidates": ["Grow the data_processing advert on a bundled producer", "State in the producers' READMEs that the asset surface requires a data-processing-capable producer (documented boundary)"], "at": "2026-07-24T00:55:18Z"}
{"id": "no-bundled-park-resume-demo", "event": "open", "kind": "human", "category": "demonstrability", "artifacts": ["lib/services/executors/http-node/server.go"], "summary": "No self-contained way to demonstrate real park-then-resume on the bundled stack; the only real-park path needs an external 429 endpoint", "detail": "Verified: http-node's only non-stub Park is the 429 -> Park path (server.go:189, tagged rate_limited), which requires an external endpoint returning 429. The other park/cancel paths are s.stubMode-gated probes (probe_park), for conformance probing, not a real driven-to-resume journey. So no clean-state copy-runnable park-then-resume recipe exists against the bundled stack. Adjacent to issue 23 / the http-async-node and download-node work: a first-class induce-a-real-park knob would also give the cookbook an executable park-resume journey.", "github": 39, "candidates": ["A stub-mode knob that parks with a short resume_at", "A documented 429-returning test endpoint bundled in the stack"], "at": "2026-07-24T00:55:18Z"}
{"id": "store-claim-producer-vocabulary-split", "event": "open", "kind": "human", "category": "vocabulary", "github": 34, "artifacts": ["lib/services/claim_producers/filesystem/server/opts.go", "lib/services/claim_producers/postgres/server/opts.go", "lib/services/claim_producers/filesystem/Dockerfile.filesystem", "lib/services/executors/claude-agent/expected_attributes_schema.json"], "summary": "Additional user-facing 'store' stragglers beyond the internal identifiers already recorded on this id (GitHub 34)", "detail": "Complements the 2026-07-22 row (internal Go identifiers / package names / test filenames / example template names) with the shipped, user-facing surface verified this pass: config env vars STORE_FILESYSTEM_CONFIG (filesystem/server/opts.go:21) and STORE_POSTGRES_CONFIG (postgres/server/opts.go:22); container binary/entrypoint names store-filesystem / store-postgres (Dockerfile.filesystem:24, Dockerfile.postgres:24); and the claude-agent attribute cwd_from_store, live in the shipped expected_attributes_schema.json and its error text. Pre-v1, no compat guarantees — rename, or bless as legacy names, to stop each docs reconcile re-flagging them.", "at": "2026-07-24T00:55:18Z"}
