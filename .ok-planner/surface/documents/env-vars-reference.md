---
document: env-vars-reference
target: docs/env-vars.md
---
# Environment variable reference

## Purpose
An operator deploying rimsky or a bundled service opens it to find every environment variable the shipped code reads, its default, and its effect. They close it able to configure a deployment.

## Covers
- every public environment variable
