#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/../../.."
git grep -h -o -E 'yaml:"[a-z0-9_]+' -- 'lib/control/config/' 'lib/control/launch/' 'cmd/rimsky/cli/config.go' ':!*_test.go' \
  | sed -E 's/^yaml:"//' | sort -u
