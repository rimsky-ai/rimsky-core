# Restore-feature adjudication worksheet

Source: Phase-2 feature-loss sweep, 79 `direction=restore-feature` ledger rows (ids 2367–2625, `source=phase2-sweep:*`). Grouped by theme; the **Disposition** column is for your ruling. Groupings are a first-pass heuristic — move any row you disagree with. Once ruled, group A/B items that survive become Phase-3 code rows; group C items become dossier-reconciliation edits.

Totals: 35 unexecuted-ruling · 24 genuine-gap · 20 likely-superseded · **79 total**.

## A. Unexecuted rulings (35)

A ratified 2026-07-14 / 07-17 / 06-21 ruling (or a numbered finding) that was never applied to code — the old behavior is still live, and in many cases a green test actively pins it. Default disposition: **execute the ruling** (remove/change the code, retire the pinning test). Rule per item.

| ID | Dossier | Behavior | Current code state | Pinning test | Disposition |
| --- | --- | --- | --- | --- | --- |
| 2589 | `attribute` | attribute/<key>/changed emission is diff-based and uniform across every settle path, same-RunScope-only baseline (never cross-frame/cross-scope); for a scope's FIRST run the basel… | contradicted; lib/runtime/attribute_cascade.go::emitAttributeChangesForRunInTx#36-40 diffs current.Data against GetPriorRunData; when no prior exists GetPriorRunData returns empty, so diffAttributesData(nil,current)#64-… | unguarded; The uniform-diff + same-scope baseline is guarded (uniform_attributes_delta + diffAttributesData), but the 2026-07-14 first-run SCHEMA-DEFAULT baseline is NOT implemented — code fires attribute/<key>/changed … | **EXECUTE** — schema-default baseline. User: defaults are the initial values of the attributes; an attribute is 'empty' only if no default was provided and no upstream node substituted one. First run writing exactly its defaults fires nothing. |
| 2412 | `breakpoint` | Pause-mode fails CLOSED on infra error evaluating/persisting a pause hit; after_terminal stays fail-open logged | missing; lib/runtime/runner_dispatch.go:461-472 swallows every *BreakpointInfraError (incl. pause-phase create_hit/overflow/wait_resume from breakpoint_eval.go:158-183) and dispatches anyway — fail-OPEN on the pause path | unguarded; breakpoint_eval_test infra-error tests assert only that EvaluateBreakpoints returns the error, not the caller's disposition; current fail-open code passes all tests | **EXECUTE** — pause path fails CLOSED on breakpoint infra error per the 07-17 ruling (user re-confirmed: a gate that errors must never wave runs through). after_terminal stays fail-open-logged: observation-only, nothing to gate. Guard test must assert the CALLER's disposition, not just the evaluator's error return. |
| 2413 | `breakpoint` | Async-callback after_terminal contexts carry no GraphName/child_key | missing; Contradicted: callback.go:739-740 passes Graph:acq.GraphName + ChildKey:scope.PartitionKey; reconstructAcquisition (callback.go:433-435) populates GraphName | unguarded; No test asserts presence or absence of GraphName/child_key on async after_terminal hits | **RECONCILE DOSSIER + GUARD** — the gap is already fixed in code (callback.go passes Graph/ChildKey; reconstructAcquisition populates GraphName). Strike the recorded limitation from the dossier; add a guard test asserting an async-callback after_terminal hit carries GraphName/child_key. No production code change. |
| 2568 | `cascade` | AMENDED 2026-07-14: transient/retry removed from the cascade-firing / subscribable set -- only settling events cascade-fire or are subscribable; the set is exactly {terminal/succe… | contradicted; lib/foundation/signal/taxonomy.go#24-32 canonicalSubscriptionPatterns STILL lists transient/retry/*, transient/infra/*, transient/release_and_requeue/*, transient/await_async as subscribable; ValidateSubsc… | unguarded; Worse than unguarded: lib/foundation/signal/taxonomy_test.go#59-72 TestValidateSubscriptionType_AcceptsTrailingWildcard PINS the retired behavior -- it asserts ValidateSubscriptionType('transient/retry/*') re… | **EXECUTE** — cascade proceeds ONLY on a terminal. Narrow subscribable set to settling types, retire the pinning wildcard assertion, add inverse guard (subscribing to a transient = validation error naming the type). User also flagged the recurring executor-outcome vs node-signal vocabulary conflation for a naming split — investigated separately. MECHANISM (ratified): signal.ShouldCascade(typePath) predicate; canonicalSubscriptionPatterns becomes DERIVED = filter(emit, ShouldCascade) — no second list to drift; park carve-out folds into the predicate keeping its error text; guard test pins derived set == {terminal/success, terminal/error/*, attribute/*/changed}. |
| 2617 | `control-api` | Emergency-abort teardown (a terminate that finds open frames) must close EVERY open run scope in each frame's tree (children before parents), fire OnRunScopeTerminal once per scop… | missing; lib/control/controlapi/lifecycle.go::CloseAndFanOutFrameRootRunScopesForInstance#289 iterates frames and fires ONLY f.RootRunScopeID#320 — ROOTS-ONLY. All three teardown callers use it: instance_terminator.go#1… | unguarded; No test asserts nested-scope terminal fanout on teardown of an instance with in-flight sub-graph / fan-out-partition scopes. The roots-only path passes existing tests because they terminate quiescent instance… | **EXECUTE** — teardown walks the FULL scope tree: close every open run-scope, children before parents, fire OnRunScopeTerminal exactly once per scope. Guard test terminates an instance with an in-flight sub-graph + fan-out partition and asserts every scope fired. (Same ruling as 2584.) |
| 2618 | `control-api` | AuthState is required: a nil AuthState must fail loudly at construction/serve, never run the API ungated; the nil-permissive gate branch must be removed (2026-07-14 user ruling — … | missing; lib/control/controlapi/auth_middleware.go::gate#339-344 STILL has `if deps.AuthState == nil { return inner }` — returns the handler UNGATED when AuthState is nil; parallel nil branches at auth_routes.go#13 (ski… | unguarded; No test asserts that a nil AuthState fails construction/serve; the surviving `return inner` branch means a nil AuthState silently runs every gated route with no auth, no audit identity, no anonymous-mode safe… | **EXECUTE** — remove both nil-AuthState branches (gate#339-344, auth_routes.go#13); construction/serve errors loudly on nil. Guard: constructing the API without AuthState fails; no ungated serving mode exists. Tests wanting no-auth use explicit anonymous mode. |
| 2619 | `control-api` | Diagnostics parked route collapses to ONE route — the duplicate alias is residue (2026-07-14 user ruling) | present; lib/control/controlapi/admin_diagnostics.go registers BOTH /admin/diagnostics/parked-nodes#41 AND the alias /diagnostics/parked#42 to the same handleAdminParkedNodes; actions.go#352-353 carries both routes unde… | unguarded; Both routes are registry-covered so TestRegistryCoversRouter stays green with the alias present or absent — nothing forces the collapse. The 2026-07-14 ruling declares the alias residue to remove | **EXECUTE** — keep /admin/diagnostics/parked-nodes (matches admin-prefix convention), delete the /diagnostics/parked alias, pin one-route-per-action in the registry test so aliases cannot silently accrete. |
| 2470 | `discovery-cache` | RESOLVED (2026-07-14): ref_validation_mode knob and execSchemaVisible skip RETIRED erase-completely; per-name strictness only | missing; template_validator.go still defines RefValidationMode + RefValidateAll/Available/None (54-84) gating checks at 885/1075/1165/1266; execSchemaVisible present (1248-1283); deps.RefValidationMode wired in template… | unguarded; retirement not done; TestValidateTemplate_RefMode actively asserts the retired available/none behavior | **EXECUTE** — erase ref_validation_mode (all three modes), execSchemaVisible, and the deps wiring; per-name strict validation unconditionally. Retire TestValidateTemplate_RefMode; inverse guard: the config key is rejected as unknown so it cannot return. |
| 2573 | `event-log` | USER RULING 2026-07-17: permission_denied access-denied rows populate the mode field with the requested mode (dry-run vs enforce), keeping the audit mode filter meaningful for den… | missing; lib/control/controlapi/audit.go::emitDenied#56-93 builds AccessDeniedPayload with ProtocolSkin, path, params, DenialReason, etc. but NEVER sets Mode -- even though the payload struct carries a Mode field (audit… | unguarded; No test asserts a permission_denied denial row carries the requested mode. The dossier itself flags this 'code currently never sets it (queued fix-code)'. | **EXECUTE** — emitDenied stamps the requested mode on every permission_denied row. Guard test asserts an enforce-denial and a dry-run-denial each carry their mode value. |
| 2596 | `executor` | Callback advertise: supervisor must FAIL FAST at startup rather than silently stamping http://0.0.0.0:9100 into callback_url when advertise is empty and bind is wildcard; the four… | partial; lib/runtime/supervisor.go::effectiveCallbackHostPort#284-292 returns bindHost when advertiseHost=='' with NO wildcard-bind rejection — it silently stamps whatever the listener bound (potentially 0.0.0.0); adver… | unguarded; The URL-collapse is present but the fail-fast guard is absent: effectiveCallbackHostPort proceeds with the wildcard bind host instead of erroring, and supervisor_test.go has no advertise/wildcard test. Nothin… | **EXECUTE** — wildcard bind + empty advertise = startup error naming callback.advertise_host / RIMSKY_SUPERVISOR_CALLBACK_ADVERTISE_HOST; explicit non-wildcard bind host remains a legal advertise fallback. Guard tests for both legs. |
| 2379 | `frame` | terminated as a distinct terminal frame state; admin kill drives in-flight frame to terminated with ended_at | missing; {postgres,sqlite}/frames.go derived-state CASE yields only running/failed/completed; instance kill leaves in-flight frames deriving running/failed | unguarded; no test asserts a terminated derived state; kill casualties indistinguishable from genuine failures | **EXECUTE** — 'terminated' added as a distinct terminal frame state in both drivers' derived-state CASE; the kill path stamps its casualties terminated+ended_at. Guard: kill-with-in-flight-frame asserts terminated; genuine failure still derives failed. Implement alongside 2617's scope-tree teardown (same kill path). |
| 2383 | `frame` | Lineage records capture frame-trigger fields (finding 442) | partial; lineage_writer.go declares FrameTriggerKind/TriggerMessageID but no assignment site populates them; missingLeafRunFields warn-once | unguarded; fields are dead; no test asserts population | **EXECUTE** — plumb frame_trigger_kind + trigger_message_id from the frame-open site into LeafRunEmitInput; delete missingLeafRunFields + the warn-once apology log (residue). Guard: operator-message-opened frame asserts leaf lineage rows carry both trigger fields. (Same ruling as 2620.) |
| 2384 | `frame` | Frame-timeout retirement (2026-07-14 ruling): frame_timeout_ms, >=60000 CHECK, stuck-frame warn sweep all removed | missing; retirement NOT executed: migrations still carry frame_timeout_ms + CHECK; frame/engine.go::runWarnStuckFrames live; template_validator.go::validateFrameTimeout live; frame_timeout_*_test.go still green | unguarded; the surviving frame_timeout tests guard the OLD behavior the ruling deletes | **EXECUTE** — full frame-timeout erasure: migration drops frame_timeout_ms + CHECK (no compat shim, pre-v1), validateFrameTimeout deleted (unknown-key rejection guards its return), runWarnStuckFrames deleted, fixtures scrubbed (incl. portable-template body), frame_timeout_* pinning tests retired. |
| 2385 | `frame` | Park-timeout watchdog retirement (2026-07-14 ruling): max_park_duration, parked→failed sweep, park_timeout gone | missing; retirement NOT executed: scheduler.go MaxParkDuration/PerReasonMaxPark; template_validator.go::validateMaxParkDuration; parked_lifecycle_test::TestParkedLifecycleMaxParkDurationOverrun live | unguarded; surviving watchdog tests guard the retired behavior | **EXECUTE** — full park-watchdog erasure: MaxParkDuration/PerReasonMaxPark config (unknown-rejected), validateMaxParkDuration, sweep_parked.go reaper, ListParkedOverdue, park_timeout error class all deleted; parked_lifecycle pinning tests retired. Deadlines belong to resume_at (executor) or cancel (operator), never a platform reaper. (One sweep with 2525+2526.) |
| 2509 | `host-agent` | Bundled production service binaries honor RIMSKY_AGENT_PORT first with fallback to their own port var | missing; Verified: zero non-test RIMSKY_AGENT_PORT hits under lib/services; sensor-cron/main.go:31 reads RIMSKY_SENSOR_CRON_PORT; claude-agent/opts.go:78 reads RIMSKY_EXECUTOR_PORT_GRPC | unguarded; Only test stubs read RIMSKY_AGENT_PORT; no test asserts any bundled binary honors it | **EXECUTE** — shared port-resolution helper (RIMSKY_AGENT_PORT → service-specific var → default), one definition swept across all eleven bundled binaries. Guard: late-bind one real bundled binary via the harness, assert readiness. (Same ruling as 2623.) |
| 2551 | `inertness` | Park reason_label opaque (persistence only, never routing); park/snooze payload bytes handed back verbatim on resume, typed apart from Error.payload | partial; reason_label present and inert: lib/protocols/proto/v1/executor.proto::Park.reason_label (free-form, 'Persisted on col:rimsky_node_runs.parked_reason_label'; routing is on the closed ParkReason enum), threaded … | guarded; For the surviving leg: lib/runtime/runner_terminal_park_test.go (tests at lines 20 and 43) asserts parked_reason_label ('wait', 'wait_for_remote') lands verbatim in the park event payload — dropping or transfor… | **EXECUTE (reason leg) / KEEP (scratch leg)** — reason_label persistence erases with 2524 (overrides the wave-4 'ratified' read; note: deletes our own batch-2 http-node reason_label plumbing fix). Scratch-bytes-replayed-verbatim stays, already guarded. |
| 2575 | `instance` | Force-terminate semantics: running frame finishes, no new frames open, new messages rejected, and PENDING MESSAGES are cancelled via CancelPendingForInstance (finding 437 / resolv… | partial; lib/control/controlapi/instances.go::handleTerminateInstance#633-719 force-fails running/parked/held runs (terminal/error/instance_killed), abandons in-flight claims, MarkTerminated, appends instance_terminated… | unguarded; No test asserts that terminating an instance cancels its pending queued messages. instance_terminate_claims_test.go covers claims only. A pending message queued before terminate is left uncancelled (latent le… | **EXECUTE** — duplicate of 2516 (instance-dossier view of finding 437). |
| 2386 | `lineage` | Symmetric claim walks with truthful route names (descendants down sub-claims + genuine ancestors up parent pointer) | partial; controlapi/lineage.go ships only /lineage/claims/{id}/ancestors and it walks SubClaimHandleIDs DOWNWARD; no descendants route, no parent-pointer walk | unguarded; existing tests pass on the mislabeled route; nothing asserts a true upward walk | **EXECUTE** — duplicate of ledger row 450 (user ruling 2026-07-17, option A, already queued fix-code): rename claims 'ancestors' route to 'descendants'; build the true ancestors walk up the claim-tree parent pointer; guard both directions. duplicate_of=450. |
| 2620 | `lineage-record` | Frame-trigger fields (frame_trigger_kind, trigger_message_id) plumbed into leaf_run lineage records — the code's self-declared 'NOT YET plumbed' gap is a fix-code ruling (finding … | missing; lib/runtime/lineage_writer.go — LeafRunRecord DECLARES FrameTriggerKind#46 + TriggerMessageID#47, but LeafRunEmitInput#142-161 has NO such fields and EmitLeafRunLineage's record literal#189-208 never assigns th… | unguarded; lib/runtime/lineage_writer_test.go#379-380 only checks the JSON-tag mapping (omitempty), not population. No test asserts a real frame_trigger_kind / trigger_message_id value on … | **EXECUTE** — duplicate of 2383 (lineage-record view); one implementation closes both. |
| 2513 | `message` | {{messages.<type>.<field>}} alias for {{nodes...}}: one pipeline, registration type-checked, NO auto-subscribe | partial; template_validator.go:178-215 one pipeline, registration-checked; BUT subscription_edges.go:255-258 still injects a subscription edge per message ref (edgeFromMessageRef) | guarded; Registration checking guarded by TestValidateMessageSubstitutionRef_*; runtime resolution by TestStoryTypedMessageSubstitution_RuntimeResolutionThroughBackEdge; the no-auto-subscribe half is anti-guarded — Test… | **EXECUTE** — with 2517: {{messages.<type>.<field>}} stays a pure alias through one pipeline, registration type-checked, NO auto-subscribe side effect. |
| 2516 | `message` | Instance disable/terminate calls CancelPendingForInstance (finding 437, adjudicated fix-code) | missing; controlapi/instances.go::handleTerminateInstance (tx :692-716) marks runs failed, MarkTerminated, stops publisher subs — zero CancelPendingForInstance calls; sole caller is message_delivery.go:51 (coalesce) | unguarded; No test asserts terminate cancels pending messages; TestMessages_TargetTerminatedInstanceConflict only checks new-message rejection | **EXECUTE** — finding 437: instance disable/terminate calls CancelPendingForInstance; pending queued messages cancelled. Guard: terminate-with-pending-message asserts cancellation. (Same as 2575.) |
| 2517 | `message` | Remove the messages.* auto-subscribe edge injection (finding 1312, adjudicated fix-code) | missing; lib/graph/node/subscription_edges.go:255-258 STILL injects a subscription edge per message substitution ref (edgeFromMessageRef) | unguarded; Removal not done and anti-guarded: TestBuildSubscriptionEdges_ImplicitFromMessageRef asserts the injected edge EXISTS, pinning the drift | **EXECUTE** — finding 1312: remove the messages.* auto-subscribe edge injection; subscriptions are explicit only. Retire any test pinning the injected edge; inverse guard asserts no implicit edge appears. (2513 is the same pipeline's spec side.) |
| 2422 | `message-schema` | Bodies schema-validated at ledger insertion for every producer (operator/publisher POST 400; cascade-emit same check) | missing; handleCreateMessage validates type only (messages.go:109-227); EnqueueMessage and sendCascadeMessageInTx (runner_send_message.go:42-98) insert bodies unvalidated; body_schema only compiled at registration | unguarded; No reject-at-insert test exists; worse, an INVERTED guard TestCreateMessage_AdmitsPayloadFailingBodySchema asserts an invalid payload gets 201 — it locks the defect in | **EXECUTE** — RESOLVED 07-14: bodies schema-validated at ledger insertion, one chokepoint, all three producer paths (operator POST, publisher POST, cascade-emit) → 400 naming the violation; delivery trusts the ledger. Retire the inverted no-validation test; rewrite the dossier's stale body line to its own Conflicts resolution. |
| 2427 | `observability` | declared_tags enforced at two gates: registration REJECTS undeclared subscription tags; terminal handler rejects undeclared settling tags | partial; Gate 2 hard: runner_dispatch.go:323 validateTags → executor_protocol_violation. Gate 1 WARN-ONLY: template_validator.go:549-566 appends Warnings, does not reject — the subscription still registers | unguarded; Gate 2 guarded (TestDriveTerminal_RejectsUndeclaredTag); Gate 1 has zero coverage (no test sets RegistryHooks.ExecutorDeclaredTags) and only warns anyway | **EXECUTE** — ratified intent: declared_tags enforced at both gates — registration REJECTS undeclared subscription tags (restore reject from warn); terminal-handler gate stays. Guard both gates. |
| 2524 | `parked-state` | RETIRED 2026-07-14 but still fully in code: await_callback ParkReason (enum, proto zero value, parked_reason columns + CHECK, reason_note/label, conformance probe, executor emitte… | present; spec/parked_reason.go::ParkReasonAwaitCallback; executor.proto PARK_REASON_AWAIT_CALLBACK=0 + reason_note/reason_label; migrations parked_reason/_label/_note + CHECK; runner_terminal_park.go default; park_reaso… | unguarded; User ruling retires await_callback entirely (Park collapses to resume_at + scratch + tags); no guard is appropriate — the shape must be erased per erase-completely | **EXECUTE** — full park-reason collapse per the 07-14 rulings: ParkReason enum, Park.reason_note/reason_label wire fields, parked_reason/parked_reason_label columns + CHECK all erased. Park = resume_at (required) + scratch + tags; external waits are async-callback (running), never park. Signal taxonomy collapses transient/park/{snooze,await_callback} → transient/park (audit-only, feeds ShouldCascade). (Closes 2597 + 2551 reason leg too.) |
| 2525 | `parked-state` | RETIRED 2026-07-14 but still in code: max_park_duration (per-row seconds + per-reason deployment caps) | present; node.TemplateNodeDef.MaxParkDuration in runner_terminal_park.go::applyTerminalPark; max_park_duration_seconds column; config/supervisor.go MaxParkDuration; sweep_parked.go PerReasonMaxPark | unguarded; Watchdog retired by user ruling; config and sweep code still enforce caps | **EXECUTE** — duplicate of 2385 (max_park_duration leg); one erasure sweep closes all three. |
| 2526 | `parked-state` | RETIRED 2026-07-14 but still in code: park-timeout watchdog (parked→failed sweep, error_class park_timeout, ListParkedOverdue) | present; sweep_parked.go::failOverdueParkedRow (NodeStateFailed + ReasonParkTimeout + terminal/error/park_timeout + KindParkTimeout, abandons held claims) driven by Queue.ListParkedOverdue | unguarded; Retired: a park's sole exit is the resume_at time-wake; the watchdog is anti-guarded by TestParkedLifecycleMaxParkDurationOverrun and TestParkedLifecycleParkTimeoutAbandonsHeldClaim which lock in the retired … | **EXECUTE** — duplicate of 2385 (watchdog-sweep leg); one erasure sweep closes all three. Per the wave-4 audit note: the unguarded per-reason cap gets DELETED, not guarded. |
| 2584 | `run-scope` | USER RULING 2026-07-17: administrative instance termination closes EVERY remaining open run-scope in the instance's frames -- child scopes (sub-graph, fan-out partition) included,… | partial; lib/control/controlapi/lifecycle.go::CloseAndFanOutFrameRootRunScopesForInstance#319-343 iterates frames and closes ONLY f.RootRunScopeID (the per-frame root), firing FanOutRunScopeEvent per root. It never enum… | unguarded; lib/control/controlapi/lifecycle_test.go#404 pins only the root-scope dedupe (3 frames sharing one root -> fan out once). No test asserts child scopes are closed / fanned-out on termination. Peers tracking pe… | **EXECUTE** — duplicate of 2617 (control-api view of the same defect); one implementation closes both. |
| 2585 | `run-scope` | RESOLVED 2026-07-14: a frame's settlement closes its root run scope AT SETTLEMENT (no runs left stale/running/parked), firing exactly one OnRunScopeTerminal for the root; instance… | missing; lib/graph/frame/engine.go::transitionFrameEnd#71-112 stamps the frame ended (EndFrameIfSettled) but does NOT close the root run scope or fire OnRunScopeTerminal. The only RunScopes().Close call sites are child_… | unguarded; grep of all *_test.go finds no test asserting 'one frame settlement -> exactly one OnRunScopeTerminal for its root scope' (the ruling's named Phase 2 guard test). lifecycle_test.go#404 covers only the termina… | **EXECUTE** — graceful frame settlement closes the root run-scope at settlement and fires exactly one OnRunScopeTerminal; children asserted-already-closed (not assumed). Guard test drives a frame to natural completion and asserts the callback fired with NO terminate involved. |
| 2398 | `signal` | 2026-07-14 subscribability narrowing: ALL transient/* subscriptions rejected; transient/retry + release_and_requeue emission audit-only; cascade-fire set = settling signals only | partial; taxonomy.go canonicalSubscriptionPatterns STILL lists transient/retry/*, transient/infra/*, transient/release_and_requeue/*, transient/await_async as subscribable; error-policy path still cascade-fires retry | unguarded; TestValidateSubscriptionType_AcceptsTrailingWildcard actively pins transient/retry/* as ACCEPTED — a green test guarding the pre-ruling behavior | **EXECUTE** — duplicate of 2568 (subscribability narrowing; ShouldCascade mechanism ratified there). |
| 2491 | `supervisor` | Parked-node handling: never claimed directly, wake only via wakeParkedNode, resume parked→stale, all invalidate sources through single UnifiedInvalidate waking parked targets | partial; queue.go SelectCandidates/ClaimDispatchRow state='stale' + parked NOT-EXISTS; wake_parked.go::WakeParkedNode sole ResumeParkedInTx caller — but NO UnifiedInvalidate symbol exists; cascade/operator invalidation … | guarded; TestParkedLifecycleResumeOnDeadline + conformance park_resume guard the present halves | **EXECUTE** — June 19-22 rulings: parked rows never claimed directly; wake only via wakeParkedNode; resume parked→stale; all invalidation paths sealed. |
| 2492 | `supervisor` | Recovery-aware dispatch fields: prior_dispatch_id/disposition populated on every superseding dispatch, persisted, re-emitted after restart | partial; columns exist; read-side threading complete (runner_dispatch.go:826-835 into ExecuteRequest); but the ONLY production stamping site is cascade_recalculate.go:158 ('recalculate') — nothing ever writes stale_reco… | unguarded; conformance testRecoveryAwareDispatch hand-writes the disposition and guards persistence round-trip only; removing supervisor-side population turns nothing red | **EXECUTE** — RESOLVED 07-14 disposition vocabulary {none, stale_recovery, retry_after_error, recalculate}: prior_dispatch_id/disposition populated on every superseding dispatch. Guard per disposition class. |
| 2495 | `supervisor` | Callback advertise fail-fast: startup error when advertise empty + wildcard bind (never stamp 0.0.0.0); single advertise base-URL | missing; launch/supervisor.go only log.Warn's; runtime supervisor.go::effectiveCallbackHostPort falls back to bind host, still stamping http://0.0.0.0:9100; base-URL not path-prefix capable | unguarded; callback_advertise_test.go::TestEffectiveCallbackHostPort pins the OPPOSITE behavior (empty advertise → (0.0.0.0,9100), no error) — the ratified 2026-06-21 direction never landed | **EXECUTE** — duplicate of 2596 (callback-advertise fail-fast on wildcard bind + empty advertise). |
| 2402 | `validation` | lifecycle_subscriber as fourth validation role: Validate invoked template-wide on advertising lifecycle subscribers | missing; validation_pipeline.go::RunValidationPipeline iterates only executors/claim_producers/publishers; lifecycle-subscriber loop absent though proto/client/conformance plumbing exists | unguarded; no test; role never invoked | **EXECUTE** — user ruling 07-17: lifecycle_subscriber is a real fourth validation role; registration invokes Validate template-wide on every advertising subscriber. Guard: registering with a rejecting lifecycle-subscriber validator fails registration. |
| 2407 | `validation` | Pure erasure absolute (2026-07-14 ruling): no migration-redirect/directing errors anywhere; retired shapes fail via generic paths | partial; nodes.<X>.event message and stores: template rejection verified gone, BUT config/claim_producers.go:257,260 still emits directing errors for retired `write_semantics:` and `write_semantics_envelope` keys ('rena… | unguarded; retired_aliases_test.go pins the OLD directing behavior the ruling forbids | **EXECUTE** — 07-14 pure-erasure-absolute: retired config/template shapes fail as UNKNOWN keys everywhere; no migration-redirect or directing-error paths. Guard: a retired key in config/template yields the generic unknown-key rejection, not a bespoke redirect message. |

## B. Genuine gaps / regressions (24)

A promised behavior that is missing, partial, or contradicted with no ruling saying it should be gone — looks like real loss or an unfinished build. Default disposition: **restore** (Phase-3 code row citing the dossier as spec). Confirm per item.

| ID | Dossier | Behavior | Current code state | Pinning test | Disposition |
| --- | --- | --- | --- | --- | --- |
| 2370 | `blob-backend` | Typed Topology parameter replacing stringly RIMSKY_PROCESS_ROLE seam | missing; blob_config.go still gates on os.Getenv(ProcessRoleEnv); no Topology type | unguarded; n/a — not delivered | **EXECUTE** — ratified intent, no doubt flag: typed Topology parameter replaces the stringly RIMSKY_PROCESS_ROLE seam in blob-backend gating. (With 2431.) |
| 2454 | `cancel-siblings` | 'first' policy: first commit wins, remaining in-flight children cancelled | partial; aggregateFirst returns AggregateActionCancelNonWinners but ALL PropagateIfChildAfterTerminal callers discard the []CancelAction (runner_terminal.go:346/420, runner_error_policy.go:166, runner_terminal_park.go:1… | unguarded; only the pure Aggregate decision is tested; no test asserts losers get force-abandoned when first wins | **EXECUTE** — 07-14 RESOLVED: configurable fan-out aggregation is LIVE intent, and 'first' means first commit WINS: cancel actions for remaining in-flight children must be executed, not computed-and-discarded by every caller. Guard: first-policy fan-out asserts losers actually cancelled. |
| 2477 | `claim-producer` | Filesystem pick-policy validation: required on_commit/on_give_up, pop+on_open rejected, recycle+on_drain warns, pop_and_move same-fs/pre-existing/sibling, YAML bare-string xor one… | partial; filesystem/store/store.go::validatePickPolicy + validateMoveTargetSameFS — MISSING the sibling-of-policy-root check (only containment + not-equal + same-Dev; a nested target passes) | unguarded; most rules guarded by validator_test.go; the same-fs Dev-ID check is guarded only by TestValidator_RejectsCrossFilesystemTarget which is GOOS!=linux-skipped (never runs on darwin); sibling rule has no test | **EXECUTE** — ratified intent: filesystem pick-policy validation — on_commit/on_give_up required, pop+on_open rejected, recycle+on_drain warns. Fail-closed at boot. |
| 2417 | `delegation` | Fan-out composes with delegation: partition's cloned node can run a delegated sub-graph | partial; Substrate exists (shared DispatchChildren, nestable RunScope tree) but validateFanOut (template_validator_holds.go:68-72) forbids delegate+fan_out on one node and applyTerminalComplete short-circuits delegate c… | unguarded; No test combines fan_out and delegate in one template/run; nested_subgraph_test covers delegation-within-delegation only | **EXECUTE** — ratified intent: fan-out composes with delegation (partition's cloned node can run a delegated sub-graph); remove the validator prohibition. (Duplicate pair with 2437.) |
| 2380 | `frame` | Kill force-fails every in-flight run (all five states) and closes the run-scope tree | partial; controlapi/instances.go killStates={running,parked,held} — pending and stale excluded | unguarded; no test asserts pending/stale runs are force-failed on kill | **EXECUTE** — kill path force-fails in-flight runs in ALL FIVE states and closes the run-scope tree; implementation shared with 2379 (terminated frame state) + 2617 (scope-tree teardown). Guard matrix covers each state. |
| 2466 | `host-agent-proxy` | Proxy passes executor + claim-producer conformance suites with stub child behind in-process agent fake | partial; handlers implement the full surface but no conformance runner is wired against the proxy | unguarded; no conformance test targets the proxy; the claim is unverified | **EXECUTE** — ratified intent: proxy passes executor + claim-producer conformance suites with stub child behind an in-process agent fake. |
| 2543 | `inertness` | Named-event payloads: append-only ledger, blob-spillable, read via walkPath + persistence fetch, most-recent (emitter, event_name) wins; emit_named_event tool opaque | missing; Grep for emit_named/named_event/NamedEvent across lib/ and cmd/ found no non-test, non-generated hits; the closed substitution grammar (lib/graph/attribute/substitution.go, six source kinds: claim/params/nodes/… | unguarded; Nothing guards a feature that is not present; the generic event ledger (persistence.EventTable.Append, append-only) survives but carries rimsky audit payloads, not user named-events. | **EXECUTE** — ratified intent: named-event payloads — append-only ledger, blob-spillable, walkPath read + persistence fetch, most-recent (emitter, event_name) wins. |
| 2546 | `inertness` | Attribute override/default values static: no substitution applied, {{X}} literal; validation inspects only routing keys, never fragment values | partial; lib/runtime/attribute_overrides.go::applyAttributeOverrides deep-merges by_executor/by_node/by_match overlays with no Substitute call — a '{{X}}' value stays a literal; lib/graph/node/template_validator.go::val… | unguarded; No test asserts a '{{X}}' override value passes through as a literal or that substitution is never invoked on override fragments; adding a Substitute call to applyAttributeOverrides would turn no named test r… | **EXECUTE** — ratified intent: attribute override/default values are static — no substitution applied, {{X}} stays literal; validation inspects routing keys only. |
| 2548 | `inertness` | Subscription topic filters operate only on metadata (state, attribute key, event name, error class), never payload bytes | partial; lib/foundation/signal/taxonomy.go routes on canonical type-paths (terminal/success, terminal/error/*, attribute/*/changed) — pure metadata; wait-set rows carry topic_kind + topic_filter (lib/foundation/persiste… | unguarded; No test or lint asserts filters stay metadata-only; the CEL surface is the sanctioned exception per the inertness concept doc's structural-inertness taxonomy, so the dossier's absolute 'never payload bytes' h… | **EXECUTE** — ratified intent: subscription topic filters operate on metadata only (state, attribute key, event name, error class), never payload bytes. |
| 2514 | `message` | Per-instance message_queue_mode backlog\|coalesce, copied from template at creation, operator-overridable per instance; validator warns on coalesce with ≥2 types | partial; template.go:29 MessageQueueMode; instances.go:887,900 copied at creation; message_delivery.go:50-54 coalesce → CancelPendingForInstance; template_validator.go:250-284 | guarded; TestMessageQueueCoalesce_DropsPriorPendingOnReceipt + _BacklogModePreservesEveryMessage; TestValidateMessageQueueMode_Warning_CoalesceWithMultipleTypes | **EXECUTE** — ratified intent: per-instance message_queue_mode (backlog|coalesce) copied from template at creation, operator-overridable; 07-14 pause-drain ruling presupposes this surface. |
| 2562 | `module-layout` | INTENTIONAL ABSENCE: internal/ops must not exist | present; REGRESSION FLAG: lib/services/internal/ops/ops.go EXISTS — a 15-line package exposing `func Setup(level slog.Level) *slog.Logger` that installs a JSON slog handler. It is the only internal/ops in the tree (no r… | unguarded; Nothing prevents an internal/ops package; it compiles and lints cleanly. If the intent was to ban an 'ops' abstraction, this one slipped in as a logging helper. | **EXECUTE** — recorded intentional-absence regression: internal/ops must not exist; lib/services/internal/ops/ops.go removed/relocated per module-layout intent; lint/guard keeps it out. |
| 2428 | `observability` | Publisher observability: GET /instances/{id}/messages?sender=<publisher_name> with received_at | partial; Response carries Sender + ReceivedAt (messages.go:73,191) but MessageListFilter has no by-name Sender field — only ?sender_kind categorical filtering; ?sender= does not exist | unguarded; messages_test covers ?sender_kind only; no test can exercise a filter that does not exist | **EXECUTE** — ratified intent: GET /instances/{id}/messages?sender=<publisher_name> filter with received_at. (Duplicate pair with 2622.) |
| 2622 | `publisher-subscription` | Observability of publisher activity is a message-table query GET /instances/{id}/messages?sender=<publisher_name> with received_at as timestamp — not per-subscription state | partial; lib/control/controlapi/messages.go::handleListInstanceMessages#282 wires type / sender_kind / frame_id / delivered_after\|before / pending into MessageListFilter but does NOT wire a `sender` (publisher_name) qu… | unguarded; The list endpoint + received_at are guarded by messages_test.go list tests, but there is no `?sender=<publisher_name>` filter to test — the API offers only ?sender_kind=publisher (a coarser filter). Filtering… | **EXECUTE** — duplicate of 2428 (publisher-subscription view); one filter implementation closes both. |
| 2431 | `replica` | Launch-topology hardening: typed Topology param, typed migrate-ownership field, warn-only sqlite+replicas gate | missing; No type Topology in launch/entrypoint; migrate ownership still shouldMigrate() string-switch; no sqlite+replicas warn gate | unguarded; No code exists to guard | **EXECUTE** — ratified intent: launch-topology hardening (typed Topology param, typed migrate-ownership field, sqlite+replicas warn gate). (With 2370.) |
| 2453 | `rimsky-yml` | Net position: validators:/data_processors: as top-level role blocks | partial; wrapper parses only claim_producers/executors/publishers; validation/data_processing attach via per-entry protocols: list | unguarded; no top-level blocks exist to test; protocols-list attachment round-trip unasserted | **EXECUTE** — ratified net position: validators:/data_processors: as top-level role blocks in rimsky.yml; wrapper parses all roles, not just claim_producers. |
| 2623 | `service` | Spawned local service binaries must honor the RIMSKY_AGENT_PORT contract: every bundled production binary checks RIMSKY_AGENT_PORT first, falling back to its own port var, so `rim… | missing; lib/runtime/hostagent/spawn.go#36 SETS RIMSKY_AGENT_PORT on the spawned child, but NO bundled production binary READS it: claude-agent opts.go#78 reads RIMSKY_EXECUTOR_PORT_GRPC (default 9090); sensor-cron/main… | unguarded; test/scenarios/compose_run_spawned_services_test.go::TestComposeRunSpawnedServices_NoLeakAfterExit spawns the TEST STUB binary (which honors RIMSKY_AGENT_PORT), not any bundled production binary — so it prove… | **EXECUTE** — duplicate of 2509 (service-dossier view); one helper closes both. |
| 2433 | `sub-graph` | Runtime depth gating: RunScope already in parent chain rejected via parent-chain walk (defense-in-depth) | missing; No dispatch-time ancestor-graph check exists — DispatchChildren/subgraph_dispatch create child scopes unchecked; only static detectDelegateCycles at registration | unguarded; No test drives runtime recursion to rejection because the path does not exist | **EXECUTE** — ratified intent: runtime depth gating — RunScope already in parent chain rejected via parent-chain walk (defense-in-depth under the absorption model). |
| 2434 | `sub-graph` | Entry-alias invariant 5: marker plumbed AND runtime consumer built (finding 134 fix-code) | partial; ResolvesViaCallingNode set in template_validator_graphs.go::flatten#176 but has zero runtime readers — cascade walker/BuildSubscriptionEdges never consult it; internal cascade dispatches all non-entry nodes dir… | unguarded; Tests assert the marker is SET, none assert runtime resolution-to-calling-node — there is no consumer to guard | **EXECUTE** — finding 134 adjudicated fix-code: entry-alias invariant marker plumbed AND its runtime consumer built (ResolvesViaCallingNode currently a dead flag — wire it or the invariant is vacuous). Guard proves the runtime consumer changes behavior. |
| 2437 | `sub-graph` | Fan-out composes with delegation (nested sub-graph RunScope per partition); fan-out doesn't require a sub-graph | partial; Standalone fan-out works (fanout_dispatch.go); but a delegate node short-circuits at applyTerminalComplete#176 before any fanout logic and delegate+fan_out on one node is validator-forbidden — no per-partition … | unguarded; No test drives a fanout-parent-that-delegates; standalone fanout guarded elsewhere | **EXECUTE** — duplicate of 2417 (sub-graph view): nested sub-graph RunScope per partition; fan-out doesn't require a sub-graph. |
| 2486 | `supervisor` | Attribute writebacks and scratch writes bump last_progress_at in same tx | partial; scratch half present (callback.go::scratchStoreAdapter.Write:805-812); NO in-flight attribute-writeback route exists at all (callback server registers only /v1/callback, /scratch, /keepalive, /health) | unguarded; scratch tests use a fake writer — removing the bump stays green; attribute half has no code | **EXECUTE** — ratified intent: attribute writebacks bump last_progress_at in the same tx (scratch half already present; attribute half completed to match). |
| 2493 | `supervisor` | RESOLVED-conflict phase-2: quiet-period reaping path stamps stale_recovery on the re-dispatched row | missing; SweepExecutorDeadlines→ReleaseClaim only resets the SAME row (claimed_by NULL, state stale); no superseding dispatch created, no disposition stamped; zero production writes of 'stale_recovery' | unguarded; TestScheduler_OrphanedClaim_Released never asserts prior_dispatch_disposition | **EXECUTE** — same 07-14 ruling: quiet-period reaping path stamps stale_recovery on the re-dispatched row. Guard: reap-then-redispatch asserts the stamp. |
| 2496 | `supervisor` | Instance lifecycle: soft pause stops claiming; non-idempotent 409s; create-is-idle; force-terminate via real lifecycle path; delete refused until terminal | partial; pause/create-idle/delete-409 present; DIVERGENCE: handleTerminateInstance drives runs terminal via direct Nodes().UpdateState(NodeStateFailed) row writes, not the real terminal-resolution path | guarded; conformance testSelectCandidatesSkipsPausedInstances + instance_lifecycle_fullstack_test + instance_terminate_claims_test guard the present halves | **EXECUTE** — ratified intent: supervisor instance lifecycle — soft pause stops claiming, non-idempotent transitions 409, create-is-idle, force-terminate via real lifecycle path. |
| 2497 | `supervisor` | Asset/backfill dispatch honesty: re-materialization causes real producing-node dispatch; backfill honors partition-selector override; cancel via real cancel path | missing; controlapi/assets.go exposes only list/get/versions/history/delete; no backfill/materialize/cancel commands anywhere; repo-wide grep surfaces nothing | unguarded; no code to guard | **RECONCILE DOSSIER** — strike supervisor.md#53 + data-processing.md#26 backfill legs citing the 06-14 collapse. The dispatch-honesty principle survives inherently via message-refire (a re-fire IS a real producing dispatch). NOTE: ledger 2366 (fan-out sub-claim acquire failure) is now the load-bearing fix for the whole backfill-via-messages story — raise its Phase-3 priority. |
| 2565 | `template` | both subscription flags required, no defaults | partial; lib/foundation/spec/subscription.go::SubscriptionEntry has ONLY ForceUpstreamRefresh *bool (no wake_on_change field anywhere in repo); validateSubscribes (template_validator.go:451-457) + edgeFromSubscription (… | guarded; TestValidateSubscribes_RejectsMissingForceUpstreamRefresh (template_validator_test.go:744) asserts res.Ok()==false and an error path ending .force_upstream_refresh; deleting the nil-check in validateSubscribes … | **EXECUTE** — ratified intent: both subscription flags required in template spec, no defaults; validator rejects omission naming the flag. |

## C. Likely-superseded (20)

Flagged likely-superseded / artifact-only — the code's current shape looks intentional and the dossier probably describes a retired design. Default disposition: **reconcile the dossier** (no code change). Confirm per item.

| ID | Dossier | Behavior | Current code state | Pinning test | Disposition |
| --- | --- | --- | --- | --- | --- |
| 2532 | `asset` | Backfill on an asset honors a partition-selector override (supervisor materializes runs against the override, not the template default), reports truthful per-partition progress, a… | missing; No backfill runtime code exists — grep for 'backfill' across lib/cmd/test hits only 001-initial migration column comments, a conformance attribute-override helper, and cli/roles/operator.json; no partition-sele… | missing; No code and no test. The dossier itself marks this 2026-06-08 promise 'artifact-only' — it was specified but never implemented. | **RECONCILE DOSSIER** — backfill retired 2026-06-14 (collapsed to a message convention; see _retired/backfill.md). Strike asset.md#24 citing the collapse. No code build. Corpus-sweep rule: a concept retirement greps the corpus and updates EVERY mention in the same change. |
| 2534 | `asset` | Dashboard is asset-primary: Assets top-nav, cross-instance asset list, per-asset detail with versions/materialization-history/lineage | missing; This repo carries no frontend/dashboard (CLAUDE.md: 'Public docs — not part of this repo'; no web UI sources). The asset surface exists only as control-api + CLI | missing; No dashboard code and no test in this repo. The dossier marks it 'artifact-only'. | **RECONCILE DOSSIER** — asset-primary dashboard is outside rimsky-core (no UI ships from this repo); strike from asset.md. No code work. |
| 2567 | `atomic-staging` | Copyable example at examples/atomic-staging-fs-producer/ (Apache module) ships its FOUR scenario tests (abandon-on-any-failure, commit-on-all-success, concurrent-staging, sub-stag… | partial; examples/atomic-staging-fs-producer/ module exists (Apache header). Only examples/atomic-staging-fs-producer/atomic_staging_test.go::TestAtomicStaging_StageThenSwap (one test covering commit-on-all-success AND … | guarded; TestAtomicStaging_StageThenSwap guards the commit-on-all-success and abandon-on-failure semantic paths (rename-not-copy, canonical-untouched-on-abandon). | **EXECUTE** — story promise: ship the three missing scenario tests in examples/atomic-staging-fs-producer (concurrent-staging, sub-stage-verifier-failure, + remaining named case) alongside the existing StageThenSwap. |
| 2455 | `cancel-siblings` | Cancelled siblings transition to failed with error_class sibling_failed (artifact-only) | partial; siblings recorded as LineageOutcomeForceCancelled with cause 'sibling_cancel'; no 'sibling_failed' error_class exists anywhere | unguarded; cause='sibling_cancel' guarded; 'sibling_failed' string absent so nothing guards it | **FIX CODE (user ruling, reversed from audit's drift verdict)** — cause strings name REASONS, not mechanisms; sweep the whole CauseString family to reason-style in one change: sibling_cancel → sibling_failed, descendant_cancel → parent_resolved, natural stays. Update guards + wire consumers; dossier was right; lineage.md's family list updated to the new vocabulary. |
| 2456 | `cancel-siblings` | cancel_siblings: true is the DEFAULT for strict policy (artifact-only, transcript-unconfirmed) | missing; AggregationPolicy.CancelSiblings plain bool defaulting false; aggregateStrict cancels only when explicitly true | unguarded; no test asserts a bare strict policy defaults to cancelling siblings | **FIX CODE (new user ruling 2026-07-18): DELETE the cancel_siblings knob.** strict ALWAYS cancels siblings (fail-fast is its meaning); the keep-running case is expressed as threshold at full count; cancel_siblings leaves the template vocabulary (unknown-key rejected). Validator carve-out (strict-only check) deleted with it; cancel-siblings dossier + fan-out/claim-tree corpus swept in the same change. Guard: bare strict fan-out asserts in-flight siblings cancelled (cause per 2455 family: sibling_failed); threshold-full-count asserts children complete + commits land + verdict failed. |
| 2483 | `claim-producer` | RESOLVED (2026-07-14, restore/build): terminal verbs become a durable ordered per-producer outbox — disposition recorded before dialing, persistent undelivered rows with backgroun… | missing; terminal_decision.go::ResolveClaimHandleTerminal dials the producer SYNCHRONOUSLY (fireProducerVerb) inside the settlement tx BEFORE promoteHandleState — opposite of the ruling; no outbox/undelivered-verb table… | unguarded; feature absent; current tests assert the superseded synchronous behavior | **EXECUTE (build)** — 07-14 restore/build: producer terminal verbs (Commit/Abandon/Release) become a durable ordered per-producer OUTBOX — dispositions survive crash between terminal decision and verb delivery. Largest single build item in the set; Phase-3 plan should scope it as its own pass. |
| 2594 | `conformance` | No numbered-invariant references anywhere in source code, error strings, tests, or repo-root docs; invariants live in concept docs under descriptive names and diagnostics describe… | contradicted; lib/runtime/auto_terminal_test.go#1847 and #1854 carry test-assertion messages 'claim-tree.md invariant 1' and 'claim-tree.md invariant 5' — numbered-invariant references in a test, exactly the shape the i… | unguarded; There is no lint/test that greps for numbered-invariant references (if there were, these two lines would fail it). The cleanup is incomplete and unenforced — a fresh numbered reference could be added freely. … | **EXECUTE** — remove the two numbered-invariant references in auto_terminal_test.go (name the behavior instead); add the lint/check that keeps numbered-invariant refs out of source/tests/docs permanently. |
| 2507 | `dry-run` | backfill:create rejects (400) non-fan-out target_node; dry-run branch same validation | missing; No backfill:create action, route, or handler exists in lib/control/controlapi or cmd; only runtime-internal fan-out dispatch exists | unguarded; No code, no guard; unregistered action is invisible to the registry-enumerating conformance test | **RECONCILE DOSSIER** — same 06-14 retirement: strike dry-run.md#23 + validation.md#50,#105 (backfill:create never exists; nothing to validate). No code build. |
| 2572 | `event-log` | USER RULING 2026-07-17: expired is NOT a key_revoked reason; the reason enum is manual \| rotation_grace only; no expiry sweep, expiry stays a passive per-request check | partial; lib/foundation/auth/audit.go#80-82 still DEFINES RevokeReasonExpired='expired' alongside Manual and RotationGrace. No emit site uses it: auth_sweep.go::SweepRotationGrace emits only RevokeReasonRotationGrace; a… | unguarded; No test asserts the reason enum excludes 'expired'. The constant is dead (defined, never emitted), so nothing catches its lingering presence. | **EXECUTE** — 07-17: key_revoked reason enum is exactly {manual, rotation_grace}; RevokeReasonExpired constant deleted as dead residue. Guard pins the closed enum. |
| 2597 | `executor` | 2026-07-14 RULING (erase-completely): retire Park.reason machinery entirely — ParkReason enum, Park.reason_note/reason_label, rimsky_node_runs.parked_reason/parked_reason_label co… | contradicted; The reason machinery is fully LIVE: lib/runtime/runner_terminal_park.go#21-32 parkReasonStorageForm/parkReasonFromStorageForm, #61-63 Reason/ReasonNote/ReasonLabel, #137-138 parked_reason_label/parked_reas… | unguarded; The newest transcript-tier ruling (2026-07-14) mandates erase-completely but the code still carries the entire reason surface. No mechanical check drives the retirement; this is queued fix-code that has not l… | **EXECUTE** — duplicate of 2524 (executor-dossier view). NOT superseded — unexecuted ruling; regrouped from C to A by user ruling. Conformance park_reason_emission retires/reshapes; both bundled executors' park emitters updated; make proto-gen. |
| 2598 | `executor` | 2026-07-14 RULING (erase-completely): remove the mid-dispatch scratch route POST /v1/runs/{run_id}/scratch along with its handler, adapter (ScratchWriter), and proto/comment menti… | contradicted; The mid-dispatch route is STILL MOUNTED: lib/runtime/callback.go#172 r.Method(POST, /v1/runs/{run_id}/scratch, rimskyscratch.Handler(...)); the ScratchWriter adapter lives at lib/runtime/executor/scratch_w… | unguarded; The 2026-07-14 ruling directs erase-completely of this route, but it remains mounted and the adapter remains wired into the handler context. No mechanical check enforces the removal; this is queued fix-code n… | **EXECUTE** — 07-14 erase: POST /v1/runs/{run_id}/scratch route, handler, ScratchWriter adapter all deleted; scratch rides Park outcomes only. Registry test asserts the route is gone. |
| 2510 | `host-agent` | Late-bind surface narrowed (2026-07-14) to executor + claim-producer; other kinds rejected loudly at registration/config | missing; Verified: cmd/rimsky-host-agent-proxy/main.go:55-57 still RegisterPublisherServer/RegisterValidationServer/RegisterDataProcessingServer with fully-forwarding handlers; no kind rejection anywhere | unguarded; TestHostAgentLateBindAllProtocols actively guards the OPPOSITE (superseded) behavior — asserts all five protocols forward; the narrowing was never carried out | **EXECUTE** — 07-14 trim: late-bind surface is executor + claim-producer ONLY; other kinds rejected loudly at registration naming the kind. (Same ruling family as 2463.) |
| 2463 | `host-agent-proxy` | RESOLVED-trim (2026-07-14): late-bind publisher/validation/data-processing REJECTED loudly; proxy handlers for those three trimmed | missing; cmd/rimsky-host-agent-proxy/main.go:55-57 still registers Publisher/Validation/DataProcessing servers; handlers fully forward; no rejection anywhere | unguarded; no rejection code exists; test/scenarios/host_agent_latebind_all_protocols_test.go::TestHostAgentLateBindAllProtocols actively guards the SUPERSEDED all-five behavior | **EXECUTE** — duplicate of 2510 (proxy view): proxy handlers for publisher/validation/data-processing late-bind deleted; loud rejection replaces them. Anti-guard test (pinning the trim's absence) retires. |
| 2464 | `host-agent-proxy` | Deferred validation: spawned binary's Capabilities schema validated against resolved attributes; mismatch → contract_mismatch | missing; no contract_mismatch anywhere; resolveAndSpawn discards SpawnAck.Capabilities (state.recordSpawn(...,nil,...)); no dispatch-time attribute validation | unguarded; feature absent, nothing to guard | **EXECUTE** — ratified intent: proxy deferred validation — spawned binary's Capabilities schema validated against resolved attributes; mismatch is a config error naming the field. |
| 2465 | `host-agent-proxy` | DispatchFrame multiplexing across stream-ids in one spawn-id; Kind data/half-close/cancel; no head-of-line blocking | partial; per-streamID channels in state.go; Kind enum exists but HALF_CLOSE never sent/handled — only unary DATA/CANCEL used | unguarded; no test exercises concurrent multiplexed streams or HOL-freedom | **EXECUTE** — ratified intent: DispatchFrame multiplexing across stream-ids in one spawn-id (Kind data/half-close/cancel; no head-of-line blocking). |
| 2605 | `node-run` | No empty-claimant guard bypass (2026-07-17 user ruling): the queue-level claimed_by guard on release/remove verbs must be unconditional; the ""-sentinel carve-out is rejected; adm… | contradicted; The ""-sentinel carve-out is STILL LIVE: lib/foundation/persistence/postgres/queue.go#245 / sqlite#313 guard `(claimed_by IS NULL OR claimed_by = $1)` where passing "" bypasses the claimant check; Queue.Co… | unguarded; The current conformance carve-out (claimant_guard.go + frame_settlement.go passing "") ENSHRINES the sentinel rather than proving strictness — exactly the state the ruling calls out. Nothing proves the guard … | **EXECUTE** — 07-17: empty-claimant ''-sentinel carve-out removed from both queue drivers' claimed_by guards; explicit force variant added for the legitimate force paths; conformance suite updated to enshrine the guard, not the bypass. |
| 2429 | `observability` | Park audit entries record payload size and spill flag, never payload bytes | missing; No park event/lineage carries payload_size or spill flag: parkTerminalSignal payload has reason/tags/resume_at only (runner_terminal_park.go:135-155); grep for payload_size/spilled in events finds nothing park-… | unguarded; Nothing to guard; no test references park payload-size auditing | **EXECUTE** — ratified intent: park audit entries record payload size + spill flag, never payload bytes. Guard asserts absence of bytes. |
| 2436 | `sub-graph` | Parent-run state machine admits child_transitioned aggregation with audited reasons (dossier names terminal→stale/terminal→running + ReasonSubGraphInternalCascadeFired) | partial; cascade/state.go NextStateParent admits ReasonChildTransitioned for in-flight parents only; terminal parents REJECT it; ReasonSubGraphInternalCascadeFired exists only as event kind KindSubgraphInternalCascadeFi… | guarded; cascade/state_test TestNextStateParent asserts in-flight aggregate-OK and terminal → ErrIllegalTransition; subgraph_caller_lineage_test guards running-stay | **EXECUTE** — ratified intent: parent-run state machine admits child_transitioned aggregation with audited reasons per the dossier's named transitions. |
| 2485 | `supervisor` | Keepalive endpoint POST /v1/runs/{run_id}/keepalive with cancel_token auth, 204/404/401/400, bumps last_progress_at | partial; callback.go:185 route + keepalive.go::handleKeepalive (204/404/400, bump in tx) — auth is peer-mTLS authorizePeer, NOT the promised cancel_token | guarded; keepalive_test.go::TestKeepalive_Success/UnknownRun/InvalidRunID/MTLSRejects* | **EXECUTE** — decision:keepalive-endpoint: POST /v1/runs/{run_id}/keepalive full contract (cancel_token auth; 204/404/401/400; bumps last_progress_at). |
| 2441 | `wait-set` | Parked receiver woken by upstream cascade: parked→stale in-tx with stale-mark and wait-set insert | missing; Cascade walk never mutates a parked row — ensureCascadePending creates a NEW pending run (FindLatestCascadePending filters state='pending'); no parked→stale-on-cascade path exists | unguarded; No test asserts parked→stale-with-insert; current concept catalog (parked-state/wait-set) affirms the opposite in-flight-sealed invariant | **EXECUTE** — June-19 ruling: parked receiver woken by upstream cascade — parked→stale in-tx with stale-mark + wait-set insert; cascade info arriving during a park survives and produces a later dispatch. |
