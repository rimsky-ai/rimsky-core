{"id": "2275", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "dossier=node-run.md,error-policy.md,parked-state.md,claim.md; doc=node-reset-as-pure-retry-budget-clear.md:10; code=lib/control/controlapi/nodes.go::handleResetNode#127-177, lib/foundation/persistence/postgres/migrations/018-frame-isolation-restoration.sql", "note": "Retry-budget clearing on reset is real, corroborated transcript-tier intent ('signed off') that the handler still doesn't implement -- queued fix-code. Separately, the doc's frame-pointer-nulling clause is now structurally obsolete: migration 018 (frame-isolation-restoration) dropped rimsky_nodes.frame_id entirely, so there is no node-identity-row frame pointer left to null; edited the doc now to drop that clause.", "applied": true}
{"id": "2279", "classification": "conforms", "direction": "refute", "verdict": "REFUTED", "intent_provenance": "corroborated", "sources": "dossier=orphan-reaper.md (Net position: 'dispatched_at checked against per-row deadlines'); doc=orphan-reaper-connection-state.md:11; code=lib/runtime/conductor.go::decideExecutorDeadlineRelease#84-107 (uses ClaimedAt), lib/foundation/persistence/dispatch_row.go (no dispatched_at column, only ClaimedAt)", "note": "The dossier itself uses 'dispatched_at' as prose for the same timestamp the code calls ClaimedAt -- there was never a literal dispatched_at column (checked migrations), and self-contained decision-doc prose is not required to track a literal Go/DB field name. The behavior (max_runtime measured from the claim/dispatch timestamp) matches intent; not a defect.", "applied": false}
{"id": "2278", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "doc=parallel-cap-removal.md:10,14; code=Makefile:81-93 (test-services/test-examples carry `-p 2 -parallel 4` with a docker-saturation comment)", "note": "The Choice's blanket 'runs each module's tests without a test-parallelism cap' contradicts both the doc's own Rationale (a cap is admitted with a naming comment) and the Makefile, which still caps the services/examples docker-stack suites for a documented reason. Edited the Choice to state the removed cap was specifically the retired synchronous-Subscribe budget, and that the docker-saturation cap remains.", "applied": true}
{"id": "2280", "classification": "defect", "direction": "fix-code", "verdict": "CONFIRMED", "intent_provenance": "transcript", "sources": "dossier=validation.md (Conflicts: 'RESOLVED 2026-07-14 ... pure erasure is absolute -- no sanctioned exceptions ... present migration error -> defect'); doc=pre-v1-pure-removal-for-retired-surfaces.md:10; code=lib/control/config/claim_producers.go:223-246 (RetiredStoresKey + stores/write_semantics/write_semantics_envelope migration errors)", "note": "A 2026-07-14 transcript-tier ruling explicitly settles this exact ambiguity: a present migration error for a retired shape is a defect, not documentation drift. The decision doc's 'no detection rule, no migration error string' claim is the correct current-state target; the config parser's retained RetiredStoresKey/migration-error scaffolding is stale residue to remove. Queued fix-code; no doc edit needed.", "applied": false}
{"id": "2281", "classification": "defect", "direction": "fix-code", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "doc=progress-flags.md:10, progress-default.md:14 (sibling decision, 'deeper-frequency events (frame ticks, claim openings) become noise in the common case'); code=cmd/rimsky/cli/compose/progress.go::ProgressPrinter#18-24 (no scheduler-decision or claim-handle method at all)", "note": "Two sibling, both-adopted decision docs (progress-flags + progress-default) corroborate that verbose is meant to surface the deeper-frequency categories (frame ticks, claim/claim-handle events); no source corroborates 'scheduler decisions' specifically, but nothing contradicts it either. The ProgressPrinter interface has no hook at all for claim-handle or scheduler-decision events (only FrameTick exists, and only verbose overrides it) -- a real feature-completeness gap. Queued fix-code.", "applied": false}
{"id": "2282", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "transcript", "sources": "dossier=persistence-database.md ('persistence is excluded from the -race -count=3 slice (keeps -race -count=1)', 2026-06-21 transcript); doc=race-gate-split.md:10; code=Makefile:139-146 (test-race scopes only ./lib/runtime/... ./lib/graph/scheduler/..., with a comment naming the deliberate persistence exclusion)", "note": "Transcript-tier intent explicitly confirms persistence's exclusion from the repeated-race gate is deliberate (driver contention, not Go data races), directly contradicting the doc's 'runs the same set repeatedly' claim. Edited the Choice to state the dedicated gate covers only the load-bearing runtime+scheduler subset, with persistence covered by the everyday gate's single-iteration slice.", "applied": true}
{"id": "2283", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "doc=release-chain.md:10; code=Makefile:389 (`release: lint license-lint core-images service-images test-all test-race scan push-images`)", "note": "The Makefile release target (fair evidence for this decision per task scope) runs test-race between test-all and scan; the decision doc's seven-step enumeration omits it. Edited the Choice to add the repeated race-detection gate as a step, citing decision:race-gate-split, and noted the reservation of its repetition budget to release time in the Rationale.", "applied": true}
{"id": "2287", "classification": "defect", "direction": "fix-code", "verdict": "CONFIRMED", "intent_provenance": "artifact-only", "sources": "dossier=area--misc.md (confirms --name override exists, silent on validation); doc=run-name.md:10 (status: adopted); code=cmd/rimsky/cli/compose/run.go:96-100, artifact.go::EnsureRunDir#54-59 (no regex check on name), manifest.go:76 (projectRe applied only to m.Project)", "note": "No dossier or sibling artifact corroborates the specific validation claim, but the single adopted decision doc is coherent (reuses the project field's own filesystem-safety rationale) and uncontradicted; the code gap is concrete and security-relevant (unvalidated operator input flows into a run-directory path). Treated as known-enough intent given coherence + adopted status; queued fix-code to validate --name against the same character class as project.", "applied": false}
{"id": "2286", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "transcript", "sources": "dossier=executor.md (Conflicts: 'RESOLVED 2026-07-14 ... remove the mid-dispatch scratch route ... POST /v1/runs/{run_id}/scratch (zero in-tree callers) is retired ... erase-completely'); doc=scratch-protocol.md:11; code=lib/runtime/callback.go (route still present, not yet retired)", "note": "A 2026-07-14 transcript-tier ruling retires the entire mid-dispatch scratch channel the decision doc describes (HTTP callback plus the claude-agent MCP-wrapper and in-process direct-call variants it never actually shipped); scratch now rides settling terminal outcomes only. Rewrote the doc to the post-retirement design. Code still carries the not-yet-removed callback route/handler -- queued fix-code separately (out of scope here).", "applied": true}
{"id": "2284", "classification": "defect", "direction": "fix-code", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "dossier=node-run.md ('retry-after-error re-dispatch receives the prior attempt's scratch verbatim (terminal event's scratch copied into the acquisition)'), executor.md ('copy the terminal event's scratch into the acquisition'); doc=scratch-recovery.md:11; code=lib/runtime/runner.go:220 (acq.Scratch = terminal.Scratch unconditionally), lib/runtime/runner_terminal_scratch.go:20 (empty terminal = persist no-op)", "note": "Intent (two independent transcript sources) is that in-place retry copies the terminal event's scratch into the next acquisition in-memory -- the doc's 'without any copy' framing understated this and was corrected in the same edit as 2285. But the copy is unconditional even when terminal.Scratch is empty, silently discarding whatever the row already carried, which violates the well-established empty-terminal-is-a-no-op invariant (scratch-protocol.md) on the one path that never re-reads the row. Queued fix-code: the retry-copy must skip the assignment when terminal.Scratch is empty.", "applied": true}
{"id": "2285", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "dossier=orphan-reaper.md ('re-assignment is the normal pending-queue claim path'), node-run.md (Intentional absences: fresh-row-retry machinery incl. prior-dispatch dispositions simplified away in commit 339809cc); doc=scratch-recovery.md:11; code=lib/runtime/conductor.go::SweepExecutorDeadlines#27-47 (Queue.ReleaseClaim, same row), lib/foundation/persistence/sqlite/queue.go:439-447 (same-row state='stale'), lib/runtime/cascade_recalculate.go:145-161 (sole PriorDispatchDisposition='recalculate' writer)", "note": "The dossier confirms dead-supervisor recovery reuses the same row via release-and-reclaim rather than creating a new one, and grepping every writer of PriorDispatchDisposition in production code finds only 'recalculate' -- 'stale_recovery' is unused residue (schema CHECK value + a dead mapping case), never written. Rewrote the doc: the sole new-row-plus-copy site is the recalculate enqueue; dead-supervisor recovery needs no copy because it never creates a new row.", "applied": true}
