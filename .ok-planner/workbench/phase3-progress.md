# Phase 3 — fix everything (drift-remediation, final phase)

Started 2026-07-19 from commit 182b314d. Sequencing approved by user:

1. Erasure sweeps (ratified deletions first — they moot defects in doomed code)
2. Canonical executes by subsystem cluster (fold ruling into design corpus in the same change)
3. ~239 distinct open defects in area batches (per-row current-code evidence in ledger phase3_note)
4. Dossier reconciliations + guard-with-change tests, riding their owning clusters
5. Minor/nit tail by-kind sweeps (~1,424 rows incl. dup riders)
6. Close-out: recorded follow-ups, final all-rows-closed re-audit, corpus true-up, workbench teardown

Ledger: `review-findings-2026-07-06.csv` (phase3_status/phase3_note columns; scripts only, never hand-edit).
Exit gate per phase-2 standard: make lint green, all five modules green, stack suites green on
freshly built images, every ledger row closed or refuted, corpus is source of truth, workbench removed.

## Step 1 — erasure packets

| Packet | Ledger rows | Scope | Status |
|---|---|---|---|
| E-proxy-trim | 2463 | delete proxy late-bind publisher/validation/data-processing handlers; loud rejection; anti-guard | DONE: three handlers + dead forwarding deleted (proxy AND hostagent side); Unimplemented fail-loud proven by new scenario + unit guards; stubchild trimmed; corpus already post-erasure (b7e34819). Closes riders 83/84/323. Verified, staged. |
| E-empty-claimant | 2605 | remove ''-sentinel carve-out from both queue drivers' claimed_by guards; explicit force variant | DONE: bypass branch deleted in both drivers; four explicit Force* verbs added; two admin callers switched; conformance ClaimantGuard/RunForceOverride proves empty claimant = wrong claimant on both backends; node-run.md invariant tightened. Closes rider 1030. Verified, staged. |
| E-controlapi-auth-routes | 2618, 2619 | remove nil-AuthState branches (fail loudly at construction); delete /diagnostics/parked alias, one-route-per-action pin | DONE: NewApp panics on nil AuthState (5 permissive branches swept incl. mcp_route/enroll); alias deleted; 44-action route-count pin test; control-api.md invariants added. Integration fixups by lead: 11 residual sites (RefValidationMode wiring x3, InsertRunningFrame arity x11 calls, frame_timeout_ms raw-SQL fixtures x6 files) fixed forward; full controlapi suite green; repo build green. Staged. |
| E-auto-subscribe | 2517 | remove messages.* auto-subscribe edge injection; explicit subscriptions only; inverse guard | DONE: injection loop + edgeFromMessageRef deleted; anti-guard retired, inverse guard added + red-checked; all fixtures already explicit (ref-coverage validator made injection redundant); corpus already correct, no doc edits needed. Verified, staged. |
| E-frame-timeout | 2384 | drop frame_timeout_ms + CHECK (migration), delete validateFrameTimeout + runtime path | DONE: migration 024 both backends (sqlite table-rebuild); validateFrameTimeout + stuck-frame sweep + persistence surface deleted; ~60-file fallout cleaned; 3 pinning scenario files retired; unknown-key 400 guard added; 2 decision docs corrected. RESIDUAL in locked controlapi: frames.go FrameTimeoutMs + messages_test 5-arg InsertRunningFrame. Staged. |
| E-cancel-siblings | 2456 | delete cancel_siblings knob; strict always cancels; threshold-at-full-count is keep-running form | DONE: field erased from spec/persistence/runtime/validator; strict cancels unconditionally; unknown-key 400 guard; threshold-at-full-count keep-running guards added both domains; corpus (cancel-siblings/claim-tree/claim-co-holdership) rewritten. Runtime suite re-verify deferred to post-park integration gate (proto regen in flight). Staged. |
| E-ref-validation-mode | 2470 | erase ref_validation_mode (all modes) + execSchemaVisible + deps wiring; strict per-name validation unconditional | DONE (one residual): knob + env + templates: YAML block erased (unknown-key rejection, no redirect hint per 2407); strict path inverse-guarded; scenario/e2e fallout fixed on merits; template.md invariant rewritten. RESIDUAL: 3 RefValidationMode wiring sites in lib/control/controlapi (app.go/templates.go/templates_test.go) await the in-flight controlapi agent — integration fix on its landing. Staged except controlapi. |
| E-transient-subscribe | 2568 | narrow subscribable set to settling types (ShouldCascade); inverse guard: subscribing to transient = validation error | DONE: ShouldCascade(TypePath) in signal taxonomy is sole authority; subscribable set DERIVED from it (twin list dead); runtime cascade walk gates on same predicate; inverse guards at taxonomy + validator; cascade/node-subscription/signal corpus updated. Signal pkg staged; runner_terminal + validator test staged after frame-timeout lands. |
| E-park-cluster | 2385, 2526, 2524, 2597, 2551(reason leg), 2598 | park watchdog erasure; ParkReason erased (Park = resume_at required + scratch + tags); scratch route deleted | DONE: all 3 legs; proto fields reserved + regen; migration 025 both backends (incl. dead parked_resume_at); park_timeout transition illegal-guarded; conformance park_emission replaces reason scenarios; http-node 429 tags rate_limited; claude-agent report_park reshaped; scratchStoreAdapter was still live (phase-2 note wrong) — now truly erased; corpus swept (8 docs). Closes riders 1478/1656. Staged. |
| E-numbered-invariants | 2594 | remove numbered-invariant refs in auto_terminal_test.go; add lint keeping them out | DONE: 4 offenders fixed (3 in auto_terminal_test.go + 1 found by sweep in claim_content_inertness_test.go); TestNoNumberedInvariantReferences fitness test added + red-checked. Verified, staged. |
| E-delegation-fanout | 2417 | remove validator prohibition: fan-out partition node can run a delegated sub-graph | DONE: prohibition removed AND real runtime bug fixed (SettleFromDelegate never released the calling node's own claims — partition sub-claims stuck active forever); red-checked e2e proof (3 partitions, per-partition sub-graph scopes, all claims commit); delegation.md + fan-out.md updated. Closes rider 59. Staged. |
| E-pure-erasure-guard | 2407 | LAST: guard that retired keys fail as UNKNOWN everywhere (no redirect/directing-error paths) | DONE: 3 surviving redirect hints from EARLIER retirements (stores, write_semantics, write_semantics_envelope) found + genericized; table-driven guards (config: 6 keys, registration: 4 keys) with forbidden-word assertions, red-checked; Park reserved-fields descriptor test. Closes rider 2280. Staged. |

Writer discipline (carried from phase 2): shared working tree — waves of ≤4 on disjoint areas;
ABSOLUTE BAN on git checkout/restore/reset/stash/clean; fix forward only; byte-copy to /tmp before
any temporary mutation and restore by copy-back; agents do NOT stage, commit, or touch the ledger;
I verify each packet (build + affected tests + red-check where applicable), stage, then refill.
Pre-v1: migrations drop/recreate freely; dev DBs may be nuked (say so in the report).

**Step 1 gate MET (2026-07-19).** make lint green; root module full suite green (incl. complete
test/scenarios); foundation, protocols, examples, services modules green — the final examples +
services docker-stack legs ran against images rebuilt from the exact committed source. Three gate
regressions found and fixed forward during verification (strict-aggregate signal overwrite,
lifecycle-subscriber wiring lie, dead parked-alias callers incl. the CLI verb).

## Divergence / discovery log

(items surfaced during execution land here)

- Aggregation-threshold off-by-one between run_tree.go (failures >= max) and child_execution.go (abandoned > max) rediscovered during 2456; already tracked as ledger 1526 (CONFIRMED, OPEN) — cancel-siblings guards written to current semantics; 1526 fixes the divergence in its own batch.

- runtime_diagnostics_e2e_test.go transient leg: RESOLVED — retargeted to a settled-signaler terminal wait-set row + blocked-inheritor wedge; green in 3s. (Post-2568 undrained wait-set rows no longer occur: transients cannot cascade, and terminal rows drain in the settle transaction.)
- NEW (from 2417): delegate-with-claims FAILURE path never releases the caller's claims (success path fixed in-packet; failure/abandon path latent leak in runner_error_policy) — dispatch as its own fix packet in step 2.
- NEW (from 2417): plain fan-out double-acquires per partition (split sub-claim + direct re-resolve; harmless, wasteful) — queue with the minor sweeps.
- Erasure exposed a wiring lie in examples/lifecyclesubscriber: harness declared the subscriber as an EXECUTOR while templates referenced it as a STORE — only the erased ref-validation leniency let that register. Fixed to the blessed shape per concept:lifecycle-subscriber (claim-producer primary + lifecycle protocol alongside; fake + reference binary now serve ClaimProducer Capabilities); e2e green.
- GATE REGRESSION (fixed): unconditional sibling-cancel made the claim family resolve inside the first child's terminal tx, so the deferred-held poison write stamped terminal/error/abandoned on the fan-out parent run before the run-domain aggregate could project strict_failed (children still [running,stale,stale] at poison time — verified by trace). Fix: walkUpwards now corrects the settling signal on an already-settled parent when the aggregate verdict differs (row correction only, no re-cascade). Deterministic 3x green + blast-radius suites green. The deeper claim-domain vs run-domain verdict unification remains the kill-path cluster's job (2379/2617/2380).
- GATE HANG (fixed): TestClaudeAgentCrossStack's parked-wait helper polled the DELETED /v1/diagnostics/parked alias (404 forever = designed loud hang). Sweep found two more stragglers outside the controlapi agent's module scope: mcp_transport_parity_e2e and the rimsky parked CLI verb (a real production bug — the shipped CLI called the dead route). All three now use /v1/admin/diagnostics/parked-nodes; cross-stack test green in 10s, full CLI suite green.
- Re-run at integration gate: TestTemplateFanOut_AbandonPropagatesToParentError + TestFanOutChildren_ReuseParentNodeRow_UserdataVerbatim (failed transiently amid concurrent in-flight diffs, attributed not-caused; verify on quiet tree) and lib/runtime/hostagent full suite (601s hang seen once under load).

## Step 2 — canonical execute clusters

Ledger true-up (rides the first step-2 commit): the 18 erasure-wave canonical rows + 10 dup riders stamped CLOSED citing 4fd0fce7; OPEN 276→266.

13 packets built from the 56 remaining EXECUTE rows + ruled rows 1945/784/785 + row 2366 + the delegate failure-path claim-leak discovery (60 ledger rows total; 370 closes as dup of 2492). Packet briefs: scratchpad/phase3/step2/*.json.

| Packet | Rows | Model | Status |
| --- | --- | --- | --- |
| kill-teardown | 2379 2380 2617 2584 2585 2575 2496 2516 | session | wave 1 — running |
| supervisor-recovery | 2485 2486 2491 2492 2493 2495 2596 2441 | session | wave 1 — running |
| webhook-reconcile | 1945 | sonnet | wave 1 — running |
| proxy-hostagent | 2464 2465 2466 2509 2510 2623 | sonnet | wave 1 — running |
| subgraph-delegation (+delegate-failure claim leak) | 2433 2434 2436 2437 | session | queued |
| messages-publisher | 2513 2514 2422 2428 2622 2386 | sonnet | queued |
| producer-outbox | 2483 2477 2567 2366 | session | queued |
| debug-frame-scope | 784 785 | sonnet | queued |
| validation-config | 2402 2453 2546 2548 2565 2412 | sonnet | queued |
| eventlog-audit | 2572 2573 2427 2429 2543 | sonnet | queued |
| topology-launch | 2370 2431 | sonnet | queued |
| signal-residue | 2398 2525 2589 2562 2383 2620 | sonnet | queued |
| cancel-first | 2454 2455 | sonnet | queued |

Wave discipline unchanged: ≤4 concurrent writers on disjoint areas; lead verifies + stages each packet; writers never stage/commit or touch ledger/workbench; docker-stack suites run at lead level before commit.

### Step-2 wave log
- webhook-reconcile DONE (sensor side): AttachStateDB rehydrates a watermark cache via new ListWatermarks; Subscribe consults cache-first; 3 deterministic tests (restart, replay-dedup, idempotent reconcile ticks). Sensor cannot rehydrate route bindings locally by design (auth secret never persisted — guarded by TestStateDB_OnlyPersistsWatermarkNeverConfig). Row 1945 stays open pending the control-plane half: runtime publisher reconciler ticks only Mounting rows; Active-row diff resync runs only at control-API startup → folded into the signal-residue packet brief. Staged.
- kill-teardown DONE (8/8): terminated frame state derived from terminal/error/instance_killed signal (no migration; shared frameStateCaseSQL per driver); kill force-fails all five in-flight states; full scope-tree teardown via recursive-CTE ListTreeDeepestFirst, children-before-parents, exactly-once fan-out with retry re-offers; settlement closes the frame's scope tree in the EndFrameIfSettled tx with a frame_settled fan-out hook wired scheduler→launch; CancelPendingForInstance in the terminate tx; force-terminate now runs the real terminal-resolution path (producer-notified abandon, wait-set drain, sibling/parent aggregation, post-commit run-domain propagate) = the claim/run-domain verdict unification. Conformance + kill-matrix + settlement guards, both drivers. Staged. Residuals reported: split-role settlement fan-out only reaches scheduler-registry subscribers (in-proc supervisor-only subscribers hear termination-time re-offers, not settlement); kill fail-open on producer error falls back to record-only abandon.
- validation-config DONE (6/6): 2402 lifecycle_subscriber now actually invoked as a 4th validation role at registration (plumbing existed, never called); 2412 pause-mode breakpoints fail CLOSED on infra error (after_terminal stays fail-open); 2453 validators:/data_processors: top-level blocks parsed + dialed — LEAD FOLLOW-UP: launch/controlapi.go + launch/supervisor.go still need the one-line Validators/DataProcessors passthrough (deferred here only because the supervisor writer owns lib/control/launch right now; wire it when that packet lands); 2546 + 2548 proven already satisfied, guard tests added; 2565 closes as dossier drift — wake_on_change was superseded by the authoritative node-subscription concept (subscription IS the wake declaration), existing force_upstream_refresh guard suffices; note this in the ledger closure. Staged.
- supervisor-recovery DONE (8/8): keepalive route with cancel_token bearer auth (204/404/401/400); new POST /v1/runs/{id}/attributes writeback route bumping last_progress_at + claim expiry in-tx; scratch writes bump in-tx; single parked wake path (resumeParkedRunInTx), cascade now wakes subscribed parked receivers state-only (cascade_resume reason) while the round rides a new pending row's wait-set; retry_after_error + stale_recovery dispositions stamped by production paths with migration 026 (both dialects); callback advertise fail-fast on empty-advertise+wildcard-bind. Bugs fixed en route: sqlite SelectCandidates missing the undrained-wait-set NOT EXISTS gate (dialect divergence, conformance-guarded); stale proto comments from the scratch-route erasure scrubbed + regenerated. Tensions resolved in corpus: sealed-in-flight carve-out (state-only wake), keepalive bearer layered under peer-auth for incremental callbacks only. Staged.
- LEAD: 2453 launch passthrough wired (Validators/DataProcessors into both launch literals), build green, staged. validation-config follow-up cleared.
- proxy-hostagent DONE (6/6): deferred Capabilities-schema validation settles contract_mismatch before dispatch; per-stream multiplexing proven (+ real fix: singleflight around check-then-spawn kills a duplicate-spawn race); proxy now passes BOTH protocol conformance suites via in-process tunnel agent + real stubs — which surfaced two genuine proxy bugs, fixed (falsely advertised SupportsSplitScope; Release dropped the claim route breaking idempotent retry); RIMSKY_AGENT_PORT precedence via new shared lib/services/internal/agentport helper wired into all 9 in-scope bundled binaries with a compiled-binary e2e guard; 2510 late-bind narrowing proven already-satisfied. Staged.
- Re-run note: cmd/rimsky/cli/compose TestUpdateLatestSymlink_ConcurrentReadersNeverSeeBroken failed once under full-suite wave load, green in isolation — verify on quiet tree at gate; if it reproduces, fix the nondeterminism at root.
- debug-frame-scope DONE (784+785): debug verbs frame-scoped per ruling — latest run resolved within the active frame only; cross-frame pairing refused 409 naming node + both frame ids; terminal in-frame runs never mutated (set_attribute redirects onto a fresh invalidated run carrying the bag forward); held/parked runs remain legal targets. Bonus bug: set_attribute previously ALSO spuriously invalidated the node on every call (unconditional CreateNonCascadeStale) — gated. The two wrong-behavior pin tests replaced; 5 guards added. Staged.
- messages-publisher DONE (6/6): 2513 proven already-satisfied (one pipeline, no injection, guarded); message_queue_mode operator override at instance creation (400 on bad value, surfaced on GET); body schema validation moved to the single EnqueueMessage chokepoint so ALL producer paths validate at ledger insertion (operator/publisher POST 400 naming type+field; the inverted admits-bad-payload test flipped); ?sender= filter on the instance messages route with cross-backend conformance; lineage claim walks made truthful — /ancestors now walks parent_claim_handle_id, new /descendants walks sub_claim_handle_ids (old mislabeled route + test corrected; route count pin updated). No migrations needed. Staged.
- eventlog-audit DONE (4 implemented + 1 adjudicated): key_revoked enum closed to manual|rotation_grace (dead 'expired' constant deleted, closed-set pin); permission_denied audit rows now carry the requested mode (identity-stage denials stay null per event-log concept); declared_tags registration gate hardened warn→REJECT in a new subscription_tag_gate.go feature file (terminal gate was already hard); park audit entries record scratch_size + scratch_spilled, never bytes. Row 2543 (named-event payload ledger) NOT restored: design history shows the named-event ledger was retired outright by the ratified 2026-06-16 decision (decisions/terminal-tags.md; migration 013 squashed away) — the row's EXECUTE disposition was stale; close as RETIRED-BY-LATER-DECISION in the ledger, not as delivered work. Staged.
- topology-launch DONE (2/2): typed persistence.Topology (unified|split) minted at the single LoadRimskyConfigYAML edge from RIMSKY_PROCESS_ROLE; ValidateBlobConfig/OpenBlobBackend/metricsPortFor take the type (env read removed from internals — gating actually lives in lib/foundation/persistence/blob_config.go, packet pointer corrected); entrypoint gets Role.OwnsMigration + LaunchPlan with typed MigrateOwner; sqlite+replica downgraded to warn-only with the warning text corrected to the real caveat (cross-process flock already landed 2026-06-13 — the intent dossier's in-process-mutex premise is stale; route to step-4 replica dossier reconciliation). Staged.
- Re-run note (2nd): cmd/rimsky/cli/compose TestDrain_SIGTERMThenSIGKILLChildren_BoundedTime also flaked under wave load, green in isolation — verify both compose tests on quiet tree at gate; "BoundedTime" name suggests a wall-clock verdict, fix at root if it reproduces.
- subgraph-delegation DONE (4/4 + leak fix): runtime delegate-recursion gate via parent-chain walk (partition scopes exempt); entry-alias marker finally has runtime consumers — walker binds entry-alias edges to the CALLER's settling signal (wait-set sender = caller run) and substitution resolves entry deps to the caller's bag; parent state machine admits child_transitioned from every state incl. terminal re-projection + audited sub-graph-internal running-stay; 2437 fan-out+delegation composition proven already delivered by the erasure wave and preserved. Delegate FAILURE-path claim leak fixed: settled-to-failed delegate callers now abandon their claims in the settlement loop (idempotent with the success path), chaining into fan-out aggregation. Staged. Discoveries: run-tree CancelActions computed-and-discarded (no executor) — folded into cancel-first brief for adjudication; InTx+require harness footgun noted for the minor sweeps.
- signal-residue DONE (6/6 + 1945 control-plane half): 2398 + 2525 proven already satisfied (transient/* rejection + max_park retirement, guards exist); 2589 first-run attribute baseline from schema defaults — required a real GetPriorRunData fix in BOTH dialects (no-prior-row vs nil-data were indistinguishable; conformance-guarded) — diff-based emission now uniform from run 1; internal/ops deleted with a module-layout fitness test (13 call sites inlined); frame-trigger fields (frame_trigger_kind, trigger_message_id) now populated on leaf_run lineage records, apology-log residue deleted; publisher reconciler now runs the FULL Active+Mounting diff resync on every injected tick (Mounting-only pass + dead markSubscriptionFailed deleted) — 1945 now fully closable. Also fixed: conformance seed helper returning the same run twice made a prior test pass vacuously. Staged. Flagged for later wave: LeafRunRecord.ExecutorVersion declared but never populated.
- producer-outbox DONE (4/4): terminal verbs now a durable ordered per-producer outbox (migration 027 both dialects; disposition recorded before any dial; per-(producer,scope) head-of-line ordering; injected-Clock backoff; Open barrier drains a scope's undelivered terminals synchronously; Release + partial-open abandon routed through it; fireProducerVerb + verb-error fallback deleted — a producer error no longer flips a decided handle). Latent bug fixed: deferred SetVersionID claimant-guard no-oped post-promote (both dialects). 2477 pick-policy sibling-of-root check restored with all-platform device-ID test. 2366 root-caused in RUNTIME, not the store: fan-out children re-resolved the parent selector instead of attaching to their linked sub-claims — new reuseLinkedSubClaim fixes it, kills the known double-acquire discovery at the root, and TestSubClaimPayloadSubstitutionE2E is UN-skipped (needs fresh images — lead gate). 2567 atomic-staging four scenario tests shipped (-race green). Follow-up candidate logged: outbox diagnostics surface is store-method + logs only, no control-API endpoint (ruling's diagnostics clause satisfied minimally). Staged.
- Gate re-check list additions: TestOperatorInvalidateQueuesDuringFlight failed once amid in-flight edits (verify quiet); claude-agent TestSpawnWritesRealSystemPrompt... failed once under extreme parallel load, unreproducible ×2 (watch at gate); mass auth-middleware 500s at default parallelism are a load artifact, use sane parallelism at gate.
- cancel-first DONE (2/2 + adjudication): run-tree CancelActions now EXECUTED for the first policy (winner cancels in-flight losers recursively, sibling_cancelled reason, terminal/error/sibling_failed, claims abandoned; 60/60 -race); strict deliberately stays claim-domain-only (wiring both regressed the strict signal test — narrowed back). Cause vocabulary renamed per ruling: sibling_cancel→sibling_failed, descendant_cancel→parent_resolved. Real pre-existing bug found+fixed: ReleaseClaim{,WithDisposition} had no terminal-state guard — a delayed release could revert a fresh/failed run to stale, discarding its outcome (conformance-guarded, both dialects). Staged.
- OPEN BEFORE GATE: pre-existing intermittent race (~1/8–15) in TestRunTreePersistedShapeAndStrictAggregation_ChildFailureFailsParent — claim-domain auto-terminal (transitionHolderIfFullyResolved) and run-domain walkUpwards both write the fan-out parent's settling signal; reproduces with cancel-first inert. The erasure-wave signal-only correction narrowed but did not close it. Root-cause fix required before the step-2 gate (no-flakes rule).
- LEAD: strict-aggregate race FIXED by dedicated fixer (resumed across a 529 kill, work preserved): trace-proven interleaving — transitionHolderIfFullyResolved stamped the fan-out parent terminal/error/abandoned inside the first child's terminal tx, ~20ms before walkUpwards corrected it; the wrong verdict was published (and its cascade emitted) transiently. Fix: aggregating-parent gate (holder with run-tree children leaves the run verdict to the run-domain aggregate; claim handle still promotes + verb fires), settlement bridge now emits the terminal signal event (emitSignalInTxOnce), raced auto-terminal writes to settled runs are logged skips (instance-kill too), and the testcontainers pg image gets max_connections=1000 (16 parallel stacks deterministically exceeded the default 100). Proof: 30/30 + -race 10/10 on the racy test; full scenarios sweep 22/23. Staged.
- LEAD: the 23rd — TestOperatorInvalidateQueuesDuringFlight — was a real regression from the debug-frame-scope packet: set_attribute on an in-flight (parked) run stopped queuing the operator_invalidate stale row the story ratifies. Fixed: dispatched in-flight runs (running/held/parked) get live-bag merge + one queued stale row (snapshot carries the merged bag); queued runs (stale/pending) get merge only; terminal redirect unchanged. Story test 3x green, all Debug/Override/Breakpoint guards green; breakpoint concept updated. Staged.

## Step 2 — gate record
- make lint: exit 0, all five modules.
- Root module go test ./... -count=1 -p 4: exit 0, no failures (covers the two cli/compose load-flagged tests + hostagent on quiet tree).
- lib/foundation + lib/protocols: green. claude-agent spawn test -count=3: green.
- Images rebuilt from final source (core + service + test), exit 0.
- Services docker-stack: all 9 suite packages green vs fresh images.
- Examples docker-stack: green except TestSubClaimPayloadSubstitutionE2E, which hung at suite timeout — diagnosed LIVE against the stack: the RUNTIME WORKS (all three partitions settled terminal/success with processed_value 1/2/3 from sub-claim payloads; verified via events surface on the running container). The failure was a stale test assertion polling per-NODE latest_attributes, which can only ever show one value under the ratified reuse-parent-node-row fan-out design. Rewrote waitForProcessedValues to poll /v1/events attribute deltas; green in 4s, re-confirmed -count=2.
