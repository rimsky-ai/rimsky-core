# Phase 3 — fix everything (drift-remediation, final phase)

Started 2026-07-19 from commit 182b314d. Sequencing approved by user:

1. Erasure sweeps (ratified deletions first — they moot defects in doomed code)
2. Canonical executes by subsystem cluster (fold ruling into design corpus in the same change)
3. ~239 distinct open defects in area batches (per-row current-code evidence in ledger phase3_note)
4. Dossier reconciliations + guard-with-change tests, riding their owning clusters
5. Minor/nit tail by-kind sweeps (~1,424 rows incl. dup riders)
6. Close-out: recorded follow-ups, final all-rows-closed re-audit, corpus true-up, workbench teardown

Ledger: `review-findings-2026-07-06.csv` (phase3_status/phase3_note columns; scripts only, never hand-edit).
Exit gate per phase-2 standard: make lint green, all five modules green, stack suites green on
freshly built images, every ledger row closed or refuted, corpus is source of truth, workbench removed.

## Step 1 — erasure packets

| Packet | Ledger rows | Scope | Status |
|---|---|---|---|
| E-proxy-trim | 2463 | delete proxy late-bind publisher/validation/data-processing handlers; loud rejection; anti-guard | DONE: three handlers + dead forwarding deleted (proxy AND hostagent side); Unimplemented fail-loud proven by new scenario + unit guards; stubchild trimmed; corpus already post-erasure (b7e34819). Closes riders 83/84/323. Verified, staged. |
| E-empty-claimant | 2605 | remove ''-sentinel carve-out from both queue drivers' claimed_by guards; explicit force variant | DONE: bypass branch deleted in both drivers; four explicit Force* verbs added; two admin callers switched; conformance ClaimantGuard/RunForceOverride proves empty claimant = wrong claimant on both backends; node-run.md invariant tightened. Closes rider 1030. Verified, staged. |
| E-controlapi-auth-routes | 2618, 2619 | remove nil-AuthState branches (fail loudly at construction); delete /diagnostics/parked alias, one-route-per-action pin | DONE: NewApp panics on nil AuthState (5 permissive branches swept incl. mcp_route/enroll); alias deleted; 44-action route-count pin test; control-api.md invariants added. Integration fixups by lead: 11 residual sites (RefValidationMode wiring x3, InsertRunningFrame arity x11 calls, frame_timeout_ms raw-SQL fixtures x6 files) fixed forward; full controlapi suite green; repo build green. Staged. |
| E-auto-subscribe | 2517 | remove messages.* auto-subscribe edge injection; explicit subscriptions only; inverse guard | DONE: injection loop + edgeFromMessageRef deleted; anti-guard retired, inverse guard added + red-checked; all fixtures already explicit (ref-coverage validator made injection redundant); corpus already correct, no doc edits needed. Verified, staged. |
| E-frame-timeout | 2384 | drop frame_timeout_ms + CHECK (migration), delete validateFrameTimeout + runtime path | DONE: migration 024 both backends (sqlite table-rebuild); validateFrameTimeout + stuck-frame sweep + persistence surface deleted; ~60-file fallout cleaned; 3 pinning scenario files retired; unknown-key 400 guard added; 2 decision docs corrected. RESIDUAL in locked controlapi: frames.go FrameTimeoutMs + messages_test 5-arg InsertRunningFrame. Staged. |
| E-cancel-siblings | 2456 | delete cancel_siblings knob; strict always cancels; threshold-at-full-count is keep-running form | DONE: field erased from spec/persistence/runtime/validator; strict cancels unconditionally; unknown-key 400 guard; threshold-at-full-count keep-running guards added both domains; corpus (cancel-siblings/claim-tree/claim-co-holdership) rewritten. Runtime suite re-verify deferred to post-park integration gate (proto regen in flight). Staged. |
| E-ref-validation-mode | 2470 | erase ref_validation_mode (all modes) + execSchemaVisible + deps wiring; strict per-name validation unconditional | DONE (one residual): knob + env + templates: YAML block erased (unknown-key rejection, no redirect hint per 2407); strict path inverse-guarded; scenario/e2e fallout fixed on merits; template.md invariant rewritten. RESIDUAL: 3 RefValidationMode wiring sites in lib/control/controlapi (app.go/templates.go/templates_test.go) await the in-flight controlapi agent — integration fix on its landing. Staged except controlapi. |
| E-transient-subscribe | 2568 | narrow subscribable set to settling types (ShouldCascade); inverse guard: subscribing to transient = validation error | DONE: ShouldCascade(TypePath) in signal taxonomy is sole authority; subscribable set DERIVED from it (twin list dead); runtime cascade walk gates on same predicate; inverse guards at taxonomy + validator; cascade/node-subscription/signal corpus updated. Signal pkg staged; runner_terminal + validator test staged after frame-timeout lands. |
| E-park-cluster | 2385, 2526, 2524, 2597, 2551(reason leg), 2598 | park watchdog erasure; ParkReason erased (Park = resume_at required + scratch + tags); scratch route deleted | DONE: all 3 legs; proto fields reserved + regen; migration 025 both backends (incl. dead parked_resume_at); park_timeout transition illegal-guarded; conformance park_emission replaces reason scenarios; http-node 429 tags rate_limited; claude-agent report_park reshaped; scratchStoreAdapter was still live (phase-2 note wrong) — now truly erased; corpus swept (8 docs). Closes riders 1478/1656. Staged. |
| E-numbered-invariants | 2594 | remove numbered-invariant refs in auto_terminal_test.go; add lint keeping them out | DONE: 4 offenders fixed (3 in auto_terminal_test.go + 1 found by sweep in claim_content_inertness_test.go); TestNoNumberedInvariantReferences fitness test added + red-checked. Verified, staged. |
| E-delegation-fanout | 2417 | remove validator prohibition: fan-out partition node can run a delegated sub-graph | DONE: prohibition removed AND real runtime bug fixed (SettleFromDelegate never released the calling node's own claims — partition sub-claims stuck active forever); red-checked e2e proof (3 partitions, per-partition sub-graph scopes, all claims commit); delegation.md + fan-out.md updated. Closes rider 59. Staged. |
| E-pure-erasure-guard | 2407 | LAST: guard that retired keys fail as UNKNOWN everywhere (no redirect/directing-error paths) | DONE: 3 surviving redirect hints from EARLIER retirements (stores, write_semantics, write_semantics_envelope) found + genericized; table-driven guards (config: 6 keys, registration: 4 keys) with forbidden-word assertions, red-checked; Park reserved-fields descriptor test. Closes rider 2280. Staged. |

Writer discipline (carried from phase 2): shared working tree — waves of ≤4 on disjoint areas;
ABSOLUTE BAN on git checkout/restore/reset/stash/clean; fix forward only; byte-copy to /tmp before
any temporary mutation and restore by copy-back; agents do NOT stage, commit, or touch the ledger;
I verify each packet (build + affected tests + red-check where applicable), stage, then refill.
Pre-v1: migrations drop/recreate freely; dev DBs may be nuked (say so in the report).

**Step 1 gate MET (2026-07-19).** make lint green; root module full suite green (incl. complete
test/scenarios); foundation, protocols, examples, services modules green — the final examples +
services docker-stack legs ran against images rebuilt from the exact committed source. Three gate
regressions found and fixed forward during verification (strict-aggregate signal overwrite,
lifecycle-subscriber wiring lie, dead parked-alias callers incl. the CLI verb).

## Divergence / discovery log

(items surfaced during execution land here)

- Aggregation-threshold off-by-one between run_tree.go (failures >= max) and child_execution.go (abandoned > max) rediscovered during 2456; already tracked as ledger 1526 (CONFIRMED, OPEN) — cancel-siblings guards written to current semantics; 1526 fixes the divergence in its own batch.

- runtime_diagnostics_e2e_test.go transient leg: RESOLVED — retargeted to a settled-signaler terminal wait-set row + blocked-inheritor wedge; green in 3s. (Post-2568 undrained wait-set rows no longer occur: transients cannot cascade, and terminal rows drain in the settle transaction.)
- NEW (from 2417): delegate-with-claims FAILURE path never releases the caller's claims (success path fixed in-packet; failure/abandon path latent leak in runner_error_policy) — dispatch as its own fix packet in step 2.
- NEW (from 2417): plain fan-out double-acquires per partition (split sub-claim + direct re-resolve; harmless, wasteful) — queue with the minor sweeps.
- Erasure exposed a wiring lie in examples/lifecyclesubscriber: harness declared the subscriber as an EXECUTOR while templates referenced it as a STORE — only the erased ref-validation leniency let that register. Fixed to the blessed shape per concept:lifecycle-subscriber (claim-producer primary + lifecycle protocol alongside; fake + reference binary now serve ClaimProducer Capabilities); e2e green.
- GATE REGRESSION (fixed): unconditional sibling-cancel made the claim family resolve inside the first child's terminal tx, so the deferred-held poison write stamped terminal/error/abandoned on the fan-out parent run before the run-domain aggregate could project strict_failed (children still [running,stale,stale] at poison time — verified by trace). Fix: walkUpwards now corrects the settling signal on an already-settled parent when the aggregate verdict differs (row correction only, no re-cascade). Deterministic 3x green + blast-radius suites green. The deeper claim-domain vs run-domain verdict unification remains the kill-path cluster's job (2379/2617/2380).
- GATE HANG (fixed): TestClaudeAgentCrossStack's parked-wait helper polled the DELETED /v1/diagnostics/parked alias (404 forever = designed loud hang). Sweep found two more stragglers outside the controlapi agent's module scope: mcp_transport_parity_e2e and the rimsky parked CLI verb (a real production bug — the shipped CLI called the dead route). All three now use /v1/admin/diagnostics/parked-nodes; cross-stack test green in 10s, full CLI suite green.
- Re-run at integration gate: TestTemplateFanOut_AbandonPropagatesToParentError + TestFanOutChildren_ReuseParentNodeRow_UserdataVerbatim (failed transiently amid concurrent in-flight diffs, attributed not-caused; verify on quiet tree) and lib/runtime/hostagent full suite (601s hang seen once under load).

## Step 2 — canonical execute clusters

Ledger true-up (rides the first step-2 commit): the 18 erasure-wave canonical rows + 10 dup riders stamped CLOSED citing 4fd0fce7; OPEN 276→266.

13 packets built from the 56 remaining EXECUTE rows + ruled rows 1945/784/785 + row 2366 + the delegate failure-path claim-leak discovery (60 ledger rows total; 370 closes as dup of 2492). Packet briefs: scratchpad/phase3/step2/*.json.

| Packet | Rows | Model | Status |
| --- | --- | --- | --- |
| kill-teardown | 2379 2380 2617 2584 2585 2575 2496 2516 | session | wave 1 — running |
| supervisor-recovery | 2485 2486 2491 2492 2493 2495 2596 2441 | session | wave 1 — running |
| webhook-reconcile | 1945 | sonnet | wave 1 — running |
| proxy-hostagent | 2464 2465 2466 2509 2510 2623 | sonnet | wave 1 — running |
| subgraph-delegation (+delegate-failure claim leak) | 2433 2434 2436 2437 | session | queued |
| messages-publisher | 2513 2514 2422 2428 2622 2386 | sonnet | queued |
| producer-outbox | 2483 2477 2567 2366 | session | queued |
| debug-frame-scope | 784 785 | sonnet | queued |
| validation-config | 2402 2453 2546 2548 2565 2412 | sonnet | queued |
| eventlog-audit | 2572 2573 2427 2429 2543 | sonnet | queued |
| topology-launch | 2370 2431 | sonnet | queued |
| signal-residue | 2398 2525 2589 2562 2383 2620 | sonnet | queued |
| cancel-first | 2454 2455 | sonnet | queued |

Wave discipline unchanged: ≤4 concurrent writers on disjoint areas; lead verifies + stages each packet; writers never stage/commit or touch ledger/workbench; docker-stack suites run at lead level before commit.

### Step-2 wave log
- webhook-reconcile DONE (sensor side): AttachStateDB rehydrates a watermark cache via new ListWatermarks; Subscribe consults cache-first; 3 deterministic tests (restart, replay-dedup, idempotent reconcile ticks). Sensor cannot rehydrate route bindings locally by design (auth secret never persisted — guarded by TestStateDB_OnlyPersistsWatermarkNeverConfig). Row 1945 stays open pending the control-plane half: runtime publisher reconciler ticks only Mounting rows; Active-row diff resync runs only at control-API startup → folded into the signal-residue packet brief. Staged.
- kill-teardown DONE (8/8): terminated frame state derived from terminal/error/instance_killed signal (no migration; shared frameStateCaseSQL per driver); kill force-fails all five in-flight states; full scope-tree teardown via recursive-CTE ListTreeDeepestFirst, children-before-parents, exactly-once fan-out with retry re-offers; settlement closes the frame's scope tree in the EndFrameIfSettled tx with a frame_settled fan-out hook wired scheduler→launch; CancelPendingForInstance in the terminate tx; force-terminate now runs the real terminal-resolution path (producer-notified abandon, wait-set drain, sibling/parent aggregation, post-commit run-domain propagate) = the claim/run-domain verdict unification. Conformance + kill-matrix + settlement guards, both drivers. Staged. Residuals reported: split-role settlement fan-out only reaches scheduler-registry subscribers (in-proc supervisor-only subscribers hear termination-time re-offers, not settlement); kill fail-open on producer error falls back to record-only abandon.
- validation-config DONE (6/6): 2402 lifecycle_subscriber now actually invoked as a 4th validation role at registration (plumbing existed, never called); 2412 pause-mode breakpoints fail CLOSED on infra error (after_terminal stays fail-open); 2453 validators:/data_processors: top-level blocks parsed + dialed — LEAD FOLLOW-UP: launch/controlapi.go + launch/supervisor.go still need the one-line Validators/DataProcessors passthrough (deferred here only because the supervisor writer owns lib/control/launch right now; wire it when that packet lands); 2546 + 2548 proven already satisfied, guard tests added; 2565 closes as dossier drift — wake_on_change was superseded by the authoritative node-subscription concept (subscription IS the wake declaration), existing force_upstream_refresh guard suffices; note this in the ledger closure. Staged.
- supervisor-recovery DONE (8/8): keepalive route with cancel_token bearer auth (204/404/401/400); new POST /v1/runs/{id}/attributes writeback route bumping last_progress_at + claim expiry in-tx; scratch writes bump in-tx; single parked wake path (resumeParkedRunInTx), cascade now wakes subscribed parked receivers state-only (cascade_resume reason) while the round rides a new pending row's wait-set; retry_after_error + stale_recovery dispositions stamped by production paths with migration 026 (both dialects); callback advertise fail-fast on empty-advertise+wildcard-bind. Bugs fixed en route: sqlite SelectCandidates missing the undrained-wait-set NOT EXISTS gate (dialect divergence, conformance-guarded); stale proto comments from the scratch-route erasure scrubbed + regenerated. Tensions resolved in corpus: sealed-in-flight carve-out (state-only wake), keepalive bearer layered under peer-auth for incremental callbacks only. Staged.
- LEAD: 2453 launch passthrough wired (Validators/DataProcessors into both launch literals), build green, staged. validation-config follow-up cleared.
- proxy-hostagent DONE (6/6): deferred Capabilities-schema validation settles contract_mismatch before dispatch; per-stream multiplexing proven (+ real fix: singleflight around check-then-spawn kills a duplicate-spawn race); proxy now passes BOTH protocol conformance suites via in-process tunnel agent + real stubs — which surfaced two genuine proxy bugs, fixed (falsely advertised SupportsSplitScope; Release dropped the claim route breaking idempotent retry); RIMSKY_AGENT_PORT precedence via new shared lib/services/internal/agentport helper wired into all 9 in-scope bundled binaries with a compiled-binary e2e guard; 2510 late-bind narrowing proven already-satisfied. Staged.
- Re-run note: cmd/rimsky/cli/compose TestUpdateLatestSymlink_ConcurrentReadersNeverSeeBroken failed once under full-suite wave load, green in isolation — verify on quiet tree at gate; if it reproduces, fix the nondeterminism at root.
- debug-frame-scope DONE (784+785): debug verbs frame-scoped per ruling — latest run resolved within the active frame only; cross-frame pairing refused 409 naming node + both frame ids; terminal in-frame runs never mutated (set_attribute redirects onto a fresh invalidated run carrying the bag forward); held/parked runs remain legal targets. Bonus bug: set_attribute previously ALSO spuriously invalidated the node on every call (unconditional CreateNonCascadeStale) — gated. The two wrong-behavior pin tests replaced; 5 guards added. Staged.
- messages-publisher DONE (6/6): 2513 proven already-satisfied (one pipeline, no injection, guarded); message_queue_mode operator override at instance creation (400 on bad value, surfaced on GET); body schema validation moved to the single EnqueueMessage chokepoint so ALL producer paths validate at ledger insertion (operator/publisher POST 400 naming type+field; the inverted admits-bad-payload test flipped); ?sender= filter on the instance messages route with cross-backend conformance; lineage claim walks made truthful — /ancestors now walks parent_claim_handle_id, new /descendants walks sub_claim_handle_ids (old mislabeled route + test corrected; route count pin updated). No migrations needed. Staged.
- eventlog-audit DONE (4 implemented + 1 adjudicated): key_revoked enum closed to manual|rotation_grace (dead 'expired' constant deleted, closed-set pin); permission_denied audit rows now carry the requested mode (identity-stage denials stay null per event-log concept); declared_tags registration gate hardened warn→REJECT in a new subscription_tag_gate.go feature file (terminal gate was already hard); park audit entries record scratch_size + scratch_spilled, never bytes. Row 2543 (named-event payload ledger) NOT restored: design history shows the named-event ledger was retired outright by the ratified 2026-06-16 decision (decisions/terminal-tags.md; migration 013 squashed away) — the row's EXECUTE disposition was stale; close as RETIRED-BY-LATER-DECISION in the ledger, not as delivered work. Staged.
- topology-launch DONE (2/2): typed persistence.Topology (unified|split) minted at the single LoadRimskyConfigYAML edge from RIMSKY_PROCESS_ROLE; ValidateBlobConfig/OpenBlobBackend/metricsPortFor take the type (env read removed from internals — gating actually lives in lib/foundation/persistence/blob_config.go, packet pointer corrected); entrypoint gets Role.OwnsMigration + LaunchPlan with typed MigrateOwner; sqlite+replica downgraded to warn-only with the warning text corrected to the real caveat (cross-process flock already landed 2026-06-13 — the intent dossier's in-process-mutex premise is stale; route to step-4 replica dossier reconciliation). Staged.
- Re-run note (2nd): cmd/rimsky/cli/compose TestDrain_SIGTERMThenSIGKILLChildren_BoundedTime also flaked under wave load, green in isolation — verify both compose tests on quiet tree at gate; "BoundedTime" name suggests a wall-clock verdict, fix at root if it reproduces.
- subgraph-delegation DONE (4/4 + leak fix): runtime delegate-recursion gate via parent-chain walk (partition scopes exempt); entry-alias marker finally has runtime consumers — walker binds entry-alias edges to the CALLER's settling signal (wait-set sender = caller run) and substitution resolves entry deps to the caller's bag; parent state machine admits child_transitioned from every state incl. terminal re-projection + audited sub-graph-internal running-stay; 2437 fan-out+delegation composition proven already delivered by the erasure wave and preserved. Delegate FAILURE-path claim leak fixed: settled-to-failed delegate callers now abandon their claims in the settlement loop (idempotent with the success path), chaining into fan-out aggregation. Staged. Discoveries: run-tree CancelActions computed-and-discarded (no executor) — folded into cancel-first brief for adjudication; InTx+require harness footgun noted for the minor sweeps.
- signal-residue DONE (6/6 + 1945 control-plane half): 2398 + 2525 proven already satisfied (transient/* rejection + max_park retirement, guards exist); 2589 first-run attribute baseline from schema defaults — required a real GetPriorRunData fix in BOTH dialects (no-prior-row vs nil-data were indistinguishable; conformance-guarded) — diff-based emission now uniform from run 1; internal/ops deleted with a module-layout fitness test (13 call sites inlined); frame-trigger fields (frame_trigger_kind, trigger_message_id) now populated on leaf_run lineage records, apology-log residue deleted; publisher reconciler now runs the FULL Active+Mounting diff resync on every injected tick (Mounting-only pass + dead markSubscriptionFailed deleted) — 1945 now fully closable. Also fixed: conformance seed helper returning the same run twice made a prior test pass vacuously. Staged. Flagged for later wave: LeafRunRecord.ExecutorVersion declared but never populated.
- producer-outbox DONE (4/4): terminal verbs now a durable ordered per-producer outbox (migration 027 both dialects; disposition recorded before any dial; per-(producer,scope) head-of-line ordering; injected-Clock backoff; Open barrier drains a scope's undelivered terminals synchronously; Release + partial-open abandon routed through it; fireProducerVerb + verb-error fallback deleted — a producer error no longer flips a decided handle). Latent bug fixed: deferred SetVersionID claimant-guard no-oped post-promote (both dialects). 2477 pick-policy sibling-of-root check restored with all-platform device-ID test. 2366 root-caused in RUNTIME, not the store: fan-out children re-resolved the parent selector instead of attaching to their linked sub-claims — new reuseLinkedSubClaim fixes it, kills the known double-acquire discovery at the root, and TestSubClaimPayloadSubstitutionE2E is UN-skipped (needs fresh images — lead gate). 2567 atomic-staging four scenario tests shipped (-race green). Follow-up candidate logged: outbox diagnostics surface is store-method + logs only, no control-API endpoint (ruling's diagnostics clause satisfied minimally). Staged.
- Gate re-check list additions: TestOperatorInvalidateQueuesDuringFlight failed once amid in-flight edits (verify quiet); claude-agent TestSpawnWritesRealSystemPrompt... failed once under extreme parallel load, unreproducible ×2 (watch at gate); mass auth-middleware 500s at default parallelism are a load artifact, use sane parallelism at gate.
- cancel-first DONE (2/2 + adjudication): run-tree CancelActions now EXECUTED for the first policy (winner cancels in-flight losers recursively, sibling_cancelled reason, terminal/error/sibling_failed, claims abandoned; 60/60 -race); strict deliberately stays claim-domain-only (wiring both regressed the strict signal test — narrowed back). Cause vocabulary renamed per ruling: sibling_cancel→sibling_failed, descendant_cancel→parent_resolved. Real pre-existing bug found+fixed: ReleaseClaim{,WithDisposition} had no terminal-state guard — a delayed release could revert a fresh/failed run to stale, discarding its outcome (conformance-guarded, both dialects). Staged.
- OPEN BEFORE GATE: pre-existing intermittent race (~1/8–15) in TestRunTreePersistedShapeAndStrictAggregation_ChildFailureFailsParent — claim-domain auto-terminal (transitionHolderIfFullyResolved) and run-domain walkUpwards both write the fan-out parent's settling signal; reproduces with cancel-first inert. The erasure-wave signal-only correction narrowed but did not close it. Root-cause fix required before the step-2 gate (no-flakes rule).
- LEAD: strict-aggregate race FIXED by dedicated fixer (resumed across a 529 kill, work preserved): trace-proven interleaving — transitionHolderIfFullyResolved stamped the fan-out parent terminal/error/abandoned inside the first child's terminal tx, ~20ms before walkUpwards corrected it; the wrong verdict was published (and its cascade emitted) transiently. Fix: aggregating-parent gate (holder with run-tree children leaves the run verdict to the run-domain aggregate; claim handle still promotes + verb fires), settlement bridge now emits the terminal signal event (emitSignalInTxOnce), raced auto-terminal writes to settled runs are logged skips (instance-kill too), and the testcontainers pg image gets max_connections=1000 (16 parallel stacks deterministically exceeded the default 100). Proof: 30/30 + -race 10/10 on the racy test; full scenarios sweep 22/23. Staged.
- LEAD: the 23rd — TestOperatorInvalidateQueuesDuringFlight — was a real regression from the debug-frame-scope packet: set_attribute on an in-flight (parked) run stopped queuing the operator_invalidate stale row the story ratifies. Fixed: dispatched in-flight runs (running/held/parked) get live-bag merge + one queued stale row (snapshot carries the merged bag); queued runs (stale/pending) get merge only; terminal redirect unchanged. Story test 3x green, all Debug/Override/Breakpoint guards green; breakpoint concept updated. Staged.

## Step 2 — gate record
- make lint: exit 0, all five modules.
- Root module go test ./... -count=1 -p 4: exit 0, no failures (covers the two cli/compose load-flagged tests + hostagent on quiet tree).
- lib/foundation + lib/protocols: green. claude-agent spawn test -count=3: green.
- Images rebuilt from final source (core + service + test), exit 0.
- Services docker-stack: all 9 suite packages green vs fresh images.
- Examples docker-stack: green except TestSubClaimPayloadSubstitutionE2E, which hung at suite timeout — diagnosed LIVE against the stack: the RUNTIME WORKS (all three partitions settled terminal/success with processed_value 1/2/3 from sub-claim payloads; verified via events surface on the running container). The failure was a stale test assertion polling per-NODE latest_attributes, which can only ever show one value under the ratified reuse-parent-node-row fan-out design. Rewrote waitForProcessedValues to poll /v1/events attribute deltas; green in 4s, re-confirmed -count=2.

## Step 3 — open-defect batches
21 area packets built from the 249 OPEN rows (scratchpad/phase3/step3/*.json; shared writer brief in PROMPT.md). Per-row outcomes required: FIXED / ALREADY-FIXED (evidence) / STALE (evidence) — ledger stamped per outcome at close. Wave discipline unchanged (≤4 concurrent, disjoint areas, lead verifies+stages, Sonnet default).

| Packet | Rows | Status |
| --- | --- | --- |
| runtime-a/b/c/d | 13+13+13+11 | a: wave 1 — running; b/c/d queued |
| persistence-a/b/c | 10+10+9 | a: wave 1 — running; b/c queued |
| controlapi-a/b/c | 10+10+10 | a: wave 1 — running; b/c queued |
| graph | 17 | wave 1 — running |
| protocols | 16 | queued |
| svc-producers | 14 | queued |
| svc-executors | 11 | queued |
| svc-sensors-subs | 11 | queued |
| svc-other | 8 | queued |
| cli-cmd | 13 | queued |
| core-misc | 14 | queued |
| test-support | 13 | queued |
| design-docs-a/b | 12+11 | queued |

### Step-3 wave log
- runtime-a DONE: 12 FIXED / 1 STALE (1462 — path deleted by the step-2 outbox). Highlights: revocation audit-write atomic + surfaced; callback ack-outcome race closed by construction; notify-only breakpoints never block on overflow; aggregate threshold unified (>= at max) with run-tree side. Staged.
- persistence-a DONE: 10 FIXED. Highlights: RebindRunFrameInTx deleted (dead primitive violating run-scope invariant); 9 new cross-scope isolation conformance tests; LockReceiverCascade mutual-exclusion proof verified toothy against a broken lock; 2 extra bugs fixed — GetMostRecentRunForNodeInScope ordered by random UUID on enqueued_at ties (now sequence DESC, both dialects) and a misleading FK-cascade test state. Lead added the third blob-backend citation (orphan_blobs.go). Staged. Follow-up noted: GetInFlightRunForNode unordered LIMIT 1 ambiguity (no defect row; step-6 list).
- controlapi-a DONE: 9 FIXED / 1 ALREADY-FIXED (102 via runtime-a). Highlights: asset GET/history/delete now behind the data-processing predicate (fails closed); asset delete atomic with a race-proof DeleteResolvedIfNoActiveHolders (FK-implied row locking); expiring-sole-key guard on key create/rotate; frame ?state= validated with terminated/failed/completed distinct; observability handler had the same ?state= bug + a test pinning it — both fixed. Cross-area persistence adds were additive-only. Staged.
- graph DONE: 17 FIXED (incl. dup 1330). Highlights: substitution preserves numeric fidelity via json.Number; frame end-detection can no longer complete before its triggering message delivers; delegate targets validated in both template forms; holds get self-ref + cycle rejection (narrowed from the upstream-dependency reading — real templates legitimately hold non-upstream claims; concept updated to match enforcement); pure-cascade no longer settles claim-bearing nodes on def-lookup errors; breakpoint orphan sweep spares notify_only + live blocked runners. Cross-area edits additive (dispatch schema gate = the in-flight edit runtime-a noted; breakpoint_hits both dialects). Staged.
- GATE FIX LIST: TestDryRunCoverage_AllWriteActions failing every run (likely controlapi-a's new asset-delete semantics vs dry-run coverage) and TestFanoutPostMortem_MixedOutcomesEmitFullForensicsTrail failing every run (likely runtime-a 1526 threshold flip or cancel-first interplay) — both must be root-caused and fixed before the step-3 gate.
- persistence-b DONE: 10 FIXED, migrations 028 (messages frame_id index) + 029 (frames.root_run_scope_id FK; prune deletes root scope — also exposed and fixed a fixture violating the one-frame-per-scope invariant). Highlights: blob spill writes now ride the caller's tx (rollback-safe, no second-connection self-deadlock — MaxConns=1 proof); UpdateNodeRunID claimant-guarded (the concept had documented the hole as an intentional carve-out — corrected); MarkSourceNodeStale read the wrong JSON key so required_stores never populated. Lead added the 2 remaining advisory-lock citations (scheduler tick guard, runner_locks). Staged.
- controlapi-b DONE: 10 FIXED. Highlights: template-missing terminate/delete fallbacks unified and now close root scopes with ordered fan-out; lifecycle fan-out exactly-once under single-process races (striped mutex; multi-replica DB row lock → step-6 follow-ups); run-lineage descendants walk the real substitution_refs relation; messages cursor pagination implemented at the persistence root cause (both dialects); MCP gains the 4 lineage tools, spec-correct isError, and caller idempotency keys. Staged.
- runtime-b DONE: 12 FIXED / 1 REASSIGNED (1699 — needs a coordinated controlapi+builtin DeclaredErrorClassesFor mechanism; stays open for lead follow-up, not stamped closed). Highlights: run-scope lifecycle fan-out moved post-commit (rollback can no longer discard a delivered event); fan-out parallelism cap actually enforced (per supervisor process); resolver errors retryable instead of terminal; cancelled messages excluded from MarkDelivered at the SQL predicate; dead-lettered messages persisted as audit events; in-proc executor client cancellable. Step-6 follow-ups added: lineage.Query pagination unimplemented both dialects; cluster-wide fan-out cap needs queue-SQL predicate; FanOutRunScopeEvent runtime/controlapi DRY collapse. Staged.
- persistence-c DONE: 7 FIXED / 1 ALREADY-FIXED / 1 STALE. Highlights: SelectCandidates late-bind predicate fixed in BOTH dialects (mixed static+late-bound store sets were undispatatchable in postgres; sqlite ignored late-binding entirely); scratch-handle blob orphans enrolled on overwrite AND the two delete paths; template re-registration idempotent at the persistence layer; GetInFlightRunForNode sequence-ordered (the ambiguity persistence-a flagged — reproduced live by a new test's flake, fixed at root). Persistence family complete (29 rows). Staged.
- controlapi-c DONE: 10 FIXED + known-RED TestDryRunCoverage_AllWriteActions root-caused (the sibling fail-closed predicate was right; the auth fixture never wired a producer registry — fixture fixed, test green). Highlights: params redaction handles dotted paths and fails closed; register-with-tag can no longer silently move a tag without tag:set (permission-gated, not blanket-blocked — a blanket 409 would have broken the legitimate admin re-point workflow); template register/deregister fan-out ordered inside the tx; observability node/instance reads torn-read-free with errors propagated. Controlapi family complete (30 rows). Staged.
- GATE FIX LIST addition: lib/control/config TestStartSupervisor_MTLSWiresIdentityBeforeOutboundDial flakes ~1/3 — Handle.Shutdown (lib/runtime/supervisor.go) not fully synchronous before t.Cleanup tempdir removal; fix at root once runtime-c frees the area.
- protocols DONE: 14 FIXED (incl. 2 dup pairs) / 1 STALE (1418 — disproven live, guard added). Highlights: conformance kit no longer trusts vacuous probes — restart scenario forces a real interruption (exposing that claude-agent NEVER retried a failed callback POST — fixed with bounded retry), tags/scratch/abandon paths driven end-to-end, unsupported-capability wire behavior negatively probed; serverkit gains the run-scope-terminal lifecycle mount; open/instance bodies carry their spec'd fields. Stub dataprocessing silent no-op bug fixed along with the test that pinned it. probe_cancel gap in both executors folded into the svc-executors packet. Staged.
- svc-producers DONE: 12 FIXED (incl. dup pair) / 2 REASSIGNED (2363 batch-lease identity — needs a proto lease token + runtime consumption, queued with 1699 for the lead's cross-cutting close-out packet; 1826 sql-checks dedup — folded into the running svc-executors packet, which owns the other copy). Highlights: filesystem claim ids and list-array keys sanitized against path traversal; sentinel-before-payload crash-orphan window closed with self-cleaning; ledgers never evict OPEN records (grow-over-cap beats silent claim loss); postgres staged_async no longer silently falls back in-place; infra vs check-failure error classes separated. Also purged a sleep-vs-visibility-timeout wall-clock pattern from three store tests. Staged.
- runtime-c DONE: 13 FIXED + known-RED TestFanoutPostMortem resolved (stale test asserting pre-unification > semantics; expectations updated to the deliberate >= threshold). Highlights: three terminal paths (pass, infra give-up, pure-cascade) now run run-tree propagation + lineage like every other; give-up while holding active claims routes through held+poison-abandon instead of unfiltered failure cascade; fan-out recursion guard narrowed so delegation scopes fan out; co-holder acquisition fails loud; async-ack registry rejects collisions. LEAD FIX: TestSweepRunTreeRetention_TraceTrailingOnly was the runtime-module twin of the shared-scope fixture bug (two frames, one root scope) — migration 029's FK caught it; fixture now seeds a scope per frame, 3x green. Staged.
- svc-sensors-subs DONE: 11 FIXED. Highlights: three object-store watermark correctness bugs (equal-timestamp siblings dropped, later-added lower-sorting names invisible, same-ETag collisions); webhook longest-prefix routing under path_prefix; never-firing cron no longer treated as permanently due; OpenLineage emitter spec-legal (runId = node-run UUID, facets in legal positions), cursor lag-window vs in-flight peer commits, required backend URL, and 4xx dead-lettering without head-of-line blocking. Several tests pinning the old wrong shapes updated. Staged.
- svc-executors DONE: 11 FIXED + 1826 (handed over mid-task — shared checkspec package under lib/services/internal, both consumers swept) + probe_cancel implemented in both executors (claude-agent Execute made context-aware for the first time; conformance cancel passes live, full suite 9/9 claude-agent, 7/9+2 expected skips http-node). Highlights: claude-agent session rebinding fixed on both resume paths; unbounded callback POST client bounded; verifier misconfigurations can no longer be silently downgraded by severity:warning; http-node honors per-dispatch timeout only. Staged.
- cli-cmd DONE: 11 FIXED / 1 ALREADY-FIXED / 1 STALE + both compose flakes fixed at root (the SIGKILL test's 8s wall-clock verdict removed — invariant is process-dead-after-Drain with the suite timeout as backstop; the symlink test's purposeless settle-sleep deleted). Highlights: proxy instance-cache eviction on terminate (was a no-op handler); dispatch delivery no longer blocks the connection lock; compose wait finally uses the documented no-open-frame+no-pending-message idle rule (zero-run nodes settle; naive per-node fix would have raced instance-create-is-idle); shared template hash survives sibling tag removal; startup helpers deduped across all six binaries; agent stop escalates honestly; CLI config 0600. One agent process violation (path-scoped git stash) — self-restored, stash list empty, nothing lost. Staged.
- GATE FIX LIST addition: cmd/rimsky-host-agent-proxy TestProxyPassesExecutorConformanceSuite fails the two NEW kit scenarios (async_callback_survives_restart forced-interruption, scratch_park_round_trip) — the proxy conformance harness's stub child needs the new probe conventions. Dedicated fixer dispatched.
- proxy-conformance fixer DONE: both reds were the shared stub child lagging the tightened kit — probe_async now returns AwaitAsync and retries its callback with backoff (mirroring claude-agent), probe_park echoes scratch (mirroring http-node); the proxy test's local non-retrying probe_async override deleted. Proxy suite 3x green, stub honesty tests added. LEAD FIX: TestStatePropagation_ParkedChildAggregatesAsTerminal renamed/inverted to ParkedChildHoldsParentOpen — it pinned the pre-1562 parked-counts-as-settled semantics runtime-c deliberately reversed. Green. REMAINING RED TO DIAGNOSE AT GATE: test/scenarios/canary hung once to suite timeout amid in-flight config/mcp edits — verify on quiet tree once core-misc lands.
- core-misc DONE: 14 FIXED. Highlights: unified stack opens the blob backend once and shares it (concurrent SetBlobBackend race across roles gone); production supervisor finally wraps its resolver for late-bind proxies (1682 — the config knob existed but production never used it); bundled advertisement no longer clobbers configured remotes; publishers advertising lifecycle_subscriber are dialed in all three roles; audit event kinds derived from the typed constructors with an init-time exhaustiveness guard over the proto enum; CapturingLogger records shared across With children. Staged.
- svc-other DONE: 8 FIXED. Highlights: two failure-blind terminal-wait helpers deleted (tests could pass with failed_count>0); shared NodeObservability harness helper swept across 11 duplicated poll sites; overlap tests now assert the REAL all-or-nothing sub-claim rejection (the row's own bug hypothesis was wrong — conflict-check exists and is intentional); cron recovery pinned to the persisted watermark; webhook restart recovery proven at docker-stack level. Services family complete (44 rows). Staged.
- runtime-d DONE: 11 FIXED + the supervisor Shutdown join bug (root cause of the mTLS config flake — verbDispatcher goroutine never joined; 10/10 -race green now) + a second same-class join bug in CallbackServer.Close. Migration 030 adds the persisted per-run changed bit so run-tree aggregation stops hardcoding changed=true. Park no longer emits attribute-changed; running→held no longer double-audits terminal; parked wake targets the exact parked run. Runtime family complete (50 rows). Staged. New same-class flag: TestStartControlAPI_StartsSubscriptionReconciler flaked once with the tempdir signature — StartControlAPI/SubscriptionReconciler join, routed to the cross-cutting fixer.
- design-docs-a DONE: 11 FIXED / 1 ALREADY-FIXED (450 — the lineage walks controlapi-b built). All prose corrections verified against current code; delegation and lifecycle-subscriber docs no longer overclaim (entry absorption is run-level only; registration does not validate delegate targets; instance-terminated delivery is async); named-lock traceability closed with citations at the four enforcement sites. Staged.
- design-docs-b DONE: 8 FIXED / 3 ALREADY-FIXED (all three staleness cases were this session's own sibling fixes). Decisions/stories no longer overclaim: --name unvalidated, four unified-marker setters, stdio transport unit-tested only, denial reason undifferentiated, rules-doc check bounded. Staged (two files the agent had un-staged re-staged by lead; content untouched). Step-6 follow-up additions: acq.Scratch never refreshed from a mid-dispatch callback-persisted scratch write on retry (guard test gap); blob_config unified-marker error text names three setters, doc now says four.
- test-support DONE: 11 FIXED / 1 ALREADY-FIXED-by-deletion / 1 STALE (2131 — DB CHECK makes the nil-holder leak structurally impossible; constraint now pinned by test, reaper nil-check documented dead) / 2106 completed by LEAD (bagsEqual now propagates on-demand resolve errors instead of silently reporting unequal; unit test via erroring template table, green). Highlights: frame-start atomicity test races the REAL open-frame unique constraint again (pre-seeded no-op removed); host-agent disconnect test forces a genuine mid-dispatch disconnect and pins the specific error class; several vacuous/failure-blind assertions hardened; two dead test files deleted. All 21 step-3 packets complete. Staged.

## Step 3 — gate + cleanup
- Ledger stamped from 23 per-packet outcome files: 249 rows → CLOSED (FIXED/ALREADY-FIXED) or STALE, each with verdict+detail+packet; 0 OPEN. Phase-3 tally 420 CLOSED / 15 STALE. The ~2,150 blank rows are the step-5 minor/nit tail.
- Gate: lint green (5 modules); foundation+protocols green; services build green; root `go test ./...` surfaced 5 failures, all resolved on the quiet tree:
  1-2. held_abandon/held_commit cascade tests pinned the double-audit that ratified fix 1637 removed (concept:signal — held moment emits NO terminal audit, member cascade retained) → tests updated; inheritor exactly-once cascade still holds.
  3. TestRetentionSweepsReapOnTick REAL regression: migration 029's RESTRICT FK made the prune's unconditional root-scope delete violate the FK when a surviving frame shares the scope, rolling back the WHOLE prune tx (node_runs unpruned, blob orphans un-enrolled). Fixed both dialects: root-scope delete guarded by NOT EXISTS(surviving frame) AND NOT EXISTS(surviving node_run).
  4-5. canary + test/scenarios were NOT load — three real hangs from this session's runtime changes, all fixed:
     - deregister fan-out ran AFTER DeleteByHash (which deletes the lifecycle-idempotency rows it reads) → OnTemplateDeregistered never dispatched; reordered fan-out before the deletes.
     - REFINEMENT of runtime-c row 126: co-holder acquire on a nil upstream claim must soft-skip (continue), not hard-fail — hard-fail spun the candidate stale forever; genuine lookup errors still fail.
     - REFINEMENT of runtime-c row 1580: lookupClaimHandleForAlias must be durable-aware — accept Active OR Committed, and apply the frame filter only to non-durable claims (durable holds are cross-frame by design). One runtime unit test re-pinned to the ratified no-bind-no-error behavior.
- Clean-tree verification (fixer): test/scenarios full 178s PASS, canary PASS, held tests 3x PASS, persistence both dialects PASS, runtime+graph+controlapi PASS, lint+plumbline clean.

## Step 3 — docker-stack gate (round 2)
- Images rebuilt from final source; lint + all 5 module suites green (root scenarios via the quiet-tree fixes above).
- Services docker-stack: 1 regression fixed — atomic_staging TestConformanceClaimProducerTerminalsCLI: fix 143's correct staged_async selector rejection surfaced that the generic conformance kit drove SplitScope/Serialization9b with slash selectors (only "passed" before via the silent in-place lie 143 removed). Fixed by making the kit's two WRITE-intent selectors identifier-safe (valid for both path-shaped filesystem and schema-swap postgres stores); 143 untouched; no image rebuild (kit compiles into the in-test CLI). atomic_staging + filesystem conformance green.
- Examples docker-stack: sub-claim-payload e2e hangs (real, reproduced in isolation) — a step-3 runtime change re-broke the fan-out child sub-claim attach/payload path that was fixed earlier this session (reuseLinkedSubClaim). Prime suspect: the cross-cutting lease-token threading through runner_subclaim.go / filesystem findByScope. Fixer dispatched with live-inspection playbook + image-rebuild instruction. COMMIT HELD until this is green.
- sub-claim-payload e2e FIXED: root cause was a downstream consequence of fix 1637 (no-double-audit held transition) — held fan-out children now emit their single terminal/success only at deferred settlement (transitionHolderIfFullyResolved), which built the signal with a NIL attributes_delta, so the children's substituted processed_value never reached the event the test polls. Fixed by loading the holder's persisted final attributes onto that one settlement audit (respects no-double-emit; no revert). core-images rebuilt; e2e green 2x; held-cascade + retention scenario tests re-confirmed green.
- Round-2 docker re-run vs rebuilt core image: examples ALL green (sub-claim 5.5s). Services: 1 new red — TestSubscriberOpenlineage hits its 60s arrival deadline (real, reproduced in isolation ~65s; subscriber not emitting an awaited event). Suspect: held-settlement-attributes interaction with leaf-run/claim-terminal emission, or this session's openlineage lag-window/runid/dead-letter changes. Fixer dispatched (live diagnosis + rebuild). COMMIT STILL HELD.
- TestSubscriberOpenlineage FIXED (test-only): stale-image trap. Row 1959 changed the OL runId from instance-keyed to the node-run UUID; the svc-other packet's earlier pass ran against an un-rebuilt subscriber image with the OLD instance-keyed runId, so its HasPrefix(runId, instanceID) assertion passed. Against the freshly rebuilt image the assertion is unsatisfiable (node-run UUIDs are random). Live evidence confirmed rimsky emits correctly + subscriber carries the rimsky-side IDs faithfully; assertion corrected to membership-in-leaf-run-ID-set (stronger, matches the invariant). No production code, no rebuild. e2e green 2x. This is why the rebuild-before-docker-stack discipline exists.

## Step 4 — dossiers + guards (worktree wave, new discipline)
- Orchestration discipline ratified into the plan (4d2c164f); step-4 runs as the first worktree wave: per-packet worktrees/branches, single commit each, lead rebases in finish order.
- dossier-reconcile DONE + INTEGRATED (ba83d75b, clean ff): 5 rows + 2 queued discoveries reconciled with code-level verification. Row 2413's premise was WRONG — child_key IS recovered on async settle but graph_name is NOT (AsyncContext has no field), so graph-scoped breakpoints never match async-settled after_terminal; reality pinned by guard test, dossier corrected, code fix queued as its own packet (add GraphName to AsyncContext → driveTerminal breakpoint eval). Backfill legs struck across 5 dossiers citing the 06-14 collapse (verified zero backfill code); replica.md in-process-mutex premise corrected (flock landed 06-13); typed-Topology marked DELIVERED in both dossiers.
- guard-with-change DONE + INTEGRATED (346f1929): 2388 already-guarded (coverage proven across 3 owning-packet tests); 2419/2533/2555 guards added. Real bug found: publisher.proto retired target_node field 5 lacked a reserved declaration (only such gap on the proto surface) — reserved + regenerated + guarded.
- image-freshness machinery DONE + INTEGRATED (1d3fdeae): src-<tree-hash> tags on all builds (temp-index git plumbing, deterministic, cache-no-op on unchanged tree); harness self-resolves its own tree's tag, zero :latest consumers left, missing image fails in 2s naming the build command; per-agent isolation audited clean; reap-images + check-image-freshness targets; CLAUDE.md updated. Pulled forward from step 6 ahead of the step-5 waves.
- graphname fix DONE + INTEGRATED (00ee5200): AsyncContext carries GraphName from both build sites; guard flipped to assert recovery; no migration (context never persisted).
- LEAD: plumbline sensor fitness test updated for the build-image macro idiom (was grepping literal :$(VERSION) lines); TestReadSpec_NonObject flagged failure did not reproduce in isolation (load artifact). Step-4 ledger rows stamped (9 CLOSED). Wave gate next: images from the new tip, docker legs.

## Step 5 — minor/nit sweeps (worktree waves)
- Mechanical pass: 464 phase1-applied CLOSED, 71 refuted STALE, 57 dup riders CLOSED (incl. critical 1710 via its verified canonical). 1,549 rows → 31 by-kind packets.
- Integrated so far (linear, one commit each): structure-a 4eaf05ae, behavior-a 0c5d87ea, behavior-b b32d5dfc, structure-b bb0f0a6c, behavior-c c486202d, reap-fix 9f6644dd (test-only map-iteration bug + wall-clock verdict purge; production concurrent reap exonerated), structure-c 18e1483d. In rebase: behavior-d (registry-mutex→namedRegistry composition), structure-d queued behind it.
- Real bugs so far: supervisor-panic race on unvalidated RealizedWriteSemantics cast (966); ListPendingForInstance LIMIT 1 truncating multi-message results BOTH drivers (994/1153); two dry-run coverage defects inside behavior-b's own commit (caught+fixed at structure-b's rebase); migration 032 drops dead claim_holders.frame_id.
- Step-6 follow-up queue additions: LifecycleRegistry relocation (979); repo-wide tx-param position idiom (1005); X/XInTx pair collapse (1006); DB columns still carrying store vocabulary (schema migration).
- Step-5 close-out design question (surface to user at step 6): sensor emission path retries 403/404 forever like 5xx; the dossier's drop-observation-on-4xx policy is artifact-only, never reaffirmed by ruling. Needs a call: dead-letter/drop on permanent rejection vs retry-forever.

## Step 5 — close
All 31 packets integrated (one commit each, linear): structure a-f, behavior a-f, tests a-h, misc a-g, dead-code a-b, naming-docs a-b, + reap-fix. Ledger stamped: 1,484 CLOSED + 65 STALE this step; TOTAL LEDGER NOW TERMINAL — 2,434 CLOSED / 151 STALE / 0 OPEN / 0 blank (2,585 rows).
Real production bugs found by the sweeps (selected): supervisor-panic race on unvalidated write-semantics cast; ListPendingForInstance LIMIT 1 truncation (both dialects); egress redirect header leak; cron missed-window replay storm; sensor last_poll_at liveness dead on restart + never restored; non-UTF8 webhook body corruption; pure-cascade after_terminal breakpoint never fired; two Shutdown TOCTOU races (supervisor + scheduler); CANCEL-before-registration race; delegate settle wait-set gap; parked_at not cleared on resume; holds-as validation keyed wrongly; migration-036 index-resurrection defect caught at integration.
Step-6 follow-up queue (accumulated): LifecycleRegistry relocation (979); tx-param idiom (1005); X/XInTx collapse (1006); DB store-vocabulary columns migration; payload-map triplication (1276); giant validator test split (1386); Tables() not-found idiom (984); multi-replica lifecycle row lock; outbox diagnostics API; 2218/2219 latent items; tryClaim concurrency gate (1668 half); fanout dir placement; ExecutorVersion dead field; sensor 4xx-retry design question (user ruling needed).
Gate next: lint + module suites + images + docker legs.

## Step 5 — gate close
Gate run on the integrated tip: lint green; root/foundation/protocols suites green; all three image sets rebuilt per tree hash. Docker legs surfaced 7 reds (6 named + sibling), all resolved:
- REAL PRODUCT BUG: filesystem claim-producer recycle ring ordered by sentinel mtime; Docker VirtioFS floors utimensat to whole seconds, so recycled entries sorted to ring FRONT and the ring never rotated. Fixed with explicit int64 ring position stamped in sentinel content (pick_policy.go stampRingPosition/ringPosition; admin bump-to-head writes 0); unit test pins mtime-independence. Smoke test also rewritten off the settle-racing invalidate_node driver onto empty-wake cycles with committed-row polling.
- TEST FIXES (pinned stale/vacuous behavior): cli_example_spec took only first README run-invocation regex match; idempotency e2e used the wake-posting create helper against the create-is-idle baseline; executor dispatch-context e2e demanded prior-linkage across frame boundaries against story ratification (fresh context per frame; also fixed getWorkerDispatchesInOrder ORDER BY ambiguity); verifier registration e2e pinned superseded {service_name,role} wire shape + statically-unreachable probe (now proves validator RPC runs via unknown_check_kind class); sensor cascade e2es waited fresh on a never-runs bystander (vacuous under old helper, deadlock under WaitForNodeSettledTo).
- Examples validation e2e: findingsToProjection dropped the machine-readable class from rejection bodies; class now projected (templates.go).
Full re-run vs final-tree images (src-b0c7d9eb3a8e): services leg ALL green, examples leg ALL green. Step 5 CLOSED.

## Step 6 — follow-up wave + gate
Five worktree packets integrated linearly (structure-misc 9fd4c0e9, outbox-latent 38e9f097, persistence-idioms fa8b24f4, test-runner fc3a1849, lifecycle-runtime 3696c79e). One rebase conflict set (lifecycle-runtime × tx-sweep, 3 files) resolved by the original agent composing both intents.
Wave gate: lint green all modules; foundation/protocols/services-unit green; full root suite green; BOTH docker legs green incl. the new in-stack runner path (instack + instackdriver). Two gate fixes found at the gate:
- Cross-packet semantic conflict: harness pinning test inserted into required_stores, renamed to required_claim_producers by migration 037 (test/support/scenario/harness_test.go).
- REAL harness race, deterministic under full-suite load: hostagent fakeProxy closed its connected gate on Register receipt, BEFORE sending RegisterAck — a test frame could overtake RegisterAck, be consumed by connectOnce's strict handshake as a protocol error, and be dropped on the abandoned stream; the test then blocked forever awaiting an ack for a frame the agent never processed (TestSpawnReadyCapabilitiesHandshake 10m timeout, reproduced 2/2 full-suite runs). Fixed by closing the gate only after RegisterAck is sent (HTTP/2 stream ordering then guarantees the agent completes the handshake before any test frame); also removed the pump's silent default-drop on a full frame channel. hostagent green -race -count=3 and in two subsequent full-suite runs.
