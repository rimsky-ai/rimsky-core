{"id": "246", "cluster": "conformance", "verdict": "UPHELD", "evidence": "2026-05-20-userdata-collapse-into-attributes (artifact): 'concept:userdata retires; @blessed-invariant 11 retires; concept:attribute absorbs the unified surface'; executor.proto reserves the field", "note": "Five independent design-corpus artifacts confirm userdata was retired into attributes with no migration shim, and no transcript search surfaced any owner reversal. The reworded 'stub-probe flag in the request attributes' matches both the retirement decision and the shipped code."}
{"id": "247", "cluster": "conformance", "verdict": "UPHELD", "evidence": "2026-05-12-nomenclature-resolution (artifact): 'the probe sidecar rimsky-conformance-probe stays generically named because it is not protocol-specific and could extend to other protocols'", "note": "The 'protocol-agnostic' doc wording traces to an aspirational naming rationale ('could extend to other protocols') that was never built; the shipped probe only ever issues executor Execute RPCs (runConformanceProbe builds a genv1.ExecuteRequest). The edit describes shipped behavior accurately; the aspiration explains the stale wording rather than contradicting the fix."}
{"id": "249", "cluster": "conformance", "verdict": "UNVERIFIABLE-OK", "evidence": "record silent (greps across all-entries.json and raw transcripts for the DataProcessing battery's check names found no owner statement)", "note": "Mechanical doc-vs-runner correction: checkBeginCandidateIdempotent and checkConcurrentWrites are named directly in dataprocessing/runner.go and the edit only renames them correctly. Conservative — matches code reality without inventing intent."}
{"id": "250", "cluster": "conformance", "verdict": "UNVERIFIABLE-OK", "evidence": "record silent (no owner statement on per-role malformed-input coverage in distilled entries or transcripts)", "note": "The validation runner's role switch unambiguously runs malformed-input checks only for the executor role; the edit scopes the doc sentence to match. Conservative code-reality fix, no intent claim added."}
{"id": "251", "cluster": "conformance", "verdict": "UNVERIFIABLE-OK", "evidence": "record silent (greps for 'two shared fixture' / 'fixture packages' found no owner statement)", "note": "The count 'two' has no resolvable referent against the repo's actual fixture inventory (three claim-producer testfixture packages plus the stub-executor fixture). Dropping the unverifiable count is the conservative move; nothing in the record fixes the number at two."}
{"id": "252", "cluster": "conformance", "verdict": "UNVERIFIABLE-OK", "evidence": "record silent on Adjacent-list composition; the doc's own body describes publisher/data-processing/validation conformance and the lifecycle-subscriber escape hatch", "note": "Completes the Adjacent list to match concepts the doc's own body already engages, all of which exist as catalog entries. Fixes an internal self-inconsistency; encodes no new design intent."}
{"id": "253", "cluster": "conformance", "verdict": "UNVERIFIABLE-OK", "evidence": "record silent (greps for 'require-stub-mode'/'RequireStubMode'/'LLM-calling' across all-entries.json and transcripts found no owner statement on probe conditionality)", "note": "executor/runner.go probes unconditionally before consulting RequireStubMode and auto-skips RequiresStub scenarios on probe failure regardless of the flag; the rewrite states that actual causal shape and drops the undefined 'LLM-calling' qualifier. Conservative code-reality fix. Minor caveat: the rewritten invariant also asserts the stub signature is 'hard-coded at each conformance-surface site... not centralized', a decentralization claim resting on code inspection only, but it describes reality rather than inventing intent."}
{"id": "67", "cluster": "inertness", "verdict": "UPHELD", "evidence": "2026-05-04-foundation-contract / 2026-05-22-fan-out-safety-scope-first (artifact): byte-equality named as the conflict primitive — 'single-writer-per-scope; overlap is producer-defined, byte-equal as the trivial default'", "note": "The byte-equality conflict predicate over claim-scope bytes is a deliberately designed rimsky-side mechanism throughout the corpus since the 2026-05-04 foundation contract, so the exclusive 'only' enumeration was a genuine omission. Anomaly: the finding also flagged the lineage-record HashBytes(ClaimScope) read site, which the edit did not add by name — the new trailing clause 'each owning concept enumerates its own stream's exact sites' defers that enumeration rather than contradicting the record."}
{"id": "71", "cluster": "inertness", "verdict": "UPHELD", "evidence": "2026-05-04-modeling-layer-contract (walkPath returns claim content verbatim) vs 2026-05-21-attribute-overrides-matcher-overlay: 'The read is primitive-equality only; no traversal beyond the named path' — scoped to the matcher site alone", "note": "The record shows the equality-predicate description was always scoped to the one matcher site, never generalized to all sanctioned reads; substitution-leaf verbatim extraction and executor-wire transport predate it and are not equality predicates. The rewrite describing varied read shapes matches the record; the absolutist sentence had no primary support."}
{"id": "72", "cluster": "inertness", "verdict": "UPHELD", "evidence": "2026-05-15-control-plane-mcp-and-auth (artifact): 'Audit records store request_params verbatim, not hashed... the only sensitive value in an auth-relevant exchange is the API key itself, which is in the Authorization header (not stored in any audit record)'", "note": "The primary record frames verbatim storage as a policy justification resting on the bodies-never-carry-secrets premise, not as a logical entailment of the inertness discipline. The edit's 'deliberate policy choice, not a consequence of inertness' tracks the source more faithfully than the pre-edit over-claim."}
{"id": "300", "cluster": "inertness", "verdict": "UPHELD", "evidence": "same record as ruling 67: byte-equality conflict primitive designed in from 2026-05-04-foundation-contract; conflict.go + ScopesConflict RPC are corpus-confirmed rimsky-side reads", "note": "Same underlying edit as 67. No record evidence the owner intended the conflict comparison excluded from sanctioned byte-opaque reads — the opposite: it is named as the core conflict primitive from the foundation contract onward."}
{"id": "301", "cluster": "inertness", "verdict": "UPHELD", "evidence": "2026-05-19-multi-instance-template-ergonomics (artifact): path-walking substitution kinds admit 'an optional-empty trailing path', including claim payload fields", "note": "The corpus explicitly treats claim-payload substitution as a path-walking source kind that resolves nested JSON structure, directly contradicting a literal 'never traverses the bytes at all'. Softening to interpretive opacity ('treats the bytes as meaningless outside the enumerated sanctioned sites') is well-supported."}
{"id": "423", "cluster": "inertness", "verdict": "UPHELD", "evidence": "3f71f90a 2026-07-06/2026-07-13 (transcript): coalesce 'cancels ALL pending messages per instance' and terminate must cancel pending messages; delivery is instance-scoped per schema", "note": "instance_id-scoped delivery and the cancelled/coalesce mechanism are load-bearing, transcript-confirmed behavior; nothing in the record marks the routing-field list as deliberately excluding them. Completing the illustrative list is a safe, supported fix; the payload-inertness invariant is untouched."}
