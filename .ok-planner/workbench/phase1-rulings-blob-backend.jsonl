{"id": "15", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "transcript", "sources": "dossier=Required behaviors bullet 1 (2026-06-14, 752fe200, transcript: 'the same spill discipline for named-event payloads ... parked payloads, and scratch'); doc=What it it said 'one surface: attribute values'; code=lib/runtime/executor/scratch_writer.go, runner_terminal_scratch.go, callback.go spill scratch via ShouldSpillBlob/Blob.Write; test=concept:node-run doc line 15 states scratch spill follows concept:blob-backend", "note": "Dossier transcript entry establishes blob-backend explicitly covers attributes, event payloads, parked payloads, and scratch, not attribute values alone. Doc's 'one surface' claim was stale. Edited What-it-is to name all four surfaces.", "applied": true}
{"id": "173", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "dossier=silent on userdata (not mentioned anywhere in the dossier); doc=Boundaries carved out 'userdata (always inline)'; code=lib/protocols/proto/v1/executor.proto:38-45 and validation.proto:45-49 explicitly reserve the field and name with a comment stating userdata is retired and template-supplied values now flow through attributes; rg finds no non-generated, non-test Go code carrying userdata", "note": "The proto's own reservation comment is an independent, authoritative artifact confirming userdata no longer exists as a concept; the doc's boundary carve-out pointed a reader at a nonexistent noun in violation of current-state-only prose rules. Removed the userdata clause from Boundaries.", "applied": true}
{"id": "174", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "transcript", "sources": "dossier=Required behaviors bullet 1 (2026-06-14, 752fe200, transcript) establishing scratch as a blob-backend-covered surface; doc=Invariants line enumerated only 'the substitution path-walk leaf and ... the persistence-layer fetch on read'; code=lib/runtime/runner_acquire_helpers.go:175 loadScratchIntoAcquisition calls args.Blob.Read to rehydrate spilled scratch at dispatch acquisition, a third read site; test=concept:node-run.md line 15 corroborates scratch spill under concept:blob-backend", "note": "Once scratch is recognized as a covered surface (row 15), its dispatch-acquisition read site is a load-bearing sanctioned read site per concept:inertness's 'sanctioned read sites are precisely enumerated' rule. Added the runtime-layer scratch load to the enumeration.", "applied": true}
{"id": "175", "classification": "defect", "direction": "fix-doc", "verdict": "CONFIRMED", "intent_provenance": "corroborated", "sources": "dossier=Corrections/Required-behaviors: 'Backend-mismatch reads fall back to the inline data column rather than erroring — continuity over strictness, an accepted silent storage downgrade for that row (2026-05-08, notes, artifact-only)'; doc=Invariants line said 'cross-prefix reads fail'; code=postgres/node_attributes.go:57 falls through to the inline {} placeholder with no error/log on backend-name mismatch; lib/runtime/runner_acquire_helpers.go:168-173 logs a warning and passes empty scratch on mismatch", "note": "Dossier note is artifact-only tier but is independently corroborated by two separate code sites (persistence-layer attribute read and runtime-layer scratch read) both implementing the exact fallback-not-error behavior it describes, and the position is design-coherent (graceful degradation), so no suspect-canonization flag applies. Doc's 'cross-prefix reads fail' directly contradicted this; rewrote the invariant to describe the fallback-to-inline degradation instead.", "applied": true}
