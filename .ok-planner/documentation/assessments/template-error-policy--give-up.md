---
assessment: template-error-policy--give-up
subject: story:template-error-policy
way: give-up
release: d977250c
outcome: held
warrant: experiment:template-error-policy
---
# Declaring that an error class should end the run immediately

Under the give-up action, over the same deterministic failure and with nothing else changed between the templates, the run settled failed. The declaration alone decides the outcome, so an author expressing "this class is not worth another attempt" gets exactly that, with no handler written.

## Unverified remainder

None: the passing run demonstrates the way as promised.
