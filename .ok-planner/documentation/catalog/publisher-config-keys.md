---
kind: publisher-config-keys
release: d977250c
population: 20
---

- `cron: cron` — public under: template keys; not driven by any measured story way
- `http: match.jsonpath.path` — public under: template keys; not driven by any measured story way
- `http: match.jsonpath.value` — public under: template keys; not driven by any measured story way
- `http: match.status` — public under: template keys; not driven by any measured story way
- `http: poll_interval` — public under: template keys; not driven by any measured story way
- `http: url` — public under: template keys; not driven by any measured story way
- `object-store: backend` — public under: template keys; not driven by any measured story way
- `object-store: bucket` — public under: template keys; not driven by any measured story way
- `object-store: poll_interval` — public under: template keys; not driven by any measured story way
- `object-store: prefix` — public under: template keys; not driven by any measured story way
- `object-store: settle_window` — public under: template keys; not driven by any measured story way
- `object-store: watermark_field` — public under: template keys; not driven by any measured story way
- `webhook: auth.header` — public under: template keys; not driven by any measured story way
- `webhook: auth.mode` — public under: template keys; not driven by any measured story way
- `webhook: auth.replay_window_seconds` — public under: template keys; not driven by any measured story way
- `webhook: auth.secret` — public under: template keys; not driven by any measured story way
- `webhook: auth.signature_header` — public under: template keys; not driven by any measured story way
- `webhook: auth.timestamp_header` — public under: template keys; not driven by any measured story way
- `webhook: idempotency_header` — public under: template keys; not driven by any measured story way
- `webhook: path_prefix` — public under: template keys; not driven by any measured story way
