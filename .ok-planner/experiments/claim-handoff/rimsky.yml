persistence:
  driver: sqlite
  sqlite:
    path: /var/lib/rimsky/state.db

claim_producers:
  "stage-store":
    endpoint: "grpc://host.docker.internal:19441"
    write_semantics_allowed: ["sync"]

named_locks: {}
executors: {}
