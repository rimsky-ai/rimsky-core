persistence:
  driver: sqlite
  sqlite:
    path: /var/lib/rimsky/state.db

claim_producers:
  "asset-store":
    endpoint: "grpc://host.docker.internal:19481"
    write_semantics_allowed: ["sync"]

named_locks: {}
executors: {}
