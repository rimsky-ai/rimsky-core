---
story: one-shot-to-terminal
---

# Operator drives a compose manifest to terminal in one invocation

## Story

As an operator with a compose manifest, I can drive its declared instances to terminal state with one invocation — the invocation that starts the run is the one that finishes it, with nothing to stand up first and nothing to tear down after — so that I run orchestrations on a machine without provisioning rimsky infrastructure.
