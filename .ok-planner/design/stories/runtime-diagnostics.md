---
story: runtime-diagnostics
---

# Operator inspects runtime wedge state

## Story

As an operator, I can inspect the parked nodes, the pending wake dependencies, the frames a holding sub-graph is gripping, and the current holders of a claim, so that I see why the runtime is wedged when an instance isn't progressing — without ad-hoc database spelunking.
