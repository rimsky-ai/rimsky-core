---
story: service-tls-enforced
---

# Operator enforces TLS on service connections

## Story

As an operator who configures TLS as required on a service (executor or store), I get a TLS-verified connection to that service — and a loud failure if the service cannot present credentials — so that the TLS config key means what it says.
