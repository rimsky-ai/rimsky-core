---
story: host-daemon-anonymous-mode
---

# Late-bind works under anonymous mode

## Story

As an operator running a fresh anonymous-mode rimsky deployment (no api-keys minted yet) with a host-daemon connected, I can register and dispatch to late-bound services from an anonymous-mode instance, targeting a specific connected daemon, so that the dev-loop works without minting credentials first.
