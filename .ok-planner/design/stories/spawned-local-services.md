---
story: spawned-local-services
status: as-is
---

# Operator declares local executor binaries spawned for a single run

## Story

As a developer or consumer-project author, I can declare local executor binaries that get spawned for a single run and disappear with it, so that consumer projects can ship a binary plus a one-line wrapper instead of a service installer or a long-lived daemon.
