apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rimsky.fullname" . }}-claude-agent
  labels:
    {{- include "rimsky.labels" . | nindent 4 }}
    app.kubernetes.io/component: claude-agent
spec:
  replicas: {{ .Values.claudeAgent.replicas }}
  selector:
    matchLabels:
      {{- include "rimsky.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: claude-agent
  template:
    metadata:
      labels:
        {{- include "rimsky.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: claude-agent
    spec:
      serviceAccountName: {{ include "rimsky.serviceAccountName" . }}
      containers:
        - name: claude-agent
          image: "{{ .Values.bundledServicesImage.repository }}/executor-claude-agent:{{ .Values.bundledServicesImage.tag }}"
          imagePullPolicy: {{ .Values.bundledServicesImage.pullPolicy }}
          env:
            - name: RIMSKY_EXECUTOR_HOST
              value: "0.0.0.0"
            - name: RIMSKY_EXECUTOR_PORT_GRPC
              value: {{ .Values.claudeAgent.service.port | quote }}
            - name: RIMSKY_EXECUTOR_PORT_HTTP
              value: {{ .Values.claudeAgent.service.httpPort | quote }}
            - name: RIMSKY_EXECUTOR_STUB_MODE
              value: {{ .Values.claudeAgent.stubMode | quote }}
            {{- if .Values.claudeAgent.anthropicApiKey }}
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "rimsky.fullname" . }}-anthropic
                  key: api-key
            {{- end }}
          ports:
            - name: grpc
              containerPort: {{ .Values.claudeAgent.service.port }}
            - name: http
              containerPort: {{ .Values.claudeAgent.service.httpPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
