{{- if .Values.claudeAgent.anthropicApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rimsky.fullname" . }}-anthropic
  labels:
    {{- include "rimsky.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.claudeAgent.anthropicApiKey | quote }}
{{- end }}
