name: example
version: "1.0"
frame_resolution_mode: coalesce
params_schema:
  type: object
  additionalProperties: true
nodes:
  - type: hello
    executor: http-node
    userdata:
      url: "http://example.invalid/hello"
      method: GET
    attributes:
      schema:
        type: object
        additionalProperties: true
