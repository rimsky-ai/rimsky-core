name: example
version: "1.0"
frame_resolution_mode: coalesce
params_schema:
  type: object
  additionalProperties: true
nodes:
  - type: hello
    executor: http-node
    attributes:
      schema:
        type: object
        additionalProperties: true
        properties:
          url:    { type: string, default: "http://example.invalid/hello" }
          method: { type: string, default: "GET" }
