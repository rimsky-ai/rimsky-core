{
  "name": "agent-supervisor",
  "description": "Read access across the platform plus the writes a supervisor agent realistically needs: reset (recover failed nodes) and message-send (typed messages, including invalidate via a template-declared messages: type, plus other coordination kinds).",
  "permissions": [
    { "action": "*:read" },
    { "action": "node:reset" },
    { "action": "message:send" }
  ]
}
